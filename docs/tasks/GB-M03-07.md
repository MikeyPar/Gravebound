# GB-M03-07 - Successor preset, starter kit, and return to control

**Status:** In progress - the three-authority contract is fixed by accepted [`SPEC-CONFLICT-031`](../spec-conflicts/SPEC-CONFLICT-031-m03-successor-recovery-authority.md). Append-only protocol `1.17`, reliable successor kind `23`, `core_successor_v1`, migration `0060`, forward-only relation guard `0061`, ordinary-death preset/reservation authority, the transaction-local exact `04D` starter seam, the replay-first atomic successor writer, and the authenticated fail-closed service are green under hosted run `29585161647`. Reliable dispatch, response-loss support, the disabled normal runtime, the persistent PostgreSQL/real-QUIC journey, and bounded feature-absent/malformed/oversized ingress proof are locally green. Cumulative hosted execution, OS child-process restart, native two-confirmation handoff, metrics, and audit remain route-gated.

## Objective

Turn one committed normal death into exactly one legal living successor without reconstructing death-time authority, duplicating identity or starter grants, exposing a partially initialized character, or interrupting recovery with commerce. The successor must reuse the dead hero's immutable Core preset and roster slot, enter Character Select preselected, and return to Hall/control in no more than two confirmations.

## Three design authorities

1. `Gravebound_Production_GDD_v1_Canonical.md`: `DTH-020`, `DTH-021`, `UI-007`-`009`, `TECH-021`-`023`, and `QA-101` require the primary Create Successor path, last class/appearance preset, nonplayable dead identities, at most two confirmations, no commercial interruption, durable retry, and measured recovery.
2. `Gravebound_Content_Production_Spec_v1.md`: `CONT-CATALOG-003` fixes the exact Grave Arbalist starter equipment, provenance, Red Tonic unit identities/placement, empty slots, and new-instance requirement.
3. `Gravebound_Development_Roadmap_v1.md`: `GB-M03-07` plus the M03 no-duplicate, death-to-control, successor-to-combat, 25-loop, and private-loop gates own successor recovery and its evidence boundary.

Accepted [`SPEC-CONFLICT-031`](../spec-conflicts/SPEC-CONFLICT-031-m03-successor-recovery-authority.md) fixes universal preset persistence, former-slot reservation, atomic starter reuse, durable identity/replay, selected-character publication, and the two-confirmation handoff without weakening accepted identity, item, death, or terminal contracts.

## Durable contract

- Every normal death stores one immutable successor preset inside the death transaction: account/death/character binding, former roster ordinal, class ID, typed Core base-silhouette snapshot, content revision, and canonical preset hash.
- The latest unconsumed death reserves its former ordinal. Ordinary Create cannot steal it; live roster selection remains read-only with respect to the reservation.
- One serializable writer creates the successor and every required aggregate participant, initializes the exact `04D` starter plan inside the same transaction, consumes the reservation, selects the successor, and persists immutable result/receipt/audit/outbox rows.
- The new character starts at level 1, XP 0, full 120 health, safe Character Select state, no Oath/Bargains, empty Armor/Charm/backpack/CharacterSafe/Belt slot 2, exact starter Weapon/Relic, and two distinct Red Tonic units in Belt slot 1.
- The dead identity stays archived with null playable ordinal; Memorial/Echo/history, account-safe currency, Vault, and all other living characters are unchanged.
- Exact retry returns the stored result before current validation. Altered mutation reuse, changed death, foreign account, stale content, consumed/superseded reservation, corrupt result, or slot conflict fails closed with a typed audited outcome.

## Append-only protocol contract

- Advance protocol `1.16` to `1.17`; preserve all existing bytes and append reliable bounded successor message kind `23` under `core_successor_v1`.
- The client supplies only a server-issued death ID plus a nonzero mutation ID and current negotiated revision. It cannot author the successor identity, ordinal, name, class, appearance, starter plan, item UIDs, provenance, selection, or aggregate versions.
- The result carries stored death/successor/receipt IDs, former ordinal, class/base silhouette, starter item UIDs, exact post-aggregate versions, selected-character identity, content revision, canonical result hash, and typed fresh/replay/conflict/rejection status.
- Feature absence, normal-route admission, malformed/oversized frames, wrong reliable channel, foreign selection, and unsupported revision fail before repository access.

## Native recovery contract

- Create Successor is enabled only from a durably acknowledged terminal summary with negotiated authority; it remains disabled while loading/pending and absent from Memorial.
- Keyboard, pointer, and gamepad activate the same semantic command. One exact request remains retained while pending; retry resends identical bytes.
- Stored success enters Character Select with the successor preselected. Play is the second confirmation and reaches Hall/control without rename, store, promotion, or a duplicated transition.
- The surface preserves accessibility focus, reduced-effects parity, minimum 1280x720 layout, and the existing death-summary hierarchy.

## Implementation order

1. Publish this task and accepted `SPEC-CONFLICT-031`; keep normal route admission disabled.
2. Add protocol `1.17`, kind `23`, bounded DTOs, feature negotiation, compatibility fixtures, and malformed/oversized rejection.
3. Add migration `0060` for immutable universal presets, successor reservation/result/receipt/audit/outbox, unique death/mutation identities, and append-only guards.
4. Extract a transaction-local starter insert seam still driven exclusively by `StarterItemPlan`; preserve the ordinary identity/backfill behavior until its own cleanup is independently green.
5. Implement one serializable successor repository and server service with account/death/slot lock order, replay-before-current-state, rollback injection, and typed failures.
6. Add authenticated disposable real-QUIC dispatch, response-loss/restart/concurrency evidence, and leave the normal endpoint absent.
7. Integrate the native terminal action, preselected Character Select transition, Play-to-Hall control, input/accessibility behavior, and optimized visual matrix.
8. Record 25 recovery journeys, death-to-control and successor-to-combat timings, zero residue, and the three-authority audit before parent route admission.

## Required verification

- Protocol: pin all `1.16` bytes; append only kind `23`; reject absent feature, malformed/oversized/zero IDs, wrong channel, and foreign death before writes.
- Domain: copy the exact committed class/base silhouette/former ordinal; derive a new character and four new starter UIDs; exact kit/provenance/slots; level 1/XP 0/full health/no Oath/Bargains.
- PostgreSQL: fresh success, byte-identical replay after response loss/pool/process restart, altered conflict, concurrent same-death writers, separate account/death writers, reserved-slot enforcement, foreign/nonfinal/already-consumed/content mismatch/corrupt result/database outage/serialization retry.
- Atomicity: inject rollback after character, progression, world, life, Oath, inventory/items, selection, result/receipt, audit, and outbox families; every branch writes all or none.
- Real QUIC: lose response then replay stored result, restart server, reject normal endpoint, and finish with zero connection/actor/task/transaction/lock residue.
- Client: disabled before durable acknowledgement and in Memorial, one pending frame, exact retry, no replay animation duplication, preselected Character Select, Play-to-Hall, no commerce before control, and pointer/keyboard/gamepad focus parity.
- Integrated: 25 death-summary-to-successor-control journeys; median under 15 seconds, p95 under 30 seconds; at least 70% enter successor combat within 120 seconds; no duplicate identity, grant, selection, receipt, or outbox result.

## Exclusions

Editable names/epithets, production appearance entitlements, retirement, Requiem encounters, public/party routes, telemetry export, support tooling, hosting/platform actions, Core promotion, production namespace cutover, and M04+ content.

## Current Next Step

Run the cumulative authenticated successor target in the mandatory hosted PostgreSQL gate. It now covers real ordinary death, deliberate response loss, reconnect, byte-identical replay after route/pool reconstruction, altered mutation reuse, one exact selected starter graph, a normal feature-absent endpoint, codec-invalid bounded input, the exact 64 KiB+1 ingress boundary, authority non-entry, bounded sessions, and awaited endpoint teardown. After that passes, add the separate OS child-process restart and then the native two-confirmation handoff. Do not enable Create Successor or normal Play before those gates pass.

## Implementation Record

Protocol `1.17` appends `SuccessorCreateFrame` at kind `23` and the nested reliable stored/rejected result without renumbering any prior message or account-error discriminant. The request carries only mutation/death identity, bounded content revision, and canonical payload hash; the result owns class/base silhouette, former ordinal, selected successor, four distinct starter UIDs, positive aggregate versions, receipt, and canonical result hash. Focused protocol tests pass 76/76, strict protocol Clippy passes, and the complete workspace compiles across all targets. Normal servers do not advertise `core_successor_v1`.

Migration `0060` adds immutable universal presets, explicit Active/Consumed/Superseded former-slot reservations, result/receipt/audit/conflict/outbox history, reserved ordinary-Create rejection, and independent deferred closure triggers. The durable death writer now locks the account then active reservation, inserts the exact Core preset for ordinary death, supersedes only an older normal reservation, and leaves incident/admin authority untouched. The hosted fixture covers exact storage, replay, later-death supersession, both nonordinary provenances, concurrent convergence, and failpoint rollback at the new preset and reservation boundaries. Local evidence is 228/228 persistence tests, compiled PostgreSQL fixtures, strict persistence all-target Clippy, formatting, content validation, and diff hygiene; no authorized local PostgreSQL endpoint exists, so hosted execution remains mandatory before this slice is called green.

Hosted run `29562924996` applied all 60 migrations and passed the foundation, extraction, and Recall PostgreSQL suites before the durable-death fixture exposed a PostgreSQL row-shape defect in the shared successor child trigger. The repair now branches on the concrete table before referencing relation-specific `NEW` fields and pins that split in the migration source contract. The same run exposed a non-Windows `unused_mut` lint in the prior DX12-default change; that cfg-sensitive binding is independently corrected so the next exact main commit can exercise the complete hosted matrix.

Hosted run `29565618431` passed strict Linux formatting/lint/tests/content, the optimized Windows release, the mandatory PostgreSQL gate, and all 5 corrected durable-death tests. Its supplemental death-branch evidence step then correctly rejected a legacy fixture that attempted ordinary character creation after the predecessor death had opened an Active successor reservation. The fixture now models the legal production sequence: roster ordinal 2 exists before the predecessor dies, remains selectable while ordinal 1 is reserved, and enters danger only after the committed death. Production `successor_resolution_required` enforcement remains unchanged.

Hosted run `29570619518` exercised that legal chronology and exposed only a fixture decode-width mismatch: PostgreSQL `characters.level` is `INT4`, while the evidence tuple requested `i16`. Commit `ed87bf9` binds the row to `i32` without changing production authority. Hosted run [`29571991103`](https://github.com/MikeyPar/Gravebound/actions/runs/29571991103) then passed the complete strict Linux suite, optimized Windows release, all migrations and PostgreSQL transaction tests, authenticated death/Echo branch and frame-source generation, and optimized standard/reduced-effects native evidence capture.

The starter initializer now has one transaction-local persistence seam driven by the same validated `StarterItemPlan` material used by ordinary `04D` creation. Its standalone wrapper retains bounded serializable retries, commits fresh grants, and rolls exact replay back read-only; a larger successor transaction owns its own commit/rollback decision and can reject any unexpected initializer replay. All 228 persistence library tests, the focused starter contract test, strict all-target/all-feature persistence Clippy, formatting, and diff hygiene pass locally.

The successor repository is replay-first under the account lock, re-derives server-owned successor/receipt/four-item identities, locks the immutable death preset and exact reservation, rejects consumed/superseded/foreign/content/slot/cross-domain mutation authority, and writes every new aggregate plus selection and immutable result/receipt/audit/outbox before consuming the reservation. Persistence independently re-derives both domain-separated starter hashes, every exact starter UID, and every creation-ledger identity; the stored request and result hashes are byte-proven against protocol `1.17`. The identity adapter preserves PostgreSQL's exact active-reservation diagnostic as typed `SuccessorResolutionRequired`, while unrelated database failures remain unavailable. The hosted target injects rollback at character, progression, world, life, Oath, starter item, result, receipt, audit, outbox, and reservation families; it also requires typed ordinary-Create rejection with zero residue, one concurrent fresh winner, exact replay after process/pool restart, altered-payload conflict audit, and the complete selected successor graph. Local evidence is 234/234 persistence tests, 249/249 server tests, the full workspace suite, strict workspace all-target Clippy, the compiled and strict-lint-clean `postgres_successor` target, all content validators, current schemas, optimized Windows release construction, formatting, and diff hygiene. CI now runs that target in the mandatory PostgreSQL gate; hosted execution remains required before server transport integration.

Hosted run [`29579576357`](https://github.com/MikeyPar/Gravebound/actions/runs/29579576357) passed the strict Linux format/lint/unit/content/schema job and optimized Windows release build. Its mandatory `postgres_successor` execution then exposed one exact PostgreSQL `23505`: migration `0033` had already created the canonical zeroed `character_life_metrics` row through `character_life_metrics_v2_initialize`, and the successor writer repeated a non-idempotent insert at the documented repository boundary. The writer now uses `ON CONFLICT DO NOTHING` and immediately reads back and requires exact `(lifetime_ticks=0, permadeath_combat_ticks=0, life_metrics_version=1)` state. All 234 persistence tests, 249 server tests, formatting, and strict workspace all-target Clippy pass with the repair; the replacement hosted run remains the acceptance authority.

Hosted run [`29581539059`](https://github.com/MikeyPar/Gravebound/actions/runs/29581539059) cleared the life initializer and all five prior successor rollback boundaries through the starter family before PostgreSQL `42703` exposed the next schema defect. Migration `0060` attached one insert-window function to result, audit, and outbox rows, but its `NEW.published_at` expression is still resolved against the audit row type even behind a table-name condition. Windows release passed again; the failure is isolated to the successor audit/outbox boundary and requires an additive table-specific trigger split without rewriting migration `0060`.

The authenticated successor service binds account identity only from `AuthenticatedAccount`, derives successor and receipt identities plus the exact four-item `StarterItemPlan` server-side, and never invokes the standalone starter transaction. Fresh and replayed stored projections are both revalidated against namespace, account, mutation, death, request hash, content, derived IDs, selected identity, starter UIDs, versions, appearance, and canonical wire result hash. Altered mutation reuse returns only the typed conflict, ambiguous commit acknowledgement loss returns `UnresolvedMutation`, starter/internal corruption fails closed, malformed input precedes feature state, and normal authority remains disabled. All 255 server tests and strict server all-target Clippy pass locally.

Forward-only migration `0061` follows the migration-`0041` relation-guard precedent: the existing result function is replaced with result-only fields, audit and outbox receive dedicated insert-window functions, and only their two triggers are reattached under the same names. The outbox function alone can resolve `NEW.published_at`; all three retain explicit operation/table guards and exact PostgreSQL transaction-time binding. No row, table, protocol, or migration-`0060` rewrite occurs; schema readiness advances to 61, and rollback requires the documented wipe/restore route rather than running schema-60 binaries against schema 61.

Successor request validation is split into replay-safe structural authority and fresh-current content admission. The repository validates bounded identities, hashes, starter material, and content shape before locking, then looks up the stored account/mutation result under the account lock; only a mutation with no stored result reaches the current-content check. Exact retries can therefore return their immutable stored result across a content-revision deployment, while a new well-formed stale request remains the typed `SuccessorContentMismatch` and malformed authority remains corrupt. Focused tests pin both classifications and the lookup-before-fresh-content ordering.

Hosted run [`29583478039`](https://github.com/MikeyPar/Gravebound/actions/runs/29583478039) applied migration `0061`, exercised every injected successor rollback family through reservation consumption, and reached the successful post-commit graph verifier. It failed only when tuple column 5 requested Rust `i16` for PostgreSQL `character_progression.current_health`, whose migration-`0003` authority is `INTEGER`; the verifier now binds that field as `i32`, matching the already-correct `characters.level` precedent. No migration, writer, stored row, or protocol contract changed.

Hosted run [`29585161647`](https://github.com/MikeyPar/Gravebound/actions/runs/29585161647) passed the corrected successor target end to end: all migrations, 11 rollback families, one concurrent fresh winner plus exact replay, pool/process restart replay, altered-payload conflict audit, reservation consumption, selected-character update, exact aggregate graph, and four-item starter graph. The same exact revision passed strict Linux format/lint/tests/content/schema validation, optimized Windows construction, and supplemental optimized native evidence. The reliable route now accepts an injected `CoreSuccessorIntentAuthority`, maps `SuccessorCreateFrame` only to the nested reliable successor result, and shares one bounded response writer with identity traffic. Normal runtime owns `CoreSuccessorAuthority::disabled()` and still omits `core_successor_v1`; the bot exposes typed request/reply and deliberate response-loss helpers. Local proof is 5/5 bot tests, 255/255 server tests, compiled real-QUIC and hosted safe-inventory targets, strict bot/server all-target Clippy, formatting, and diff hygiene.

The persistent transport target now enters through a real committed ordinary death and `CoreSuccessorAuthority::persistent`, abandons the first bidirectional response after the server has processed the request, reconnects over fresh QUIC for stored replay, closes the original PostgreSQL pool, rebuilds every route authority, and requires the restarted reliable event to encode to identical bytes. The same restarted session sends a valid changed-death payload under the original mutation ID and requires `IdempotencyConflict`; PostgreSQL verification pins one result, receipt, audit, outbox, selected successor, consumed reservation, conflict audit, and the exact Pine Crossbow, Cracked Mark Lens, and two distinct Belt-slot-1 Red Tonic identities/provenance. Endpoint shutdown is awaited and peer database sessions must be idle. The target compiles, strict target Clippy passes, the focused successor QUIC test passes, and all 255 server tests pass locally; runtime execution remains reserved for the authorized hosted PostgreSQL gate.

The adverse reliable-ingress matrix uses the normal handshake without `core_successor_v1` and requires a well-formed successor request to return typed `FeatureDisabled`. It then injects a bounded wire frame whose content revision byte is changed without updating the canonical payload hash and requires codec `InvalidMessage` before the scripted successor authority is called. A separate raw stream sends exactly `RELIABLE_FRAME_LIMIT + 1` bytes and requires bounded transport rejection, again with zero authority calls. Both sessions have 30-second completion bounds and share a 10-second concurrent endpoint-drain verifier. The two focused real-QUIC tests and strict target Clippy pass locally.
