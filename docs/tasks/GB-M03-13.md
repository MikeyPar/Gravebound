# GB-M03-13 - Atomic Fallen Hero Echo persistence and promotion

**Status:** Complete - PASS recorded in [`GB-M03-13-audit.md`](../milestones/GB-M03-13-audit.md) on implementation commit `18dcbad` and hosted CI `29506273492`.

## Objective

Supply the in-transaction Echo projector used by `GB-M03-06C`: determine eligibility from authoritative life evidence, create one immutable Dormant Echo snapshot, and run oldest-first promotion under the same account lock. No Requiem encounter is created in M03.

## Authority mapping

1. `Gravebound_Production_GDD_v1_Canonical.md`: `DTH-001` and `ECH-001`/`002` require eligible death to create a durable Echo with the exact snapshot fields and no reusable item UID/roll.
2. `Gravebound_Content_Production_Spec_v1.md`: `CONT-ECHO-009` requires `none -> Dormant`, oldest-first `(created_at, echo_id)` promotion when no Echo is Available, at most one Available per account, and immutable transition history.
3. `Gravebound_Development_Roadmap_v1.md`: `GB-M03-13` and the M03 exit gate require qualifying death, destruction, memorial, and Dormant Echo to commit atomically or not at all; encounter assembly is explicitly deferred.

Approved [`SPEC-CONFLICT-009`](../spec-conflicts/SPEC-CONFLICT-009-m03-death-memorial.md) makes this projector a mandatory participant in the `GB-M03-06` SQL death transaction rather than an asynchronous consumer.

## Eligibility, snapshot, and promotion invariants

- Eligibility is evaluated from locked authoritative death/life records only: level `>= 10`; `permadeath_combat_ticks >= 18_000`; at least one reward-qualified dungeon boss or two distinct reward-qualified major realm events; and no verified server-incident or administrative death.
- A death can produce at most one Echo, uniquely keyed by `death_id`. Ineligible death returns a typed stored `NotCreated(reason)` projection without inserting an Echo; exact retry returns the same reason.
- Build `EchoRecord` from the committed death snapshot: echo/death/account IDs, approved character-name snapshot, class, optional Oath, level, appearance snapshot/theme, weapon/relic signature presentation tags, ordered active Bargain IDs, deed tags, killer content/pattern, death region, power band, created time, state, and content revision.
- Never copy an item UID, item roll, affix roll, purchased cosmetic entitlement, mutable live pointer, or localized string. Missing/disabled/incompatible content or malformed snapshot aborts the whole death transaction.
- Insert eligible Echo as `Dormant` and append the immutable creation transition. Under the same account lock, if no Echo is `Available`, promote the oldest Dormant by `(created_at, echo_id)` and append `Dormant -> Available`. The newly inserted Echo may therefore commit as Available.
- Enforce at most one Available Echo per account with a database constraint plus aggregate locking. Concurrent eligible deaths for one account serialize and produce deterministic queue order without duplicate promotion.
- Projector failure, uniqueness conflict, invalid eligibility evidence, transition-audit failure, or outbox failure aborts death, destruction, summary, and memorial writes. Publish outbox/telemetry records only as committed transaction rows; external delivery is post-commit.
- Existing Available/Defeated/Archived/Disabled states are not otherwise changed by M03 death projection. Practice death, server-fault restoration, and administrative action never create an Echo.

## Delivery and test checklist

- [x] Add migration-`0031` and forward closure through `0039` with Echo constraints/indexes and immutable transition-audit records without implementing encounter tables.
- [x] Add pure eligibility and snapshot builders with exact 17,999/18,000, level 9/10, boss/event, incident/admin, content, and duplicate-death fixtures.
- [x] Add the transaction-scoped projector consumed directly by `06C`; prohibit an asynchronous creation path.
- [x] Test no Available, existing Available, multiple Dormant with exact equal-time `echo_id` tie-breaking, reachable concurrent eligible deaths, same-account duplicate writers, exact retry, changed payload, restart, corruption, and injected projector/promotion/outbox rollback.
- [x] Add hosted PostgreSQL proof for one Available per account and oldest-first promotion under the same death transaction/account lock.
- [x] Prove repository-built snapshots contain no reusable item UID/roll or unapproved cosmetic entitlement and remain byte-stable across reconnect/restart.
- [x] Publish the `GB-M03-13` audit with `06E` real-QUIC/PostgreSQL closure evidence.

## Exclusions

Requiem allocation, party entry, Practice/Normal encounter execution, Echo combat assembly, rewards, Archive/Defeat UI, Requiem Portal enablement, later promotion triggers outside death, telemetry export, support lookup, and M04+ Echo content.

## Current Next Step

Parent [`GB-M03-03`](GB-M03-03.md) normal integration is active in [`GB-M03-03G`](GB-M03-03G.md) under [`ADR-037`](../decisions/ADR-037-normal-core-private-route-composition.md). Exact content, the capacity-one phase actor, schema-62 generation authority, restart-stable character IDs, mailbox-serialized Bell transitions, and bounded contiguous client delivery are green at exact source `a893acf` under hosted CI [`29624519851`](https://github.com/MikeyPar/Gravebound/actions/runs/29624519851), including Linux quality/content/schema gates, mandatory PostgreSQL coverage, optimized Windows construction, and optimized native evidence. Commit `71005d1` adds the common per-transport server writer: Core responses and Recall pushes share one private sequence space; cancelled, reset, or otherwise uncertain writes poison and close the transport; a generation-racing committed Recall retries on the winning transport; and raw Core sequence seams are no longer public. Strict workspace Clippy, 269 server library tests, 20 non-gated Core QUIC/evidence tests, focused cancellation/reset/handoff races, and content/schema validation pass locally. Run cumulative hosted proof for `71005d1` next, then implement the terminal-first PostgreSQL bootstrap projection and live extraction intent, the dedicated `BoundCorePrivateLifeServer`, and the ordinary native route. Keep normal capability advertisement, Character Select Play, and Realm Gate interaction disabled until live movement, combat, rewards, pending inventory, all terminal owners, restart/25-journey evidence, and visual review pass; keep production namespace cutover, Core promotion, M04+ content, Requiem encounters, paid Oath changes, retirement, purge, and unrelated stations disabled under their independent owners.
