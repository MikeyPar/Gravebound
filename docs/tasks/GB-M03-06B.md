# GB-M03-06B - Lifetime, deeds, cause, and combat trace

**Status:** In progress - the authoritative clocks, contract-1 clock repository/restart head, exact deed authority, cause compiler, ten-second trace, retained repository, serialized server ingestion/restart service, lethal transaction promotion, and hosted adverse execution are implemented. The audit remains open for `06E` integration evidence.

## Objective

Produce the deterministic authoritative inputs consumed by the death transaction: lifetime, permadeath-combat time, reward-qualified deeds, lethal cause, and the final ten-second damage trace.

## Authority mapping

1. `Gravebound_Production_GDD_v1_Canonical.md`: `DTH-001`, `DTH-020`, `ECH-001`, `TECH-015`, and `TECH-023` require the lethal event, prior ten seconds, lifetime/final deed, exact network state, Echo combat duration, and crash-restore behavior.
2. `Gravebound_Content_Production_Spec_v1.md`: the immutable Core content revision, exact Caldus/boss identities, reward-qualified completion records, and `CONT-ECHO-009` state rules constrain the stored deeds and Echo inputs.
3. `Gravebound_Development_Roadmap_v1.md`: `GB-M03-06`, `GB-M03-13`, and the M03 explanation/atomicity gates require stable evidence that survives replay and restart.

Approved [`SPEC-CONFLICT-009`](../spec-conflicts/SPEC-CONFLICT-009-m03-death-memorial.md) fixes the clocks, qualification threshold, deed tie-break, fallback copy, and trace requirement.

## Deterministic simulation contract

- `lifetime_ticks` advances at authoritative 30 Hz only while a living character is controllable in Hall or danger. It includes the vulnerable 90-tick LinkLost window and excludes character select, loading, and offline time. Persist `lifetime_ms = floor(lifetime_ticks * 1000 / 30)` with checked integer arithmetic.
- `permadeath_combat_ticks` advances while living after committed danger entry through death, extraction, or Recall, including staging and vulnerable LinkLost. Crash restoration rolls it back to the value captured at entry. Echo time eligibility is exactly `>= 18_000` ticks.
- Persist reward-qualified completion IDs idempotently. Echo deed eligibility requires at least one dungeon-boss completion ID or at least two distinct major-realm-event IDs during that life; rejected, practice, duplicate, dead, Recalled, or ineligible rewards do not count.
- Add stable deed IDs `deed.core.sepulcher_knight_defeated` and `deed.core.sir_caldus_defeated`. Select the latest by `(achieved_tick, deed_id)`; if no qualified deed exists, use `deed.none` with exact en-US copy `No final deed recorded.` Localization is presentation only; the death row stores the ID.
- Reuse and generalize the existing local ten-second trace behavior: retain ordered authoritative damage entries inside the final 300-tick window, allow stable same-tick entries, and order by `(tick, event_ordinal)`. The lethal entry is always present and uniquely flagged.
- Each entry captures source/killer identity, pattern/attack, raw/final damage, type, pre/post health, fixed-point source position, statuses, and relevant network/Recall state from the authoritative tick. Unknown/nonfinite data, health arithmetic disagreement, tick regression, or inconsistent lethality rejects terminal staging.
- Deterministic cause selection uses the accepted health-zero transition. Equal-tick inputs follow the simulation's canonical event order; neither client arrival order nor localization can choose the cause. The summary's last-five timeline is the final five ordered trace entries, oldest to newest.
- Clock, deed, and trace state participate in danger checkpoint/process-resume records as investigation state only. An unrecoverable crash with no committed terminal transaction restores the danger-entry values and revokes post-entry unsecured gains.

## Delivery and test checklist

- [ ] Add checked 30 Hz lifetime and combat-clock aggregates plus danger-entry/checkpoint/restore integration.
- [x] Add idempotent reward-qualified deed storage and exact Core deed localization.
- [x] Add the bounded ordered trace buffer, lethal cause compiler, last-five projection, and canonical hash fixtures.
- [x] Test Hall/danger/loading/offline/LinkLost boundaries, exact 17,999/18,000 eligibility, extraction/Recall stop, and crash rollback.
- [ ] Test same-tick damage ordering, ten-second eviction edges, more/fewer than five events, invalid positions/IDs/statuses, health mismatch, and deterministic replay across process restart.
- [x] Test boss/event qualification, duplicate completion IDs, latest-deed tie-break, and `deed.none` fallback.
- [x] Pass warnings-denied simulation/content/persistence tests before connecting these inputs to `06C`.

## Exclusions

SQL terminal commit, item destruction, memorial insertion, Echo insertion/promotion, death UI, Memorial Wall, successor creation, extraction/Recall mutation behavior, telemetry export, and route admission.

## Current Next Step

`b930e01` publishes checked 30 Hz clocks, the exact 17,999/18,000 boundary, deed/cause selection, bounded 300-tick trace, last-five projection, and transaction-ready envelope. Schemas `0047`–`0050` persist clock/trace receipts, promotion provenance, and bounded diagnostics. Commits `89e8159`, `6a3c1e6`, and `d2a3db2` carry that evidence through abandoned delivery, full restart, participant rollback, serialization recovery, pool outage, and reconnect. Commit `ac062c1` canonicalizes source/pattern/attack/status IDs, exhaustively closes every enabled Core producer against the `0b39ec1` catalog, and rejects unknown live or resumed trace producers before repository mutation; hosted CI `29442803974` is green. Preserve this binding in the durable-ack presentation reducers, then include it in the `06B`/`06E` signatures and audits.
