# GB-M03-02D - Durable death and memorial persistence

**Status:** Complete - PASS recorded in [`GB-M03-02D-audit.md`](../milestones/GB-M03-02D-audit.md) on implementation commit `18dcbad` and hosted CI `29506273492`.

## Objective

Add the wipeable Core PostgreSQL records and transaction boundary required to preserve a committed permadeath, its combat evidence, deterministic destruction result, exact summary, and memorial across response loss and process restart. This slice owns persistence mechanics, not combat-rule selection, client presentation, successor creation, extraction, or Recall.

## Authority mapping

1. `Gravebound_Production_GDD_v1_Canonical.md`: `DTH-001`, `DTH-020`, `TECH-020`-`023`, and `QA-005` require one durable, idempotent death transaction; immutable trace and memorial evidence; post-commit finality; and restoration after restart.
2. `Gravebound_Content_Production_Spec_v1.md`: `CONT-ECHO-009`, `CONT-HUB-001`, and `CONT-HUB-002` require stable content identities, oldest-first Echo promotion, and a newest-first Memorial Wall backed by the exact stored `DTH-020` snapshot.
3. `Gravebound_Development_Roadmap_v1.md`: `GB-M03-02`, `GB-M03-06`, `GB-M03-13`, and the M03 exit gate require restart-safe death/memorial state and all-or-nothing qualifying death, destruction, memorial, and Echo writes.

Approved [`SPEC-CONFLICT-009`](../spec-conflicts/SPEC-CONFLICT-009-m03-death-memorial.md) is binding: every normal committed permadeath creates one memorial; `death_id` is UUIDv7; `(character_id, "permadeath-v1")` is the final-death idempotency key; and qualifying Echo projection occurs inside the same SQL transaction.

## Persistence contract and invariants

- Migration `0031` is forward-only and additive within the wipeable Core namespace. It may replace existing restrictive checks only to admit the named `dead` life state and `permadeath` destruction reason; it must not renumber existing item security/location values or rewrite migrations `0001`-`0030`.
- Add immutable, namespace-bound records for `DeathEvent`, `DeathSummarySnapshot`, `MemorialRecord`, ordered combat-trace entries, ordered item/material destruction entries, the final-death receipt, and append-only audit/outbox events. Every child row is foreign-keyed to `death_id`; no accepted death row may exist without its one-to-one summary and memorial.
- Store stable typed IDs, fixed-point numbers, ticks, versions, content revision, and presentation keys. Never store localized prose as authority, reconstruct the summary from mutable current state, or copy item rolls into memorial/Echo records.
- Lock the account, character aggregate, inventory aggregate, all owned at-risk item rows, and the Echo account aggregate in one documented order. The transaction changes `Living -> Dead`, advances each affected version once, and commits every named record or none.
- Exact retry returns the stored terminal outcome. Reuse with changed canonical payload is an audited idempotency conflict. A second final death for the same character is impossible even under a distinct mutation ID.
- After commit, commands bound to the former character or inventory versions fail closed. A database error, serialization victim, malformed stored record, illegal at-risk location, or projector failure leaves the character living and the pre-death inventory observable.
- CharacterSafe and Vault rows are read only for the preserved projection and are never destroyed or reassigned. Existing item rows transition to `Destroyed(permadeath)` and retain identity/provenance; they are not deleted.
- Rollback documentation must state that schema `0030` can be restored only after proving there are no dead characters, death/memorial/Echo rows, or `permadeath` destruction records. No automatic lossy down migration is permitted.

## Delivery and test checklist

- [x] Publish migration `0031` and forward closures through `0054`, schema-version updates, constraints, indexes, rollback/recovery guards, and schema-drift fixtures.
- [x] Add typed persistence DTOs and strict validation for all death, summary, memorial, trace, destruction, receipt, audit, and outbox records.
- [x] Add a serializable transaction repository with canonical lock ordering, exact replay, changed-payload conflict, optimistic-version checks, and injected pre-commit rollback.
- [x] Prove immutable-row constraints, one death/summary/memorial per character/death, exact destruction ordinals, preserved safe storage, and no item deletion.
- [x] Prove concurrent lethal writers, response loss, serialization retry, process restart, corruption rejection, unavailable PostgreSQL, and Echo-projector rollback against hosted PostgreSQL.
- [x] Publish the joint `GB-M03-02D`/`GB-M03-06A` audits after real PostgreSQL and real-QUIC evidence is green.

## Exclusions

Cause selection, clocks, deed qualification, live trace capture, terminal arbitration, death-summary rendering, Memorial Wall presentation, Echo encounter assembly, successor creation, extraction, Recall, telemetry export, support tooling, production namespace cutover, and normal-route admission.

## Current Next Step

Parent [`GB-M03-03`](GB-M03-03.md) normal integration remains active in [`GB-M03-03G`](GB-M03-03G.md) under [`ADR-037`](../decisions/ADR-037-normal-core-private-route-composition.md). Exact source `e069987` is green under hosted CI [`29633952947`](https://github.com/MikeyPar/Gravebound/actions/runs/29633952947) for the dormant persistent foundation, generation-safe shared-writer session/Recall ownership, terminal-first bootstrap and schema-63 intent foundations, strict quality/content/schema checks, PostgreSQL transactions, Windows release construction, and optimized native evidence. Commit `26c420b` constructs the dormant persistent foundation, commit `551c527` owns the monotonic per-account transport generation and shared reliable writer with dynamic Recall, and commit `cf933f2` adds the dynamic extraction runtime. Extraction now requires an opaque five-producer executor proof, samples a generation-bound authoritative tick only after command dequeue, publishes one cancellation-safe completion, marks retries/handoffs as replayed, binds Hall installation to the exact transport/terminal/result, retires safely for every competing terminal winner, and reports route/runtime residue without closing the continuing Hall writer. Local formatting, 9/9 extraction-intent runtime tests, 11/11 extraction execution tests, the complete 286-test server suite, all workspace targets, and strict workspace all-target Clippy pass at `cf933f2`; an independent authority re-audit found no remaining runtime blocker. Commit `a30e644` adds the terminal-first process bootstrap adapter and post-commit/replayed world-flow reconciliation: first-time identity defers cleanly; Character Select and storage-blocked states create no route actor; restart danger atomically restores to Hall or yields to a committed terminal; Hall generations are durable and reused in-process; stored death/extraction/Recall outcomes remain ordered before destination control; and exact Hall/microrealm/Character Select transition replays converge without rewind while changed material fails closed. Corrective commit `51be807` withholds extraction and Recall Hall actors until the exact stored terminal result is delivered and acknowledged; restart never invents extraction request correlation or exposes destination control first. Focused bootstrap tests pass 5/5, route-actor tests pass 11/11, the complete 293-test server suite and all server targets pass, and strict server all-target Clippy is green. Commit `93dde0a` unifies dynamic Recall, extraction, and restart recovery on the private-life session's single writer: symmetric two-phase handoffs are recoverable and exact; reconnect rebinds both owners before visibility; only the session retires shared transports; stored Explicit/LinkLost Recall and exact extraction retries remain account/character/content/version bound; current outer ticks and stored inner correlation are preserved; and Hall remains gated behind exact terminal delivery. Real-QUIC composition proves one writer identity, contiguous sequences `1/2/3` per transport, stale-detach safety, coordinated reconnect, and zero residue. Local formatting/diff checks, strict server/workspace all-target Clippy, 301/301 server library tests, all workspace tests, four content validators, and generated-schema verification pass. Commit `45c8bcb` integrates the provenance-complete, hash-locked Realm Gate, Lantern Fork, and Bell Sepulcher art into the optimized disposable microrealm at both certified resolutions and effects modes without changing compiled authority or normal admission. Commit `37cf99d` adds rollback-safe live microrealm movement, lifecycle, server-clear, and Bell-range ownership; focused tests cover the complete spawn-to-Bell route and stale-authority rollback. Commit `736cb59` adds provenance-complete Drowned Pilgrim, Bell Reed, and Chain Sentry candidates; keep them outside runtime/content hashes until optimized B1/B5 telegraph and anchor review passes. Next, bind this live microrealm owner to the persistent private-life session, then compose its combat-clear producer, fixed-room combat, rewards, pending-inventory ownership, and all five terminal producers before opening the ordinary route. Keep `core_world_flow_integration`, Character Select `Play`, production Realm Gate interaction, and normal extraction/Recall admission disabled until live movement, combat, rewards, pending inventory, all terminal owners, real-QUIC restart/25-journey evidence, and visual review pass; keep production namespace cutover, Core promotion, M04+ content, Requiem encounters, paid Oath changes, retirement, purge, and unrelated stations disabled under their independent owners.
