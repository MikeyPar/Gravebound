# GB-M03-02D - Durable death and memorial persistence

**Status:** In progress - migrations through `0052`, strict DTOs, the serializable terminal writer with mandatory retained-trace promotion, immutable read repository, protocol `1.13`, authenticated dispatch, and hosted real-QUIC/PostgreSQL restart, rollback, serialization, outage, altered-row, and outbox-immutability proof are published. The joint `06A` audit remains open for the remaining `06E` cardinality and measurement evidence.

## Objective

Add the wipeable Core PostgreSQL records and transaction boundary required to preserve a committed permadeath, its combat evidence, deterministic destruction result, exact summary, and memorial across response loss and process restart. This slice owns persistence mechanics, not combat-rule selection, client presentation, successor creation, extraction, or Recall.

## Authority mapping

1. `Gravebound_Production_GDD_v1_Canonical.md`: `DTH-001`, `DTH-020`, `TECH-020`-`023`, and `QA-005` require one durable, idempotent death transaction; immutable trace and memorial evidence; post-commit finality; and restoration after restart.
2. `Gravebound_Content_Production_Spec_v1.md`: `CONT-ECHO-009`, `CONT-HUB-001`, and `CONT-HUB-002` require stable content identities, oldest-first Echo promotion, and a newest-first Memorial Wall backed by the exact stored `DTH-020` snapshot.
3. `Gravebound_Development_Roadmap_v1.md`: `GB-M03-02`, `GB-M03-06`, `GB-M03-13`, and the M03 exit gate require restart-safe death/memorial state and all-or-nothing qualifying death, destruction, memorial, and Echo writes.

Approved [`SPEC-CONFLICT-009`](../spec-conflicts/SPEC-CONFLICT-009-m03-death-memorial.md) is binding: every normal committed permadeath creates one memorial; `death_id` is UUIDv7; `(character_id, "permadeath-v1")` is the final-death idempotency key; and qualifying Echo projection occurs inside the same SQL transaction.

## Persistence contract and invariants

- Migration `0031` is forward-only and additive within the wipeable Core namespace. It may replace existing restrictive checks only to admit the named `dead` life state and `permadeath` destruction reason; it must not renumber existing item security/location values or rewrite migrations `0001`-`0030`.
- Add immutable, namespace-bound records for `DeathEvent`, `DeathSummarySnapshot`, `MemorialRecord`, ordered combat-trace entries, ordered item/material destruction entries, the final-death receipt, and append-only audit/outbox events. Every child row is foreign-keyed to `death_id`; no accepted death row may exist without its one-to-one summary and memorial.
- Store stable typed IDs, fixed-point numbers, ticks, versions, content revision, and presentation keys. Never store localized prose as authority, reconstruct the summary from mutable current state, or copy item rolls into memorial/Echo records.
- Lock the account, character aggregate, inventory aggregate, all owned at-risk item rows, and the Echo account aggregate in one documented order. The transaction changes `Living -> Dead`, advances each affected version once, and commits every named record or none.
- Exact retry returns the stored terminal outcome. Reuse with changed canonical payload is an audited idempotency conflict. A second final death for the same character is impossible even under a distinct mutation ID.
- After commit, commands bound to the former character or inventory versions fail closed. A database error, serialization victim, malformed stored record, illegal at-risk location, or projector failure leaves the character living and the pre-death inventory observable.
- CharacterSafe and Vault rows are read only for the preserved projection and are never destroyed or reassigned. Existing item rows transition to `Destroyed(permadeath)` and retain identity/provenance; they are not deleted.
- Rollback documentation must state that schema `0030` can be restored only after proving there are no dead characters, death/memorial/Echo rows, or `permadeath` destruction records. No automatic lossy down migration is permitted.

## Delivery and test checklist

- [x] Publish migration `0031` and forward closures through `0039`, schema-version updates, constraints, indexes, rollback/recovery guards, and schema-drift fixtures.
- [x] Add typed persistence DTOs and strict validation for all death, summary, memorial, trace, destruction, receipt, audit, and outbox records.
- [x] Add a serializable transaction repository with canonical lock ordering, exact replay, changed-payload conflict, optimistic-version checks, and injected pre-commit rollback.
- [ ] Prove immutable-row constraints, one death/summary/memorial per character/death, exact destruction ordinals, preserved safe storage, and no item deletion.
- [ ] Prove concurrent lethal writers, response loss, serialization retry, process restart, corruption rejection, unavailable PostgreSQL, and Echo-projector rollback against hosted PostgreSQL.
- [ ] Publish the joint `GB-M03-02D`/`GB-M03-06A` audit only after real PostgreSQL and real-QUIC evidence is green.

## Exclusions

Cause selection, clocks, deed qualification, live trace capture, terminal arbitration, death-summary rendering, Memorial Wall presentation, Echo encounter assembly, successor creation, extraction, Recall, telemetry export, support tooling, production namespace cutover, and normal-route admission.

## Current Next Step

Commits `fb8f56a` through `e16424e` define, load, restart, adversely inspect, and enforce exact causal-outbox cardinality for the complete typed death authority. Hosted CI `29488569454` accepted ten authenticated real-QUIC/PostgreSQL journeys with a 12.868 ms maximum durable-commit-to-ready client model and zero measured residue; native presentation remains the separately inspected `06D` authority. Workflow `29489909161` accepted [`GB-M03-06E-death-memory-soak.json`](../performance/GB-M03-06E-death-memory-soak.json): 8,509 journeys, 425 completed reconnects/replays/signature checks, stable supplemental process memory, and zero residue. Commits `2d61ab5`, `e32cf1f`, and `32e5f6f` add bounded death provenance, consume it in the exact `ECH-001` closure, and advance readiness through migrations `0053`/`0054`. Commit `0d9156e` adds the six reachable death/Echo branches; latest hosted run `29492700515` is executing them. Under `Gravebound_Production_GDD_v1_Canonical.md`, `Gravebound_Content_Production_Spec_v1.md`, and `Gravebound_Development_Roadmap_v1.md`, archive the branch report, close native frame timing and concurrent eligible-death coverage, then publish the joint `02D`/`06A` audit. Parent `GB-M03-02` remains open and the player route remains disabled.
