# GB-M03-03D - Fixed Bell rooms and Core encounter roster

**Status:** Complete - audited in [`GB-M03-03D-audit.md`](../milestones/GB-M03-03D-audit.md); normal route admission remains disabled.

## Objective

Deliver the strict unpromoted content, renderer-independent simulation, and disposable native evidence for the complete Core encounter roster, the `pack.bell.01` private-microrealm fight, and the fixed Bell private-life room chain. Reuse the exact First Playable enemies and combat primitives where their IDs already exist, add only the missing Core records/behavior, and keep Sir Caldus, committed extraction, production ingress, and seeded player-facing dungeon generation outside this package.

## Three design authorities

1. `Gravebound_Production_GDD_v1_Canonical.md`: `COM-001`-`006`, `DNG-001`, `DNG-003`-`006`, `DNG-040`, `ENC-001`-`005`, `ENC-014`, `ENC-020`, `UI-030`, `ART-005`-`011`, `TECH-004`, and `TECH-020`-`023`.
2. `Gravebound_Content_Production_Spec_v1.md`: `CONT-001`-`003`, `CONT-WORLD-001`-`003`, `CONT-ROOM-001`-`007`, `CONT-ENEMY-001`-`004`, exact Core encounter manifest/packs, Bell room geometry/anchors, fixed private-life layout, scaling, warnings, assets, localization, and validation matrices.
3. `Gravebound_Development_Roadmap_v1.md`: `GB-M03-03`, the M03 cumulative six-normal/two-miniboss/one-major-boss/nine-room/28-audio-cue/35%-presentation targets, and approved `SPEC-CONFLICT-006` `03D` ownership and route gate.

## Exact Core content closure

- Normal enemies are exactly `enemy.drowned_pilgrim`, `enemy.mire_leech`, `enemy.bell_reed`, `enemy.bell_acolyte`, `enemy.chain_sentry`, and `enemy.choir_skull`.
- Minibosses are exactly `miniboss.sepulcher_knight` and `miniboss.choir_abbot`. The readable suffix does not permit substituting `enemy.sepulcher_knight` for the miniboss namespace.
- Reuse the immutable First Playable Drowned Pilgrim, Bell Reed, and Chain Sentry records/behavior. Add strict Core records for Mire Leech, Bell Acolyte, Choir Skull, both minibosses, and every derived pattern, cue, asset, and localization key.
- `pack.bell.01` is exactly six Drowned Pilgrims plus two Bell Reeds, budget 12, simultaneous spawn on the first eight legal microrealm anchors in `(y,x)` order after the `03C` warning request.
- Compile exactly nine Bell templates: `room.bell.vestibule_01`, `cross_01`, `nave_01`, `bridge_01`, `choir_01`, `knight_01`, `rest_01`, `secret_01`, and `arena.boss.caldus_01`.
- `layout.core_private_life_01` is exactly `B0 -> B1 -> B2 -> B3 -> B4 -> B5 -> B6`, with four combat rooms, one rest/Bargain room, one boss room, and the safe vestibule excluded from the six-room count. `BB1`/`BS1` branches and seeded selection remain disabled in M03 even though their templates and encounter fixtures are validated.
- Fixed room encounters are exact: B1 `6 Pilgrim + 2 Bell Reed`; B2 `6 Pilgrim + 2 Bell Acolyte + 1 Choir Skull`, budget 16; B3 Sepulcher Knight; B4 rest/Bargain; B5 `6 Pilgrim + 1 Chain Sentry`; B6 arena geometry only until `03E` supplies Sir Caldus.
- Mire Leech and Choir Abbot receive complete content/simulation/showcase coverage to close the Core `6/2` manifest, but neither may be silently inserted into the M03 main chain.

## Simulation and presentation scope

- Strict room definitions use northwest-origin geometry, exact sizes/doors/collision/hazard volumes, spawn/pattern/door/stage/reward anchors, four-tile-by-three-tile corridors, stable IDs, and deterministic digests.
- Room authority implements server-owned activation, capacity-one M03 participant lock, exact 900 ms group warning, door/hurtbox safety, required-hostile completion, two-second quiet period, hostile/projectile cleanup, three-second empty reset, immutable deaths/Recalls, and safe reactivation.
- The six normal enemy simulations implement the exact shared state contract, target tie-break, telegraph/aim locks, movement, attack cycles, aggro/leash/reset, health/armor/hurtboxes, damage type/band, threat, memory, disposition, reward profile, and XP binding.
- Sepulcher Knight and Choir Abbot implement exact three-second introductions, Elite scaling, full multi-pattern schedules, quiet windows, deterministic target/gap selection, rewards, XP, reset behavior, and 35-65 second solo-duration fixtures.
- The `03C` lifecycle consumes `pack.bell.01` construction and authoritative clear results. A reset removes enemies/projectiles and returns Dormant; Cleared remains terminal and opens only the existing Bell-portal condition.
- A disposable native encounter/room showcase renders exact compiled geometry and content-driven combat cues at 1920x1080 and 1280x720, including standard/reduced-effects modes and visual/audio priority. It must not become production Character Select or Realm Gate ingress.

## Acceptance criteria

1. Strict schema/compiler closure proves the exact Core `6/2` roster, all nine Bell templates, fixed main chain, room encounters, assets/localization, and no promoted-content mutation or invented record.
2. Every room validates door compatibility, collision, anchors, corridors, reachable safe entry/exit, objective references, threat/readability budgets, and deterministic digest; invalid geometry or disconnected paths fail closed.
3. `pack.bell.01` spawns exactly eight entities after the existing warning, uses the exact legal sorted anchors, clears/resets atomically with the `03C` lifecycle, and never respawns after terminal Cleared.
4. Each normal enemy and miniboss passes exact tick-boundary, geometry, damage, band, warning, target tie-break, reset, reward/XP binding, reduced-effects identity, and fixed-trace tests. Reused First Playable behavior remains byte/trace compatible.
5. Room activation/completion/reset passes adversarial order, boundary, duplicate, zero-living, projectile cleanup, door-hurtbox, and deterministic replay fixtures without reviving or rolling back a death/Recall.
6. Capacity-one M03 runtime is enforced. Validators exercise `N=1..8` health/budget expansion and minimum-speed safe routes for future compatibility without enabling party admission or changing damage/projectile speed/gaps.
7. B1, B2, B3, B5, microrealm pack, Mire Leech fixture, and Choir Abbot fixture pass deterministic full-combat traces with no softlock, budget breach, or impossible mandatory pattern.
8. Inspected optimized native evidence proves all six roles, both minibosses, fixed room readability, 900 ms warnings, exact disabled branch/boss states, 1280x720, and reduced effects without obstructing the playfield.
9. Focused/workspace tests, warnings-denied Clippy, strict content validation, deterministic traces, generated schemas, optimized Windows construction, and hosted CI pass before the completion audit.

## Exclusions

Production Sir Caldus behavior/rewards; `03E` boss participant lock and committed extraction/Hall return; `03F` loading/reconnect/real-QUIC journey; player-facing seeded room generation; enabled Bell branch/Secret traversal; normal route admission; durable death/Recall/extraction arbitration; ship-quality M05 art/audio; multiplayer/public allocation; release packaging; and Core promotion.

## Implementation order

1. Add strict Core encounter/room schemas, records, assets, localization, manifest closure, and compiler validation without duplicating First Playable records.
2. Compile exact room/corridor/encounter definitions and the fixed branch-disabled private-life graph with stable digests.
3. Implement the three missing normal enemy behaviors and adapt/revalidate the three reused First Playable enemies under the shared Core contract.
4. Implement both minibosses and deterministic safe-route/duration fixtures.
5. Connect `pack.bell.01`, fixed room activation/completion/reset, reward/XP seams, and disposable journey fixtures.
6. Add native standard/reduced-effects presentation, optimized evidence, cumulative gates, and the completion audit.

## Completed foundation

- Strict non-promotable contracts now model the ordered six-normal/two-miniboss roster, explicit immutable-FP reuse, pattern/reward/XP references, typed room doors/volumes/anchors, `pack.bell.01`, and the fixed branch-disabled layout.
- Checked-in records transcribe all nine Bell templates at exact dimensions and anchors, the exact B1/B2/B3/B5 compositions, the disabled Choir/Secret fixtures, and an empty B6 arena reserved for `03E` Sir Caldus.
- Symbolic sprite/tilemap/telegraph/audio assets and complete en-US record copy resolve through independently pinned BLAKE3 hashes.
- The fail-closed compiler proves FP definition reuse, reward/XP closure, exact IDs/order/counts/budgets/warnings, geometry bounds, three-tile doors/corridors, four-tile corridor length, six-room counting, branch/seeded-selection disablement, localization, assets, and mutation rejection.
- Renderer-independent room definitions preserve exact doors, solids, hazards, lanes, circles, and anchors under clockwise rotation. The active B0→B6 chain is placed from compatible world-space doors with a pinned layout digest; BB1/BS1 are not constructed and B6 remains geometry-only.
- Fixed-grid navigation expands blocking geometry by the player collision radius and proves every door plus required stage/shrine/chest/group utility anchor is connected. Exact encounter roles must have enough compatible room anchors before compilation succeeds.
- Five strict authored behavior records now materialize the common AI contract, exact health/armor/hurtboxes, locomotion, introductions, target/reset timings, and all eight new pattern payloads. Warning, geometry, damage, cadence, memory, disposition, scheduler maximum, and normalized projectile fields fail closed under mutation.
- Telegraph and audio references use the exact derived `pattern_id` suffixes required by `CONT-013`; both authored Major patterns own their additional `.warning.major` cues. Independent records and asset hashes pin the complete boundary.
- One immutable roster-ordered compiled view adapts the three validated First Playable simulation definitions and the five Core-authored definitions, preserving reward and XP bindings without creating runtime entities or silently replacing FP behavior with later-stage values.
- The Core simulation boundary performs the single authored-millisecond-to-30 Hz conversion for shared AI timings, locomotion, warnings, cycles, quiet windows, charge durations, rotor emissions, and projectile lifetimes. A fixed scheduler trace recomputes every persisted active-instance maximum and rejects drift; Choir Skull and Choir Abbot use the exact `move_with_rotation` counterplay contract.
- Owner-approved [`SPEC-CONFLICT-014`](../spec-conflicts/SPEC-CONFLICT-014-m03-choir-abbot-close-spawn-warning.md) preserves the Choir Abbot ring's exact final 650 ms directional gap preview while adding a distinct 2.5 s ground-origin warning across recovery. The typed warning compiles to 20 and 75 ticks respectively and rejects close-spawn, damage-band, or ordering drift.
- Executable COM-006 evidence now solves all eight Core-authored routes at 4.5 tiles/s, 0.25 hurtbox radius, 120 ms RTT, and no movement ability. It proves displacement or angular tracking, edge-to-edge corridor width, the 1.25-tile/750 ms ground-origin boundary, the 350 ms minimum arrival boundary, and the Standard 300-projectile cap from compiled definitions; the three immutable First Playable routes remain covered by their existing exact solver.
- The renderer-independent shared-AI boundary now selects living, damageable targets by exact squared distance then lowest entity ID, rejects duplicate candidates, preserves inclusive aggro edges, and snapshots cast ID, pattern, origin, target position, aim, and resolve tick into an immutable telegraph lock. Input order, target movement after warning start, and renderer state cannot change a release.
- Owner-approved [`SPEC-CONFLICT-015`](../spec-conflicts/SPEC-CONFLICT-015-m03-shared-ai-leash-and-introduction.md) measures leash actor-to-target like the immutable First Playable behavior and overlaps each miniboss's 900 ms spawn warning with its exact three-second introduction.
- A clone-stage-commit shared runtime now owns the exact SpawnTelegraph → Acquire → Move/Position → Telegraph → Attack → Recover sequence, eight-tick target rescans, immediate dead/immune/out-of-leash invalidation, first/repeated warning selection, and immutable attack release. At exactly 150 targetless ticks it atomically restores authored health and spawn position, clears target/cast/first-use state and hostile output, emits no reward, and restarts the overlapping spawn/introduction gate; a legal target returning on the boundary tick prevents reset.

- Owner-approved [`SPEC-CONFLICT-016`](../spec-conflicts/SPEC-CONFLICT-016-m03-core-kit-cycle-ordering.md) fixes Knight fan phase and rotor preview/release ordering without changing the authored six-second cycles. One clone-stage-commit scheduler boundary now owns all five Core-authored kits, including first/repeated warnings, Acolyte alternation, Mire recovery, Knight charge/stop-ring/fan priority, independently rounded rotor volleys, Abbot recovery warning layers, and equal-tick ring-before-rotor release. Golden traces pin every first use, reset, active/recovery boundary, and repeated cycle.
- A cross-document construction boundary now consumes only the exact `BeginPackWarning` lifecycle event and joins the immutable Core world and encounter catalogs. It expands `pack.bell.01`, sorts enemy IDs and the first eight enabled `(y,x)` anchors, assigns stable run-qualified entity/spawn IDs, and fixes the simultaneous activation tick 27 ticks after warning start. Wrong lifecycle events, warning drift, zero run ordinals, roster/budget/anchor drift, and tick overflow fail atomically without a partial plan.
- The immutable plan now instantiates the existing authoritative First Playable normal-wave simulation against exact 48×48 Core microrealm combat bounds, preserving the harmless group-warning window, the eight planned identities/positions, and the shared activation tick. This reuses the locked Drowned Pilgrim/Bell Reed timelines and hostile pipeline instead of creating a parallel combat implementation.
- One clone-stage-commit encounter owner now joins the `03C` lifecycle and `03D` wave. Only an authoritative eight-enemy wave defeat transitions Cleared and opens the Bell portal. Exactly 150 empty ticks clears projectiles/lanes, synthesizes no reward, preserves participant/projectile authority, discards the wave, and returns Dormant; a retrigger allocates the next eight spawn ordinals instead of reusing identities. Missing combat input and failed lifecycle/wave operations roll back every owned state.
- Renderer-independent fixed-room authority now owns the common `DNG-005` state contract: capacity-one participant lock, hurtbox-safe door closure, exact 27-tick group warning, monotonic required-hostile/objective progress, one completion/reward-XP handoff, hostile cleanup, 60-tick quiet period, and exact 90-tick empty reset with unsecured-drop cleanup and no completion event. Boundary reentry prevents reset, and every invalid step rolls back atomically.
- The fixed content compiler now materializes only B1/B2/B3/B5 as encounter plans, rotates each room before assignment, expands units in `(enemy_id, occurrence)` order, selects distinct compatible anchors in `(y,x,id)` order, and allocates one disjoint run-local identity sequence across all 25 initial actors. Every assignment retains its exact immutable runtime kind, reward profile, and XP profile; B2 rotation, the bound Knight anchor, budgets `12/16/10/12`, warning ticks, and authority construction are golden-tested.
- B1 and B5 plans now compile their rotated solids/deep water and anchors into authoritative combat arenas and instantiate through the existing immutable First Playable normal-wave owner at the shared warning tick. B2 and B3 explicitly return `AuthoredRuntimeRequired`, preventing an incomplete fallback from silently substituting FP behavior for Acolyte, Skull, or Knight combat.
- One clone-stage-commit B1/B5 owner now derives required-hostile progress exclusively from the immutable wave, joins door safety and the exact 27/60/90-tick warning/quiet/reset boundaries, transfers reward/XP-capable participant authority only after a real clear, and atomically clears hostile output and unsecured drops on reset. Exact activation-tick clears are legal while earlier warning damage remains rejected; reactivation advances by the full 25-actor route stride so every room retains a disjoint, nonreused identity range.
- Shared enemy health authority now accepts all five Core-authored actor definitions alongside the three immutable First Playable enemies without weakening collision provenance, stable damage ordering, armor, death, Frostbind, or delayed-drop semantics. Health kind, maximum health, armor, and owned reward profile remain exact for Mire Leech, Bell Acolyte, Choir Skull, Sepulcher Knight, and Choir Abbot; Core reward IDs are no longer collapsed to the prototype static reward table.
- Core-authored health now owns each actor's exact 30-tick spawn invulnerability boundary from its global spawn tick. Valid collision-proven damage before that boundary is reported as ignored without mutating health; damage becomes legal exactly on the boundary, preserving atomic rollback and replay hashing.
- Owner-approved [`SPEC-CONFLICT-017`](../spec-conflicts/SPEC-CONFLICT-017-m03-core-locomotion-phase.md) fixes Acolyte radial correction and Choir Skull orbit initialization/reset without changing authored speed or geometry. A clone-stage-commit normal-kit attack overlay now converts Mire/Acolyte/Skull scheduler events into stable cast IDs and immutable origin/target/tick locks. B2 traces prove Acolyte target movement cannot alter a cast, Skull releases remain exactly 180 ticks apart with ten locked volleys, and duplicate target input rolls back scheduler and cast identity.
- Fixed-room assignment now rejects reused coordinates in addition to reused anchor IDs. This closes the nave's role-compatible `p1`/`d1` coordinate alias: B2 deterministically assigns its Skull to rotated center anchor `d3`, and every initial combat room proves pairwise-distinct spawn positions.
- Clone-stage-commit B2 locomotion now consumes the exact authored speed and geometry contracts under approved `SPEC-CONFLICT-017`. Bell Acolytes correct radially to exactly six tiles without crossing the boundary; the Choir Skull enters its three-tile orbit due east, advances clockwise with deterministic radial correction, traverses the shared swept-hurtbox solid path, and restores spawn/phase/remainders on reset. The rotated B2 trace pins exact Acolyte positioning, Skull orbit entry/radius/direction, and reset replay.
- Owner-approved [`SPEC-CONFLICT-018`](../spec-conflicts/SPEC-CONFLICT-018-m03-choir-skull-rotor-phase.md) fixes the Choir Skull's otherwise unspecified attack phase. Every cast previews east/west, advances its first arm clockwise by exactly 14 degrees per authored volley, and restarts at phase zero independently of locomotion and renderer state.
- Core-authored Acolyte and Skull release locks now materialize through the shared hostile projectile allocator, movement, collision, damage, and cleanup owner. Acolyte scheduler phase selects the exact alternating five-shot offsets; Skull volleys emit exact opposite arms from a platform-independent angle table. The synchronized trace pins release/hostile clock agreement, monotonic IDs, payload/band/lifetime metadata, both fan phases, ten 14-degree clockwise volleys, and non-spawning telegraph rollback.
- The immutable normal-wave owner now exposes a narrow mixed-encounter seam: Core release locks enter its existing hostile allocator, and an explicit external-required-hostile count may defer only terminal clear/cleanup. A lethal immutable cohort remains dead while the room stays Active, then clears exactly when authored health reaches zero; player authority, projectile IDs, hostile stepping, drops, and reset handoff remain single-owner.
- The exact B2 owner now joins six immutable Pilgrims, two authored Bell Acolytes, and one authored Choir Skull under one clone-stage lifecycle transaction. It splits collision-proven friendly intents by owned target, preserves one player and hostile allocator, advances authored health/locomotion/attacks only through authoritative ticks, and derives room completion from all nine health records. Boundary traces pin the 27-tick warning, exact authored spawn invulnerability, nine-actor clear, 60-tick quiet period, 90-tick empty reset, hostile cleanup, and full 25-actor retry stride.
- A 260-tick sustained B2 fixture runs the production schedules and shared hostile pipeline under debug-invulnerable test damage only. Two independent executions produce the same global tick/source/pattern/projectile-ID trace, naturally emit immutable Pilgrim fans plus both Acolyte fan phases and complete Skull rotor volleys, remain Active without a scheduler softlock, then accept one collision-proven nine-actor clear.
- Owner-approved [`SPEC-CONFLICT-019`](../spec-conflicts/SPEC-CONFLICT-019-m03-sepulcher-knight-charge-and-gap.md) fixes the Knight's 17 release-tick movement segments, swept one-contact ownership, solid-truncated realized endpoint, and exact target-opposite adjacent ring-pair tie break without changing authored cadence or payload.
- A dedicated Sepulcher Knight authority now owns fixed-point pursue-to-3.5 locomotion, immutable charge and fan locks, exact five-tile/17-tick charge motion, swept solid and player contact, dynamic post-charge home, parent-only stop ring, first/repeated fan warnings, targetless reset, and first-use schedule restoration. The authored spawn remains distinct from the dynamic charge home.
- Knight stop rings and shield fans now enter the shared hostile projectile allocator, movement, collision, damage, and cleanup pipeline. Exact physical Pressure payloads, 8-of-10 target-opposite ring indices, five/50-degree fan directions, speed/range/radius/lifetime metadata, cast identity, and non-spawning telegraphs fail closed under drift.
- The exact B3 owner now composes capacity-one room lifecycle, one 1,600-health/8-armor Knight health record, player state, charge direct damage, shared hostile authority, and a typed one-shot `reward.miniboss_t1` / `xp.miniboss_t1` clear handoff under one clone-stage-commit transaction. Friendly lethal damage cancels same-tick hostile execution, quiet starts once, and empty reset emits no reward while advancing the complete 25-actor retry stride.
- A replay-identical 35-second B3 fixture pins the three-second non-damaging introduction, charge telegraph/release at ticks 90/117, all 17 movement segments, parent ring at 134 with exactly eight projectiles, first shield fan at 168 with exactly five projectiles, terminal reward/XP handoff, and no softlock. An obstruction fixture proves one contact per cast, shell truncation, realized-endpoint home/ring origin, and the approved omitted pair.
- Owner-approved [`SPEC-CONFLICT-020`](../spec-conflicts/SPEC-CONFLICT-020-m03-choir-abbot-phase-and-gap.md) fixes cast-local Abbot rotor phase, final-preview target locking, and exact target-facing four-index gap selection while preserving the approved recovery warning layers and equal-tick ring-before-rotor priority.
- A dedicated stationary Abbot authority now owns immutable first/repeated rotor locks, ten exact 12.25-degree two-arm phases, independently rounded 350 ms volleys, the 3.5-second active/2.5-second recovery boundary, origin-only and directional warning layers, immutable target-facing recovery gaps, reset, and monotonic cast identity.
- Abbot rotor volleys and 12-of-16 recovery rings now materialize through the shared hostile allocator with exact veil Pressure18/Major26 payloads, speed/range/radius/lifetime metadata, fixed phase tables, stable projectile ordering, and fail-closed non-spawning previews.
- The complete disabled-branch Abbot fixture binds the exact `room.bell.choir_01` arena, 1,900-health/6-armor shared health record, three-second introduction, player/hostile authority, one-shot `reward.miniboss_t1` / `xp.miniboss_t1` handoff, 60-tick quiet period, and rewardless reset. Its replay-identical 35-second trace pins every first loop boundary, 2/12 projectile releases, immutable preview target, all rotor phases, reset cancellation, and no softlock without enabling `BB1` traversal.
- Owner-approved [`SPEC-CONFLICT-021`](../spec-conflicts/SPEC-CONFLICT-021-m03-mire-leech-body-motion.md) fixes Mire Leech charge/retreat tick ownership, swept one-contact ordering, solid truncation, and the retreat vector without changing its authored cycle.
- A dedicated Mire body owner now composes the shared immutable normal attack scheduler with clamped 3.0 approach, exact 15-segment two-tile charge, stable swept player/solid contact, realized-endpoint retreat lock, and 45-tick 3.5-speed retreat. Targetless/reset paths restore authored spawn, first-use warning, contact groups, and health without reward.
- The complete disabled-route Mire fixture binds shared 70-health/0-armor authority, harmless 27-tick spawn warning, physical Pressure12 direct contact, `reward.normal_outer` / `xp.normal_t1` handoff, and a replay-identical 260-tick sustained trace. It pins charge/contact/retreat boundaries, movement-delayed later cycles, reset during charge, and collision-proven terminal clear without inserting Mire into the M03 main chain.
- A separate native evidence adapter loads only the strict unpromoted encounter compiler, derives all seven main-chain cards and four exact `SpawnWarning` states from renderer-independent authority, and presents the complete 6/2 roster plus explicit B6/BB1/BS1/seeded/ingress disabled states. It cannot construct a production route or mutate a run.
- Four optimized Windows frames at 1920×1080 and 1280×720 were inspected in standard/reduced-effects modes. Reduced effects preserve identical hostile-warning and route information; the first layout-displacing captures were rejected and replaced.
- Workspace tests, warnings-denied Clippy, strict content validation, duplicate deterministic trace, generated-schema cleanliness, optimized Windows construction, and exact-commit hosted CI pass. The completion record is [`GB-M03-03D-audit.md`](../milestones/GB-M03-03D-audit.md).

## Current Next Step

Parent [`GB-M03-03`](GB-M03-03.md) normal integration remains active in [`GB-M03-03G`](GB-M03-03G.md) under [`ADR-037`](../decisions/ADR-037-normal-core-private-route-composition.md). Exact source `e069987` is green under hosted CI [`29633952947`](https://github.com/MikeyPar/Gravebound/actions/runs/29633952947) for the dormant persistent foundation, generation-safe shared-writer session/Recall ownership, terminal-first bootstrap and schema-63 intent foundations, strict quality/content/schema checks, PostgreSQL transactions, Windows release construction, and optimized native evidence. Commit `26c420b` constructs the dormant persistent foundation, commit `551c527` owns the monotonic per-account transport generation and shared reliable writer with dynamic Recall, and commit `cf933f2` adds the dynamic extraction runtime. Extraction now requires an opaque five-producer executor proof, samples a generation-bound authoritative tick only after command dequeue, publishes one cancellation-safe completion, marks retries/handoffs as replayed, binds Hall installation to the exact transport/terminal/result, retires safely for every competing terminal winner, and reports route/runtime residue without closing the continuing Hall writer. Local formatting, 9/9 extraction-intent runtime tests, 11/11 extraction execution tests, the complete 286-test server suite, all workspace targets, and strict workspace all-target Clippy pass at `cf933f2`; an independent authority re-audit found no remaining runtime blocker. Commit `a30e644` adds the terminal-first process bootstrap adapter and post-commit/replayed world-flow reconciliation: first-time identity defers cleanly; Character Select and storage-blocked states create no route actor; restart danger atomically restores to Hall or yields to a committed terminal; Hall generations are durable and reused in-process; stored death/extraction/Recall outcomes remain ordered before destination control; and exact Hall/microrealm/Character Select transition replays converge without rewind while changed material fails closed. Corrective commit `51be807` withholds extraction and Recall Hall actors until the exact stored terminal result is delivered and acknowledged; restart never invents extraction request correlation or exposes destination control first. Focused bootstrap tests pass 5/5, route-actor tests pass 11/11, the complete 293-test server suite and all server targets pass, and strict server all-target Clippy is green. Commit `93dde0a` unifies dynamic Recall, extraction, and restart recovery on the private-life session's single writer: symmetric two-phase handoffs are recoverable and exact; reconnect rebinds both owners before visibility; only the session retires shared transports; stored Explicit/LinkLost Recall and exact extraction retries remain account/character/content/version bound; current outer ticks and stored inner correlation are preserved; and Hall remains gated behind exact terminal delivery. Real-QUIC composition proves one writer identity, contiguous sequences `1/2/3` per transport, stale-detach safety, coordinated reconnect, and zero residue. Local formatting/diff checks, strict server/workspace all-target Clippy, 301/301 server library tests, all workspace tests, four content validators, and generated-schema verification pass. Commit `45c8bcb` integrates the provenance-complete, hash-locked Realm Gate, Lantern Fork, and Bell Sepulcher art into the optimized disposable microrealm at both certified resolutions and effects modes without changing compiled authority or normal admission. Commit `37cf99d` adds rollback-safe live microrealm movement, lifecycle, server-clear, and Bell-range ownership; focused tests cover the complete spawn-to-Bell route and stale-authority rollback. Commit `736cb59` adds provenance-complete Drowned Pilgrim, Bell Reed, and Chain Sentry candidates; keep them outside runtime/content hashes until optimized B1/B5 telegraph and anchor review passes. Commit `e5e1a9b` binds the live microrealm owner and exact route lease to the persistent account session: reconnect receives the same allocation before visibility, stale transports are denied, `LinkLost` preserves danger authority, exact unbind leaves the shared writer alive, and shutdown reports zero residue. Formatting, strict server all-target Clippy, `306/306` server library tests, every enabled server target, and focused real-QUIC handoff/reconnect proof pass locally. Commits `302ccb3`, `f07a282`, and `587924e` now extract the lifecycle-free `pack.bell.01` owner, move the single mutable character-combat allocation through an exact rejoin envelope, and remove tick/displacement/damage/clear authority from ingress. Each invoked frame generates the next tick, equipment-speed displacement, player combat/collision, hostile wave step, and exact clear projection under one staged route compare-and-swap. Strict all-feature/all-target Clippy, all `125/125` simulation tests, and all `309/309` server tests pass locally; hosted runs [`29639877086`](https://github.com/MikeyPar/Gravebound/actions/runs/29639877086), [`29639885732`](https://github.com/MikeyPar/Gravebound/actions/runs/29639885732), and [`29639908490`](https://github.com/MikeyPar/Gravebound/actions/runs/29639908490) are not yet complete or claimed green. Commits `ffd74d6` and `370b53e` add provenance-complete Bell VFX and pending-loot risk candidates; both remain outside registries and content/runtime hashes pending optimized in-engine review. Commits `f75c662`, `8d74ca7`, and `3c6af83` now compose exact compiled-shell movement and Slipstep in the same rollback-safe live combat frame; local `388/388` simulation-core, `126/126` content, `310/310` server, formatting, and strict all-feature/all-target Clippy gates pass. Commit `ea6445c` adds verified unregistered B0/B4/B6 landmark candidates without changing registry or content authority. Hosted CI runs [`29640683239`](https://github.com/MikeyPar/Gravebound/actions/runs/29640683239) and [`29640707913`](https://github.com/MikeyPar/Gravebound/actions/runs/29640707913) are not yet complete or claimed green. Next, move input-sequence validation to session ingress and introduce one exclusive microrealm driver with a one-slot latest-state input channel, separately reliable ability presses, a `MissedTickBehavior::Skip` 30 Hz interval, LinkLost neutralization with continued danger ticks, generation-safe reconnect, frame-complete freeze/shutdown, and zero residue. Then extend the same mutable combat handoff through fixed B0-B6 rooms, rewards, pending inventory, and all five terminal producers before opening the ordinary route. Keep `core_world_flow_integration`, Character Select `Play`, production Realm Gate interaction, and normal extraction/Recall admission disabled until the independent 30 Hz driver, fixed-room combat, rewards, pending inventory, all terminal owners, real-QUIC restart/25-journey evidence, and visual review pass; keep production namespace cutover, Core promotion, M04+ content, Requiem encounters, paid Oath changes, retirement, purge, and unrelated stations disabled under their independent owners.
