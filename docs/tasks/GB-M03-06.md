# GB-M03-06 - Atomic permadeath, deterministic destruction, and memorial

**Status:** In progress - the durable writer/read boundary, atomic Echo projection, protocol/read dispatch, authoritative clocks/deeds, serialized live-trace service, mandatory lethal promotion, and hosted real-QUIC/PostgreSQL restart, rollback, serialization, and outage proof are implemented. `06D` presentation, `06E` integrated closure, and audits remain open.

## Objective

Deliver the complete durable M03 permadeath experience: authoritative lethal finality, ten-second combat evidence, deterministic loss, immutable memorial, durable-acknowledged death summary, and mandatory atomic Echo projection when eligible.

## Authority mapping

1. `Gravebound_Production_GDD_v1_Canonical.md`: `DTH-001`, `DTH-002`, `DTH-020`, `DTH-021`, `ECH-001`/`002`, `TECH-015`, and `TECH-020`-`023` define normal permadeath, practice exclusions, the exact summary order, recovery targets, Echo eligibility, terminal precedence, and transaction isolation.
2. `Gravebound_Content_Production_Spec_v1.md`: `CONT-ECHO-009`, `CONT-HUB-001`, and `CONT-HUB-002` define the Echo state transition/promotion order and the Core Memorial Wall's newest-first exact-snapshot behavior.
3. `Gravebound_Development_Roadmap_v1.md`: `GB-M03-02D`, `GB-M03-06`, `GB-M03-13`, and the M03 exit gate require restart durability, nonduplication, and atomic death/destruction/memorial/Echo persistence.

Approved [`SPEC-CONFLICT-009`](../spec-conflicts/SPEC-CONFLICT-009-m03-death-memorial.md) resolves memorial scope, death identity, destruction ordering, lifetime/combat clocks, deed selection, and mandatory in-transaction Echo projection without further approval.

## Parent contract

- `06A`/`02D` owns typed durable records, migration `0031`, transactional repository seams, and append-only reliable protocol.
- `06B` owns authoritative lifetime and permadeath-combat clocks, reward-qualified deeds, deterministic cause selection, and the ordered ten-second trace.
- `06C` owns the single-writer terminal-resolution transaction and canonical at-risk destruction plan.
- `GB-M03-13` owns real Echo eligibility, immutable snapshot construction, Dormant insertion, and oldest-first promotion inside `06C`'s transaction.
- `06D` owns durable-ack-gated death summary and the read-only Memorial Wall.
- `06E` owns real-QUIC/PostgreSQL restart, replay, crash, concurrency, performance, cleanup, and visual closure across all preceding slices.
- No qualifying player-visible death may be enabled before `06C`, `GB-M03-13`, and `06D` pass together. Normal route admission remains owned by the later integrated M03 gate.

## Non-negotiable invariants

- Authoritative health zero in a permadeath-enabled instance chooses exactly one terminal outcome. A committed death wins same-tick Recall/extraction conflicts; a server crash is never itself a legitimate death.
- Every normal committed death creates exactly one memorial at every level. Practice/tutorial/developer deaths create no durable death, destruction, memorial, or Echo records.
- All `AtRiskEquipped` and `AtRiskPending` custody is destroyed in canonical order; CharacterSafe, Vault, account currency, cosmetics, codex, recipes, mastery, and settings are preserved.
- The stored result, not mutable live state, is the authority for retry, summary, memorial, telemetry, and support consumers.
- A qualifying death writes death, destruction, memorial, and Echo state in one SQL transaction or writes none. No asynchronous projector may repair a committed qualifying death.
- Later character, item, Bargain, progression, or world mutations cannot commit after final death.

## Delivery and test checklist

- [ ] Complete and audit `GB-M03-06A`/`GB-M03-02D`.
- [ ] Complete and audit `GB-M03-06B`.
- [ ] Complete and audit `GB-M03-06C` with `GB-M03-13` enabled in-transaction.
- [ ] Complete and audit `GB-M03-06D`.
- [ ] Complete `GB-M03-06E` and publish the parent three-authority audit.
- [ ] Pass workspace formatting, warnings-denied Clippy, tests, content/hash/schema checks, hosted PostgreSQL, real QUIC, optimized Windows construction, adverse/performance/cleanup evidence, and required visual inspection.
- [ ] Preserve death and memorial state across process restart and prove no duplicate death, item ledger entry, memorial, Echo, receipt, or outbox event under retry.

## Exclusions

Successor creation (`GB-M03-07`), normal extraction and Emergency Recall (`GB-M03-08`), telemetry export (`GB-M03-09`), support lookup (`GB-M03-10`), Echo encounter assembly/rewards, voluntary retirement, item insurance, corpse recovery, random salvage, resurrection, server-incident restoration policy, production namespace cutover, and M04+ content.

## Current Next Step

Schema `0042`, writer `ae7427c`, read repository `3d11b85`, protocol/server dispatch `6bd9e66`, and evidence authority `b930e01` cover the atomic death/destruction/summary/Memorial/Echo graph under all three design authorities and accepted `SPEC-CONFLICT-009`. The adverse series through `d2a3db2` proves producer precedence, participant rollback, serialization recovery, pool loss, blocked departure, and reconnect convergence; `0b39ec1`/`ac062c1` bind the separate presentation authority to every protocol `1.14` read. Commit `ab78412` completes the renderer-independent terminal summary coordinator: local health zero remains nondurable, committed death atomically starts Latest, exact `DTH-020` order and action hierarchy are typed, `0`–`4,096` losses page deterministically, dropped responses retry safely, and invalid/fatal states cannot reveal or resume actions. Commit `0f70f68` adds bounded Memorial list/detail state to that same non-cloneable sequence coordinator, including stable newest-first paging, exact stored-snapshot cross-checks, immutable selection, atomic refresh, loss retry, and fixed-memory duplicate rejection. Commit `4fa4787` closes canonical formatting and exact reachable-source portrait policy with required-null schemas, pinned durable hashes, integer boundary goldens, and a P0–P2-clean independent re-audit; exact hosted CI `29459554095` is green. Consume those canonical values and portrait policies in the client projection and both native `06D` surfaces next, then `06E`; keep every player-visible death surface disabled.
