# GB-M03-05F - Core Oath/Bargain lifecycle closure

**Status:** Complete - see [`GB-M03-05F-audit.md`](../milestones/GB-M03-05F-audit.md).

## Objective

Attach the completed Core Oath/Bargain mechanics to one versioned character-life aggregate across reconnect, dangerous-room handoff, process resume, committed safe transfer, death, and retirement cleanup. Preserve the normal-route gate and do not broaden this package into purge, paid Oath changes, full death/retirement feature implementation, or Core promotion.

## Three design authorities

1. `Gravebound_Production_GDD_v1_Canonical.md`: `BRG-001`, `BRG-005`, `TECH-015`, `TECH-023`, `QA-005`/`006`, and `TEL-001`/`002` own life persistence, reconnect/transfer continuity, 30-second danger checkpoints, ordering, adversarial recovery, and lifecycle telemetry.
2. `Gravebound_Content_Production_Spec_v1.md`: `CONT-014`, Core immutable content revisions, source/lineage binding, and route-stage restrictions own the durable offer/loadout identity restored by a checkpoint.
3. `Gravebound_Development_Roadmap_v1.md`: `GB-M03-05` and the M03 restart/idempotency exit gates require a complete private-life Oath/Bargain loop before promotion or normal-route admission.

Approved `SPEC-CONFLICT-008` and `SPEC-CONFLICT-011` require Bell state to survive reconnect and room changes, reset on acquisition/purge/death/retirement/committed safe transfer, cancel illegal pending repeats, and be journaled in checkpoints without writing PostgreSQL for every shot.

## Delivery contract

- Publish a bounded, versioned Bell checkpoint DTO containing the counter and optional exact pending-repeat projectile snapshot. Canonical bytes and BLAKE3 bind schema, content revision, account, character, instance lineage, aggregate versions, and checkpoint tick.
- Exhaustively validate checkpoint finiteness, ranges, IDs, projectile count/behavior/provenance, delay, damage, digest, lineage, content, and monotonic versions. Corrupt or stale state fails closed.
- Persist dirty live Bell state only on the authored 30-second danger cadence and lifecycle boundaries. Ordinary shots never write PostgreSQL.
- One dormant/test-only Core live aggregate owns compiled combat and consumables by account, character, and instance lineage. Same-instance reconnect rebinds it; dangerous-room handoff preserves Bell state and remaining delay.
- Process resume may reconstruct from the latest valid danger checkpoint. It never changes item security and never replaces the account restore point after an unrecoverable instance crash.
- A committed Danger-to-Safe extraction/Recall transfer resets Bell and deletes its checkpoint only after the durable transaction commits. Failure leaves live state untouched; replay converges idempotently. Dangerous room-to-room transfer does not reset.
- Provide a reusable transaction participant for future death and retirement owners: snapshot ordered active Bargains for memorial/telemetry, delete active rows, reset/delete Bell state, advance aggregate version, and emit one outbox result. Preserve immutable offer/decision history.
- Add exact offered/declined and lifecycle outbox events needed by this boundary. Full telemetry export/redaction remains with the roadmap telemetry package.
- Prove cross-account/character/lineage rejection, stale/corrupt checkpoints, concurrent/replayed writes, disconnect on Bell releases four/five, pending repeat at `+1`/`+8`, room handoff, process restart, safe-transfer ordering, and response-loss replay.
- Prove the real Core identity endpoint remains stage-disabled for combat admission and creates no normal-route scheduler.

## Delivery checklist

- [x] Versioned/validated Bell checkpoint DTO, migration, and PostgreSQL repository.
- [x] Character-life Core live aggregate with same-lineage reconnect and dangerous-room handoff.
- [x] Thirty-second dirty checkpoint scheduler and process-resume reconstruction.
- [x] Post-commit safe-transfer reset and reusable death/retirement cleanup participant.
- [x] Lifecycle outbox events, adversarial PostgreSQL/network fixtures, and real-QUIC dormant proof.
- [x] Full workspace/release/content/determinism gates, completion audit, and next-step handoff.

## Explicit exclusions

The 50-Ash purge command/UI; later 40-Ash Oath change; full death, memorial, Echo, or retirement feature implementation; normal production milestones; unresolved rolled-affix combat; Core promotion; and normal-route admission.

## Current Next Step

Parent [`GB-M03-03`](GB-M03-03.md) normal integration remains active in [`GB-M03-03G`](GB-M03-03G.md) under [`ADR-037`](../decisions/ADR-037-normal-core-private-route-composition.md). Exact source `e069987` is green under hosted CI [`29633952947`](https://github.com/MikeyPar/Gravebound/actions/runs/29633952947) for the dormant persistent foundation, generation-safe shared-writer session/Recall ownership, terminal-first bootstrap and schema-63 intent foundations, strict quality/content/schema checks, PostgreSQL transactions, Windows release construction, and optimized native evidence. Commit `26c420b` constructs the dormant persistent foundation, commit `551c527` owns the monotonic per-account transport generation and shared reliable writer with dynamic Recall, and commit `cf933f2` adds the dynamic extraction runtime. Extraction now requires an opaque five-producer executor proof, samples a generation-bound authoritative tick only after command dequeue, publishes one cancellation-safe completion, marks retries/handoffs as replayed, binds Hall installation to the exact transport/terminal/result, retires safely for every competing terminal winner, and reports route/runtime residue without closing the continuing Hall writer. Local formatting, 9/9 extraction-intent runtime tests, 11/11 extraction execution tests, the complete 286-test server suite, all workspace targets, and strict workspace all-target Clippy pass at `cf933f2`; an independent authority re-audit found no remaining runtime blocker. Commit `a30e644` adds the terminal-first process bootstrap adapter and post-commit/replayed world-flow reconciliation: first-time identity defers cleanly; Character Select and storage-blocked states create no route actor; restart danger atomically restores to Hall or yields to a committed terminal; Hall generations are durable and reused in-process; stored death/extraction/Recall outcomes remain ordered before destination control; and exact Hall/microrealm/Character Select transition replays converge without rewind while changed material fails closed. Focused bootstrap tests pass 5/5, route-actor tests pass 11/11, the complete 293-test server suite and all server targets pass, and strict server all-target Clippy is green. Next, bind the dynamic extraction and Recall terminal publishers through the private-life session owner and its one shared writer, then compose live movement, combat, rewards, and pending-inventory ownership before opening the ordinary route. Keep `core_world_flow_integration`, Character Select `Play`, production Realm Gate interaction, and normal extraction/Recall admission disabled until live movement, combat, rewards, pending inventory, all terminal owners, real-QUIC restart/25-journey evidence, and visual review pass; keep production namespace cutover, Core promotion, M04+ content, Requiem encounters, paid Oath changes, retirement, purge, and unrelated stations disabled under their independent owners.
