# GB-M03-03 - First complete private character route

**Status:** In progress - `03A`-`03F` are complete; normal private-route integration `03G` is open under [`ADR-037`](../decisions/ADR-037-normal-core-private-route-composition.md) and approved [`SPEC-CONFLICT-006`](../spec-conflicts/SPEC-CONFLICT-006-m03-world-flow-contract.md).

## Objective

Deliver the ordinary wipeable Core route from Character Select through Lantern Halls, the private microrealm, the fixed six-room Bell dungeon, Sir Caldus, and either extraction/Recall return or durable death/successor recovery. A tester must complete the route in one native application without a showcase command, developer command, repository shortcut, or client-authored outcome.

## Three design authorities

1. `Gravebound_Production_GDD_v1_Canonical.md`: Core Prototype scope, `LOOP-001`-`003`, `DTH-001`/`010`/`011`/`020`/`021`, `UI-002`/`003`/`007`, `TECH-010`-`023`, `WRLD-001`, `DNG-001`-`006`, and `QA-101`.
2. `Gravebound_Content_Production_Spec_v1.md`: `CONT-WORLD-001`, `CONT-ROOM-007`, Core encounter/boss manifests, `CONT-BOSS-001`/`002`, and `CONT-HUB-001`/`002` fix the exact Hall, microrealm, Bell route, Sir Caldus exit, station availability, and disabled later-stage content.
3. `Gravebound_Development_Roadmap_v1.md`: `GB-M03-03` requires explicit Character Select -> Hall -> microrealm -> six-room dungeon -> boss -> Hall flow; the M03 exit gate requires no-developer-command play, restart durability, nonduplication, timing, and 25 scripted full loops.

## Completed child packages

- [`GB-M03-03A`](GB-M03-03A.md): strict unpromoted Core world-flow content.
- [`GB-M03-03B`](GB-M03-03B.md): durable location, lineage, restore, receipt, and transfer foundations.
- [`GB-M03-03C`](GB-M03-03C.md): exact Hall/private-world simulation and graybox presentation.
- [`GB-M03-03D`](GB-M03-03D.md): fixed Bell rooms, Core normal enemies, and minibosses.
- [`GB-M03-03E`](GB-M03-03E.md): Sir Caldus, durable victory, evidence extraction, and Hall return.
- [`GB-M03-03F`](GB-M03-03F.md): native transition/reconnect projections, disposable real-QUIC journeys, visual evidence, and performance.
- Later dependencies `GB-M03-04`, `05`, `06`, `07`, `08`, `12`, and `13` are closed under their own audits and may now be composed by the normal route.

## Remaining package

[`GB-M03-03G`](GB-M03-03G.md) owns the only remaining parent work: the persistent normal composition root, live private-life actor graph, ordinary native application route, honest capability admission, cumulative full-loop evidence, and parent audit.

## Parent acceptance criteria

1. Normal Character Select `Play` is negotiated, authoritative, and leads to controllable Hall state only after matching scene readiness.
2. The Realm Gate performs exact safe-inventory preflight and durable danger entry; the capacity-one actor owns microrealm movement/combat and opens the Bell portal only after authoritative clear.
3. The exact `B0 -> B6` route, Sir Caldus, rewards, extraction, Recall, lethal death, memorial, successor, and return-to-control paths work without showcase/developer commands.
4. Response loss, reconnect, restart, database outage, stale/foreign requests, actor-generation replacement, terminal races, malformed input, and cleanup preserve the stored authority without duplication or resurrection.
5. Twenty-five ordinary full-loop journeys pass with the M03 timing and durable-graph gates. Optimized native evidence passes both required resolutions and effects modes.
6. Only Core-owned route/station capabilities are enabled. Production namespace cutover, promotion, M04+ content, and all independently owned later surfaces remain fail closed.
7. A final three-authority parent audit is published after hosted Linux/Windows/PostgreSQL/QUIC/native evidence passes.

## Current Next Step

Parent [`GB-M03-03`](GB-M03-03.md) normal integration remains active in [`GB-M03-03G`](GB-M03-03G.md) under [`ADR-037`](../decisions/ADR-037-normal-core-private-route-composition.md). Exact source `e069987` is green under hosted CI [`29633952947`](https://github.com/MikeyPar/Gravebound/actions/runs/29633952947) for the dormant persistent foundation, generation-safe shared-writer session/Recall ownership, terminal-first bootstrap and schema-63 intent foundations, strict quality/content/schema checks, PostgreSQL transactions, Windows release construction, and optimized native evidence. Commit `26c420b` constructs the dormant persistent foundation, commit `551c527` owns the monotonic per-account transport generation and shared reliable writer with dynamic Recall, and commit `cf933f2` adds the dynamic extraction runtime. Extraction now requires an opaque five-producer executor proof, samples a generation-bound authoritative tick only after command dequeue, publishes one cancellation-safe completion, marks retries/handoffs as replayed, binds Hall installation to the exact transport/terminal/result, retires safely for every competing terminal winner, and reports route/runtime residue without closing the continuing Hall writer. Local formatting, 9/9 extraction-intent runtime tests, 11/11 extraction execution tests, the complete 286-test server suite, all workspace targets, and strict workspace all-target Clippy pass at `cf933f2`; an independent authority re-audit found no remaining runtime blocker. Commit `a30e644` adds the terminal-first process bootstrap adapter and post-commit/replayed world-flow reconciliation: first-time identity defers cleanly; Character Select and storage-blocked states create no route actor; restart danger atomically restores to Hall or yields to a committed terminal; Hall generations are durable and reused in-process; stored death/extraction/Recall outcomes remain ordered before destination control; and exact Hall/microrealm/Character Select transition replays converge without rewind while changed material fails closed. Corrective commit `51be807` withholds extraction and Recall Hall actors until the exact stored terminal result is delivered and acknowledged; restart never invents extraction request correlation or exposes destination control first. Focused bootstrap tests pass 5/5, route-actor tests pass 11/11, the complete 293-test server suite and all server targets pass, and strict server all-target Clippy is green. Next, bind the dynamic extraction and Recall terminal publishers through the private-life session owner and its one shared writer, then compose live movement, combat, rewards, and pending-inventory ownership before opening the ordinary route. Keep `core_world_flow_integration`, Character Select `Play`, production Realm Gate interaction, and normal extraction/Recall admission disabled until live movement, combat, rewards, pending inventory, all terminal owners, real-QUIC restart/25-journey evidence, and visual review pass; keep production namespace cutover, Core promotion, M04+ content, Requiem encounters, paid Oath changes, retirement, purge, and unrelated stations disabled under their independent owners.
