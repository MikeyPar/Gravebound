# GB-M03-05A — Core Oath and Bargain content foundation

**Status:** Complete — strict schemas, exact records, editable assets, canonical localization, immutable hashing, compiler closure, and full workspace/client gates pass.

## Objective

Author the two exact Grave Arbalist Oaths and three exact Core Bargains under an independently hashed, non-promotable development target. This package is data-only: it creates no selection state, persistence, interaction, mechanics runtime, Ash mutation, or player-route activation.

## Three design authorities

1. `Gravebound_Production_GDD_v1_Canonical.md`: `CLS-020`, `BRG-001`–`005`, global stat caps, and `ART-020`.
2. `Gravebound_Content_Production_Spec_v1.md`: Core stage allowlists, `CONT-001`–`003`, `CONT-010`–`014`, `CONT-LOC-001`, and `CONT-VALID-001`–`003`.
3. `Gravebound_Development_Roadmap_v1.md`: `GB-M03-05`, M03 exit gates, and approved `SPEC-CONFLICT-008` delivery boundaries.

## Completed slice

- Added strict `OathBargainDevelopmentTarget`, `OathBargainRecords`, `OathRecord`, and `BargainRecord` schemas.
- Closed tagged payloads materialize Long Vigil, Nailkeeper, Cinder Hunger, Bell Debt, and Lantern Ash numeric and lifecycle fields. Free-form scripts/effect arrays and unknown fields are impossible by construction.
- Nailkeeper includes impact, arming, re-entry, snapshot, consumption, and deterministic overflow semantics. Bell Debt includes accepted-emission counting, miss/repeat behavior, snapshot/live-origin behavior, lifecycle persistence/reset, cancellation, and nonrecursive/no-cost repeat semantics.
- Generated schema snapshots and warnings-denied focused static analysis pass.
- The temporary Bargain shrine remains correctly owned by `GB-M03-05D`; 05A contains only the exact two-Oath/three-Bargain data set.
- Authored the exact five approved records with the approved tag vocabulary, source owners, resolution step, and cadence semantics. The compiler rejects numeric, lifecycle, tag, ID/order, stage, class, asset, symbol, localization, warning-copy, source-hash, or promotion drift.
- Added five editable 64×64 SVG choice symbols with ART-020 source hash, dependency, anchor, bundle, readability, memory-budget, variant, and project-original provenance closure.
- Pinned the unpromoted catalog as `core-dev.blake3.91b1a3ab2157371d18dc2c8f48124b4f25c59cc6cf0be46b871274357b285533`; no release label or promotion surface exists.
- Exhaustive Core combination validation proves Long Vigil plus Cinder Hunger resolves maximum health to `79.2%`, above the global `70%` floor, and Cinder Hunger remains below the outgoing-damage cap.
- Closeout passes `cargo fmt --all -- --check`, `cargo run --locked -p tools_content -- validate-core-oaths-bargains`, `cargo test --workspace --exclude client_bevy`, `cargo test -p client_bevy --lib`, and both workspace/client warnings-denied Clippy partitions.

## Current Next Step

Parent [`GB-M03-03`](GB-M03-03.md) normal integration remains active in [`GB-M03-03G`](GB-M03-03G.md) under [`ADR-037`](../decisions/ADR-037-normal-core-private-route-composition.md). Exact source `037efe6` is green under hosted CI [`29630076008`](https://github.com/MikeyPar/Gravebound/actions/runs/29630076008) for the terminal-first bootstrap, schema-63 extraction-intent acceptance/conflict audit, route-owned `TerminalPending` permit, live Caldus intent actor/planner, response-loss/reconnect replay, strict quality/content/schema checks, Windows release construction, and optimized native evidence. Commit `26c420b` constructs the dormant persistent foundation, and commit `551c527` now owns one monotonic transport generation and one shared reliable writer per authenticated account from handshake onward. Recall binds only after a danger actor exists, rebinds before an authoritative handoff becomes visible, preserves exact LinkLost timing, rejects stale teardown, retires independently on terminal return without closing the Hall writer, and aggregates zero-residue shutdown. Local formatting, 2/2 session tests, 4/4 Recall runtime tests, strict all-target server Clippy, and the complete 283-test server suite pass. Next, implement the account/generation-keyed dynamic extraction runtime on this same writer, including committed-result publication and cleanup; then add post-commit world-flow reconciliation and the terminal-first runtime bootstrap adapter. Keep `core_world_flow_integration`, Character Select `Play`, production Realm Gate interaction, and normal extraction/Recall admission disabled until live movement, combat, rewards, pending inventory, all terminal owners, real-QUIC restart/25-journey evidence, and visual review pass; keep production namespace cutover, Core promotion, M04+ content, Requiem encounters, paid Oath changes, retirement, purge, and unrelated stations disabled under their independent owners.

## Exclusions

Initial selection state/persistence/protocol/UI (`05B`); Oath mechanics (`05C`); Ash wallet (`GB-M03-12`); milestone/offers/shrine/UI (`05D`); Bargain mechanics (`05E`); lifecycle closure (`05F`); Core promotion; normal-route activation.
