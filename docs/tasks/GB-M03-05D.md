# GB-M03-05D - Deterministic Veil Bargain offer and shrine choice

**Status:** Complete - see [`GB-M03-05D-audit.md`](../milestones/GB-M03-05D-audit.md).

## Objective

Deliver the temporary Core level-5 Sepulcher Knight milestone, exact deterministic three-choice Bargain offer, durable select/refuse state, authoritative shrine projection, and accessible choice UI without enabling Bargain mechanics, purge, death cleanup, or the normal route.

## Three design authorities

1. `Gravebound_Production_GDD_v1_Canonical.md`: `BRG-001`-`005`, `SIM-003`, resolved-stat caps, and the Core temporary milestone trigger.
2. `Gravebound_Content_Production_Spec_v1.md`: `CONT-014`, `CONT-HUB-002`, the exact three Core Bargain records/localization/assets, immutable content rules, and deterministic validation requirements.
3. `Gravebound_Development_Roadmap_v1.md`: `GB-M03-05`, the M03 idempotency/restart exit gates, and the smallest complete private-life objective.

## Approved implementation contract

- The Core trigger is only the first `miniboss.sepulcher_knight` clear in `layout.core_private_life_01` at level 5 or above. It is disabled in Slice and later bundles.
- A shared `character_oath_bargain_state` aggregate owns positive version, earned slots `0..=3`, and active Bargains in acquisition order. It is the real provider for the existing `OathBargainRestoreV1` seam.
- Qualifying milestone resolution, earned-slot grant, immutable offer creation, and any exact 10-Ash fallback share the progression transaction. A second best-effort transaction is not legal.
- Offer identity is the source reward-event idempotency key. Replay returns the exact prior disposition; concurrent duplicates create one slot/offer or fallback award.
- Candidate planning uses the exact `CONT-014` BLAKE3 preimage, raw 16-byte reward/character UUIDs, length-prefixed immutable content version and UTF-8 candidate ID, unsigned score ordering, and candidate-ID tie break.
- Candidates come only from enabled stage content, exclude active Bargains, satisfy `BRG-004`, and remain within resolved-stat caps. The exact Core catalog normally yields Bell Debt, Cinder Hunger, and Lantern Ash in score order.
- Persist candidate IDs, scores, and order immutably. Emergency one/two-candidate offers show disabled `UNAVAILABLE` cells; zero candidates resolve unavailable, retain the earned slot, and grant exactly 10 Ash once.
- An open offer transitions exactly once to `Selected(offered_id)` or `Refused`. Selection revalidates authority/content/legality and persists the active Bargain before success returns. Refusal has no penalty and leaves the earned slot unfilled. Escape closes UI and is never refusal.
- Mutation replay is checked before current state. Identical retries return the byte-identical stored result; changed material conflicts. Accepted selection and its life event commit atomically.
- The UI shows three ordered cells, exact boon/curse copy, explicit selection confirmation, refusal, unavailable cells, in-flight lockout, and authoritative before/after health, damage, cooldown, movement, healing, plus attack-rate or belt-slot axes when affected. It must support pointer, keyboard, controller, and non-color labels.

## Delivery checklist

- [x] Pure exact scorer, candidate filter, state validator, golden vectors, cap/compatibility enumeration.
- [x] Forward migration and typed repository for shared life aggregate, offers, candidates, active acquisition order, mutation receipts, and outbox.
- [x] Progression transaction integration for the Core milestone and exact Ash fallback.
- [x] Bounded reliable protocol and persistent/disabled runtime authority.
- [x] Server shrine view and replay-first select/refuse coordinator.
- [x] Native accessible three-cell UI with authoritative comparison projection.
- [x] Unit, corruption, concurrency, replay, transfer/reconnect, restart, and real-QUIC PostgreSQL evidence.
- [x] ADR, completion audit, full workspace gates, audiovisual inspection, and normal-route gate verification.

## Current Next Step

Parent [`GB-M03-03`](GB-M03-03.md) normal integration is active in [`GB-M03-03G`](GB-M03-03G.md) under [`ADR-037`](../decisions/ADR-037-normal-core-private-route-composition.md). Exact `CorePrivateLifeContent`, the capacity-one phase actor, and PostgreSQL migration `0062` generation authority are green at exact source `c768a62` under hosted CI [`29622689323`](https://github.com/MikeyPar/Gravebound/actions/runs/29622689323): Linux quality/content/schema gates, mandatory PostgreSQL migration/restart/concurrency coverage, optimized Windows construction, and optimized native evidence pass. This proves the closed Hall/micro-realm/B0-B6/Caldus content graph, route-phase authority, terminal generation retirement, and serializable monotonic allocation; live movement, combat, rewards, pending inventory, terminal execution, and normal composition remain open. Local commits `23fe3ae`, `b02f0f6`, and `d5fd282` now derive restart-stable character IDs, serialize Bell prepare/commit/reconciliation through the actor mailbox, and deliver cross-stream reliable events through a bounded contiguous client inbox; complete protocol/client/server library tests and strict targeted Clippy pass locally. Run their cumulative hosted proof next, then add the common per-transport writer, terminal-first bootstrap and live extraction intent, the dedicated `BoundCorePrivateLifeServer`, and the ordinary native route. Keep normal capability advertisement, Character Select Play, and Realm Gate interaction disabled until the complete runtime and cumulative evidence pass; keep production namespace cutover, Core promotion, M04+ content, Requiem encounters, paid Oath changes, retirement, purge, and unrelated stations disabled under their independent owners.
