# GB-M03-06E - Integrated death, Echo, and memorial closure

**Status:** Approved; final closure slice blocked on `06A`-`06D` and `GB-M03-13` implementation.

## Objective

Close `GB-M03-06` through the production reliable-QUIC and PostgreSQL boundaries with restart, replay, crash, concurrency, adverse, performance, cleanup, and visual evidence.

## Authority mapping

1. `Gravebound_Production_GDD_v1_Canonical.md`: `DTH-001`/`002`/`020`/`021`, `ECH-001`/`002`, `TECH-015`, `TECH-020`-`023`, `TECH-070`, and `QA-005`-`007` require exact terminal behavior, durable evidence, recovery targets, adverse networking, and measured closure.
2. `Gravebound_Content_Production_Spec_v1.md`: immutable Core content, `CONT-ECHO-009`, `CONT-HUB-001`, and `CONT-HUB-002` require exact Echo transition/promotion and Memorial presentation under the Core stage manifest.
3. `Gravebound_Development_Roadmap_v1.md`: `GB-M03-02D`, `GB-M03-06`, `GB-M03-13`, and the M03 exit gates require restart preservation, atomic qualifying death, no duplicates, 25 full-loop journeys, and human-readable cause evidence.

Approved [`SPEC-CONFLICT-009`](../spec-conflicts/SPEC-CONFLICT-009-m03-death-memorial.md) is the audit baseline for memorial scope, destruction order, clocks/deeds, and death/Echo atomicity.

## Integrated evidence contract

- Exercise authenticated selected-character danger entry, lethal combat, terminal commit, durable summary acknowledgement, reconnect, server-process restart, Memorial list/detail, and eligible/ineligible Echo projection through real reliable QUIC and hosted PostgreSQL. No fixture may call a coordinator in place of an action that has a production protocol surface.
- Compare one canonical terminal signature before response loss, after exact replay, after reconnect, and after a newly bound server process. It includes death/summary/memorial/trace/destruction/receipt/outbox rows, character/item/aggregate versions, preserved safe storage, Bargain/Bell cleanup, and Echo state/audit.
- Close exact replay, changed-payload conflict, stale/foreign authority, duplicate sessions, concurrent lethal writers, same-tick terminal races, database outage, serialization retry, injected rollback at every participant, corrupt durable state, malformed/oversized frames, incompatible content, and disabled normal-route behavior.
- Crash immediately before and after lethal acceptance, UUID journal, destruction planning, item ledger writes, memorial write, Echo Dormant insert, Echo promotion, receipt, commit, response, and process restart. Each case converges to one complete committed result or the unchanged pre-death state.
- Prove practice/tutorial/developer lethal flows produce no durable death/item/memorial/Echo mutation and retain the persistent practice label.
- Run a deterministic death-journey matrix large enough to exercise every eligibility and terminal branch; record death-commit-to-summary median/p95/max, query latency, mutation round-trip, resource cleanup, locks/transactions, and process memory. The summary must become interactive within two seconds of durable acknowledgement. The roadmap's 25 complete private-loop journeys remain a final integrated M03 gate.
- Capture and inspect optimized native summary and Memorial Wall evidence at 1280x720 and 1920x1080 in standard and reduced-effects modes with build/content IDs, dimensions, scenario, and file hashes.

## Delivery and test checklist

- [ ] Compose completed `06A`-`06D` and `GB-M03-13` through real QUIC/PostgreSQL without bypassing their authority seams.
- [ ] Pass the complete replay/restart/crash/concurrency/adverse matrix and exact canonical-signature comparison.
- [ ] Pass the deterministic death-journey matrix, performance targets, zero leaked tasks/sessions/transactions/locks, and stable memory evidence.
- [ ] Capture and inspect the required optimized visual matrix and evidence manifest.
- [ ] Run formatting, warnings-denied workspace Clippy, workspace tests, content/hash/schema generation checks, hosted PostgreSQL, relevant real-QUIC suites, determinism fixtures, and optimized Windows construction.
- [ ] Publish three-authority audits for `GB-M03-06E`, parent `GB-M03-06`, `GB-M03-13`, and `GB-M03-02D`; advance every applicable `Current Next Step`.
- [ ] Keep parent `GB-M03-03` and the normal production route closed until `GB-M03-07` and `GB-M03-08` also pass.

## Exclusions

Successor implementation, extraction/Recall implementation, telemetry export, support lookup, Echo encounter assembly/rewards, human cohort execution, hosting/platform evidence, production namespace cutover, Core promotion, and M04+ content.

## Current Next Step

Commit `ac062c1` and hosted CI `29442803974` establish the disjoint world/presentation authority, producer-admission, PostgreSQL restart, and real-QUIC read foundation required by this package; they do not close `06D` or `06E`. Complete the durable-ack presentation reducers and native surfaces first. Then create the full canonical death signature and remaining crash/concurrency matrix before collecting performance, cleanup, and optimized visual evidence and publishing the three-authority audits.
