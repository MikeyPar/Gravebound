# GB-M03-03E — Sir Caldus, boss lock, and committed Hall return

**Status:** Complete under [`GB-M03-03E-audit`](../milestones/GB-M03-03E-audit.md); normal route admission remains disabled.

## Objective

Deliver production Sir Caldus as the Core major boss, the exact DNG-006 participant lock and reset authority, personal Caldus reward closure, a stable committed-extraction exit, and an authoritative idempotent return to Lantern Hall. Preserve the strict 03D room/content foundation and keep GB-M03-08 inventory extraction plus the normal player route fail closed.

## Three design authorities

1. `Gravebound_Production_GDD_v1_Canonical.md`: `COM-001`–`006`, `DTH-001`, `DTH-010`–`011`, `WRLD-006`, `DNG-006`, `ENC-005`, `ENC-010`, `ENC-020`, `SOC-010`, and `TECH-004`.
2. `Gravebound_Content_Production_Spec_v1.md`: `CONT-010`, `CONT-ROOM-001`–`002`, `CONT-ROOM-005`–`007`, `CONT-REWARD-003` Core overrides, `CONT-PATTERN-001`, `CONT-BOSS-001`–`002`, major bindings, assets/localization, and validation matrices.
3. `Gravebound_Development_Roadmap_v1.md`: `GB-M03-03`, M03 one-major-boss and presentation targets, restart/idempotency gates, `GB-M03-08` ownership, and approved `SPEC-CONFLICT-006` `03E` split/route gate.

Approved [`SPEC-CONFLICT-022`](../spec-conflicts/SPEC-CONFLICT-022-m03-caldus-phase-charge-and-targeting.md) fixes only missing Caldus threshold, movement, gap, and targeting determinism. Approved [`SPEC-CONFLICT-023`](../spec-conflicts/SPEC-CONFLICT-023-m03-boss-lock-extraction-return.md) fixes boss-roster, reset, identity, reward/exit, extraction-receipt, and Hall-return ordering while preserving the GB-M03-08 boundary. Approved [`SPEC-CONFLICT-024`](../spec-conflicts/SPEC-CONFLICT-024-m03-caldus-cardinal-charge-axis.md) fixes the remaining cardinal charge-axis and exact diagonal tie without changing authored charge values.

## Exact content and simulation scope

- Add one strict non-promotable `boss.sir_caldus` record bound to `arena.boss.caldus_01` and `reward.boss_caldus`, four exact pattern records, complete assets/localization, and stable hashes. Remove only B6's `boss_behavior_disabled` state and bind no other boss.
- Compile exact 7,200 solo health, armor 10, collision/hurtbox 0.70/0.62, cardinal charge endpoints, stage/group points, Core reward override, XP profile 450, and first-clear bonus seam.
- Implement visible load/countdown/door/lock/introduction states, immutable `N_locked`, exact WRLD-006 health scaling for N=1..8, no late entry/rescale/revive, legal Recall, and the exact zero-living reset.
- Implement the complete three-phase Caldus scheduler, four-second breaks with +25% incoming damage, phase cleanup, stationary/charge/return movement, projectiles/lanes/contact, rotating group Shield targets, below-20% acceleration, and cadence-only 360-second soft enrage.
- Resolve victory once, commit exact personal reward/progression outcomes idempotently, clear hostiles, and create the stable exit only after durable reward terminality.
- Implement typed extraction request/committed-receipt authority and the resulting idempotent `HallDefault` transfer. Do not implement or simulate production inventory extraction behind that receipt.
- Provide disposable native standard/reduced-effects evidence for staging, phase learning, pressure/charge, final rings, defeat/reward/exit, and committed Hall return without enabling normal ingress.

## Acceptance criteria

1. Strict schema/compiler closure proves exact boss/pattern/exit records, B6 binding, reward/XP references, assets/localization, stable hashes, and no unrelated Core or promoted-content mutation.
2. Golden scheduler tests pin every warning, lock, release, break, cancellation, priority, target, gap, movement segment, loop restart, low-health transition, and soft-enrage boundary at 30 Hz.
3. Integrated combat uses real health/armor/hurtboxes, friendly damage, hostile projectiles/lanes/contact, phase cleanup, collision, defeat, and deterministic identity owners; it passes N=1..8 scaling plus 150–210 second solo-duration fixtures.
4. Participant authority passes all-loaded/timeout countdown, closure, immutable lock, late-entry rejection, death/Recall/disconnect non-rescale, spectate/leave, reset cancellation/expiry, and no-outside fail-closed adversarial cases.
5. Reward, exit, extraction, and Hall transfer pass duplicate, retry, payload-mismatch, disconnect, crash-supersession, wrong-receipt, transfer-failure, and ordering tests without duplication, deletion, or premature transfer.
6. The production world-flow gate cannot use the boss exit without an externally committed extraction receipt; GB-M03-08 inventory/Overflow/ResolutionHold ownership remains structurally absent from 03E.
7. Deterministic complete-combat and defeat-to-Hall traces are byte-identical across repeated runs and prove reset/retry identity nonreuse.
8. Inspected optimized native evidence at 1920×1080 and 1280×720 proves readable standard/reduced-effects telegraphs, safe entrance, participant countdown, phase language, reward-gated stable exit, and authoritative Hall arrival without obstructing the playfield.
9. Focused/workspace tests, warnings-denied Clippy, strict content validation, deterministic traces, generated-schema checks, optimized Windows construction, and hosted CI pass before the completion audit.

## Exclusions

GB-M03-08 pending-inventory conversion, Overflow, ResolutionHold, and Recall loss; production Character Select `Play`; Realm Gate/Bell portal admission; seeded layouts and branch traversal; 03F loading/error/reconnect/real-QUIC closure; public/multiplayer allocation; ship-quality M05 art/audio; release packaging; and Core promotion.

## Implementation order

1. Add strict Caldus/pattern/exit schemas, records, assets, localization, binding, and compiler mutation tests.
2. Implement participant staging, lock, scaling, introduction, empty reset, and stable identity derivation.
3. Implement the exact scheduler, movement, hostile materialization, real combat, phase cleanup, defeat, and duration/replay fixtures.
4. Join durable personal rewards, exit visibility, extraction receipt, and idempotent Hall transfer without importing GB-M03-08 inventory logic.
5. Add native evidence, cumulative gates, completion audit, and roadmap handoff.

## Completed foundation

- A separate strict `core-dev-caldus` target preserves the completed 03D normal/miniboss package while adding exactly one major boss, four exact hostile patterns, one reward-gated dungeon exit, and one typed B6 overlay. It exposes no promotion or production-ingress operation.
- The Caldus record transcribes exact arena anchors, N=1–8 health scaling, armor/radii, load/countdown/introduction/reset timings, phase thresholds/breaks, all three schedulers, group targeting thresholds, movement policy, low-health loop, soft enrage, reward/XP references, and target solo duration.
- Every pattern materializes typed damage band, counterplay, memory, disposition, hit-group rule, normalized CONT-013 fields, derived warning/major-audio assets, and persisted active-instance maximum. Free-form semantic strings and implicit defaults are rejected.
- B6 remains outside the ordinary room-encounter schema, but its boss anchor now binds `boss.sir_caldus`; the overlay proves `boss -> arena -> reward/XP -> exit` closure against the validated room, item, and progression packages.
- `portal.exit.dungeon.bell_sepulcher` is distinct from dungeon ingress, fixed at `(2.5,9)`, targets `hub.lantern_halls_01` with typed `HallDefault`, and requires a committed extraction receipt. Its asset, tags, localization, and reward prerequisite are exact.
- Four checked-in JSON Schemas, independent BLAKE3 source pins, a dedicated validation command/CI gate, exact N=1–8 scaling evidence, and mutation tests cover release-metadata leakage, hashes, boss scalars/timelines, participant policy, every pattern domain, B6 binding, exit receipt, assets, and copy.
- A clone-stage-commit DNG-006 authority now owns typed connection/life input, all-loaded or exact 300-tick timeout staging, the visible 150-tick ready countdown, mutable preclosure roster, zero-entrant abandonment without consuming attempt identity, and immutable `(party_slot, entity_id)` lock order.
- Door closure atomically allocates exact N=1–8 round-half-up Caldus health, clears the entrance radius, and starts the invulnerable/nonattacking 75-tick introduction. Recall remains legal, death/Recall/departure never rescales, late entry is rejected once per identity, and M03 runtime capacity remains one while all eight scaling values are executable.
- Zero living locked participants start the 150-tick reset only when a living unlocked party member remains outside. Reentry by a living locked participant cancels on the boundary tick; expiry clears hostiles/unsecured drops, reopens BossWarning, preserves consumed attempt identity, and permits a freshly scaled lock. No outside survivor remains fail closed for later retirement/crash ownership.
- A separate clone-stage-commit Caldus scheduler now pins Phase 1 `234`, Phase 2 `450`, Phase 3 `240`, and low-health `216`-tick loops; exact Shield, Bell Ring, preview, charge lock/start/end, and Stop Ring ticks; persistent `+5 mod18` gap reservations; and immutable nearest/rotating target locks.
- Phase thresholds use widened integer percentage comparisons, cancel all pending actions/hostiles before exact 120-tick breaks, apply the authored 125% incoming-damage window, and handle overkill by entering only the next break before chaining on the first post-break tick. Lethal health defeats immediately without a redundant transition.
- N=4–6 and N=7–8 Shield starts schedule two/three distinct complete fans at exact 12-tick staggering and advance the immutable-slot cursor after the final selected target. Phase 3 children add no ordinary ring warning, below-20% affects only the next loop, and the 10,800-local-tick soft enrage changes only future downtime to exact `230/443/234` loop lengths.
- Scheduler releases now materialize exact Shield Arc, Bell Ring, and Charge Stop Ring projectiles through the shared hostile simulation. Closed validators pin their authored geometry, damage, speed, radius, lifetime, threat, counterplay, memory, disposition, and active-instance limits; invalid/future releases roll back without consuming entity identity.
- A separate clone-stage-commit fixed-point body authority snapshots the nearest target position at the exact `+700 ms` boundary, selects the authored cardinal axis deterministically, owns all 17 movement segments, truncates against shared swept solids, and orders one contact per participant by contact time/immutable slot/entity. Its realized endpoint owns the exact opposite Stop Ring gap; the body then returns toward `(9,9)` at two tiles per second and stops within `0.25` tiles.
- A separate clone-stage-commit health authority owns the N-locked maximum, armor `10`, live `0.62` hurtbox, exact break multiplier, single lethal terminality, and per-participant applied-health contribution. Friendly intent provenance is now one shared fail-closed validator used by both Caldus and the prior Bell Proctor aggregate, preserving real collision evidence and stable projectile/contact order.
- A clone-stage-commit encounter aggregate now advances friendly damage, thresholds, scheduler, fixed-point body, hostile cleanup/spawn, canonical 48-raw charge contact, and multiplayer projectile collision atomically. Its integration fixture crosses 70%, completes the exact break, runs all 17 charge segments, damages the locked player once, and materializes 12 Stop Ring projectiles from the realized endpoint; external identity failure rolls back both encounter and player stores.
- The complete solo combat fixture now runs every real aggregate tick at 30 Hz, crosses thresholds at scripted contribution boundaries, enters Phase 2/3 after their exact breaks at ticks `1920/3720`, defeats at tick `5400` (`180 s`), and proves terminal hostile cleanup. A second full run produces the identical BLAKE3 trace, remaining inside the authored `150–210 s` target without bypassing combat systems.
- A pure victory authority now derives domain-separated, length-delimited BLAKE3 encounter, per-participant reward request, exit instance, and per-participant extraction request/receipt IDs from lineage plus consumed attempt identity. Identical retry reproduces every ID, new attempts cannot reuse them, and domains/owners cannot collide.
- SOC-010 evaluation now uses locked scaled reference health and typed evidence for exact 50% presence, 0.5% contribution, 20-second inactivity, under-20-second presence exception, alive/present defeat state, Recall, session, and anti-cheat requirements. Direct/support/objective contribution uses exact integer centi-units; objective credit is capped at two. Core boss XP `450` and first-clear `225` are pinned.
- Schema `25` adds a normalized durable Caldus victory/exit gate. It advisory-locks stable encounter identity, verifies every eligible owner against committed `reward_requests` plus accepted non-revoked `character_xp_award_results`, pins exact reward/source IDs and base XP, rejects hash/owner/payload drift, and inserts the stable exit authorization once. Identical replay returns the stored exit; no GB-M03-08 inventory/Overflow/ResolutionHold field exists in this boundary.
- A server-side victory coordinator now consumes the immutable lock and typed SOC-010 evidence, derives stable owner IDs, submits exact `reward.boss_caldus` grants through the existing deterministic item service, submits `boss.sir_caldus` encounter evidence through the existing progression/first-clear service, captures both durable hashes, and invokes the schema-25 gate only after every eligible owner returns `Accepted`. Partial completion remains replayable and cannot expose an exit.
- A disposable-PostgreSQL integration target now exercises the real item, progression, and schema-25 repositories together. It pins a fresh two-equipment plus Red Tonic x2 and 450+225 first-clear commit, exact replay with the same exit identity, changed-payload idempotency rejection without another exit, and recovery from an item-only terminal after the exit gate proves XP is still absent. The target remains explicitly opt-in and destructive-database guarded.
- A typed Caldus instance-presentation owner starts with no exit, loads the strict authored exit asset/copy/tags/point/destination, and can be populated only from a complete durable victory result matching the current lineage and attempt. Hidden reset advances the attempt and rejects stale identity; exact committed replay is stable; conflicting replay fails closed; a presented durable exit cannot be erased by reset.
- Schema `26` adds the normalized wipeable `Requested` to `Committed` Caldus extraction receipt seam. Request and receipt hashes are independent and replay-safe; request validates the exact committed exit plus active lineage/restore/version; receipt commit rejects a completed crash restore, atomically marks the restore root extraction-final and closes the lineage, and deliberately leaves the location in Danger for Hall-transfer retry. Static and API boundaries contain no item, inventory, material, storage, Overflow, ResolutionHold, or wallet mutation.
- Protocol `1.11` adds an explicit `UseCommittedExtraction` command that carries the stable request and receipt identities separately. The ordinary dormant portal coordinator rejects it; only the wipeable Caldus authority can compose a presented reward-terminal exit with schema `26` and submit the exact participant-bound identities.
- The dedicated Caldus Hall coordinator consumes only the matching committed receipt from the exact current danger lineage/restore point. One serializable write records command kind `3`, advances the character exactly once to `hub.lantern_halls_01`/`HallDefault`, consumes the extraction result, and removes the lineage-bound danger checkpoint; wrong receipts are durably rejected, accepted retries replay the stored result after a fresh database-pool connection, and the inventory version is untouched. A forced checkpoint-lineage mismatch proves a late transfer failure rolls back staged receipt consumption and location mutation before exact retry succeeds. A separate race fixture leaves the request uncommitted when crash restore wins before receipt commit. The destructive PostgreSQL fixtures compile these traces and remain opt-in until a disposable database is configured.
- Hall preflight is a separate fail-closed boundary with direct fixtures for exact payload hash, world-flow content revision, issuance time, wipeable namespace, and the authored Caldus exit command. Invalid requests cannot acquire a database transaction or consume a committed receipt.

## Current Next Step

This package's recorded status is unchanged. Commits through `a9a17e0` now move the exact B5 authority into a route-bound B6 owner and prove continuous inherited ticks, countdown, immutable participant lock, resumable introduction, same-tick Phase 1 creation, fail-closed route CAS, distinct `0.70` physical-body collision without replacing the `0.62` damage hurtbox, and deterministic rollback-safe charge separation. Next execute the complete route-bound defeat trace before reward/exit composition. See [`GB-M03-03G`](GB-M03-03G.md), the [handoff evidence](../evidence/GB-M03-03G-caldus-staging-handoff-evidence.md), and the [runtime evidence](../evidence/GB-M03-03G-caldus-route-runtime-evidence.md).

### Historical cumulative record

Parent [`GB-M03-03`](GB-M03-03.md) normal integration remains active in [`GB-M03-03G`](GB-M03-03G.md) under [`ADR-037`](../decisions/ADR-037-normal-core-private-route-composition.md). Exact source `e069987` is green under hosted CI [`29633952947`](https://github.com/MikeyPar/Gravebound/actions/runs/29633952947) for the dormant persistent foundation, generation-safe shared-writer session/Recall ownership, terminal-first bootstrap and schema-63 intent foundations, strict quality/content/schema checks, PostgreSQL transactions, Windows release construction, and optimized native evidence. Commit `26c420b` constructs the dormant persistent foundation, commit `551c527` owns the monotonic per-account transport generation and shared reliable writer with dynamic Recall, and commit `cf933f2` adds the dynamic extraction runtime. Extraction now requires an opaque five-producer executor proof, samples a generation-bound authoritative tick only after command dequeue, publishes one cancellation-safe completion, marks retries/handoffs as replayed, binds Hall installation to the exact transport/terminal/result, retires safely for every competing terminal winner, and reports route/runtime residue without closing the continuing Hall writer. Local formatting, 9/9 extraction-intent runtime tests, 11/11 extraction execution tests, the complete 286-test server suite, all workspace targets, and strict workspace all-target Clippy pass at `cf933f2`; an independent authority re-audit found no remaining runtime blocker. Commit `a30e644` adds the terminal-first process bootstrap adapter and post-commit/replayed world-flow reconciliation: first-time identity defers cleanly; Character Select and storage-blocked states create no route actor; restart danger atomically restores to Hall or yields to a committed terminal; Hall generations are durable and reused in-process; stored death/extraction/Recall outcomes remain ordered before destination control; and exact Hall/microrealm/Character Select transition replays converge without rewind while changed material fails closed. Corrective commit `51be807` withholds extraction and Recall Hall actors until the exact stored terminal result is delivered and acknowledged; restart never invents extraction request correlation or exposes destination control first. Focused bootstrap tests pass 5/5, route-actor tests pass 11/11, the complete 293-test server suite and all server targets pass, and strict server all-target Clippy is green. Commit `93dde0a` unifies dynamic Recall, extraction, and restart recovery on the private-life session's single writer: symmetric two-phase handoffs are recoverable and exact; reconnect rebinds both owners before visibility; only the session retires shared transports; stored Explicit/LinkLost Recall and exact extraction retries remain account/character/content/version bound; current outer ticks and stored inner correlation are preserved; and Hall remains gated behind exact terminal delivery. Real-QUIC composition proves one writer identity, contiguous sequences `1/2/3` per transport, stale-detach safety, coordinated reconnect, and zero residue. Local formatting/diff checks, strict server/workspace all-target Clippy, 301/301 server library tests, all workspace tests, four content validators, and generated-schema verification pass. Commit `45c8bcb` integrates the provenance-complete, hash-locked Realm Gate, Lantern Fork, and Bell Sepulcher art into the optimized disposable microrealm at both certified resolutions and effects modes without changing compiled authority or normal admission. Commit `37cf99d` adds rollback-safe live microrealm movement, lifecycle, server-clear, and Bell-range ownership; focused tests cover the complete spawn-to-Bell route and stale-authority rollback. Commit `736cb59` adds provenance-complete Drowned Pilgrim, Bell Reed, and Chain Sentry candidates; keep them outside runtime/content hashes until optimized B1/B5 telegraph and anchor review passes. Commit `e5e1a9b` binds the live microrealm owner and exact route lease to the persistent account session: reconnect receives the same allocation before visibility, stale transports are denied, `LinkLost` preserves danger authority, exact unbind leaves the shared writer alive, and shutdown reports zero residue. Formatting, strict server all-target Clippy, `306/306` server library tests, every enabled server target, and focused real-QUIC handoff/reconnect proof pass locally. Commits `302ccb3`, `f07a282`, and `587924e` now extract the lifecycle-free `pack.bell.01` owner, move the single mutable character-combat allocation through an exact rejoin envelope, and remove tick/displacement/damage/clear authority from ingress. Each invoked frame generates the next tick, equipment-speed displacement, player combat/collision, hostile wave step, and exact clear projection under one staged route compare-and-swap. Strict all-feature/all-target Clippy, all `125/125` simulation tests, and all `309/309` server tests pass locally. Cumulative hosted run [`29639908490`](https://github.com/MikeyPar/Gravebound/actions/runs/29639908490) is green for exact source `587924e`, including the prior pack and handoff commits. Commits `ffd74d6` and `370b53e` add provenance-complete Bell VFX and pending-loot risk candidates; both remain outside registries and content/runtime hashes pending optimized in-engine review. Commits `f75c662`, `8d74ca7`, and `3c6af83` now compose exact compiled-shell movement and Slipstep in the same rollback-safe live combat frame; local `388/388` simulation-core, `126/126` content, `310/310` server, formatting, and strict all-feature/all-target Clippy gates pass. Commit `ea6445c` adds verified unregistered B0/B4/B6 landmark candidates without changing registry or content authority. Hosted CI runs [`29640683239`](https://github.com/MikeyPar/Gravebound/actions/runs/29640683239), [`29640707913`](https://github.com/MikeyPar/Gravebound/actions/runs/29640707913), and [`29640811604`](https://github.com/MikeyPar/Gravebound/actions/runs/29640811604) are green for the Slipstep, landmark, and evidence sources. Commits `c3bc57e`, `995151c`, and `4516b1a` move input validation to session ingress, exclusively own the runtime behind a one-slot/reliable-action 30 Hz driver, preserve danger ticks with LinkLost neutralization, freeze exact terminal/fault frames, retain one generation-safe observer across reconnect, and join through a transport-independent binding lease. Local paused-time tests pass `5/5`, the cumulative server suite passes `315/315`, and strict all-target/all-feature Clippy is green; hosted run [`29641751122`](https://github.com/MikeyPar/Gravebound/actions/runs/29641751122) was still in progress at the latest poll and is not yet claimed green. Commits `42d9f85` and `9694790` provide fail-closed completed-room handoffs and one lifecycle-free B0-B6 owner; commits `5deee6d`, `f044a61`, and `6749141` add collision-safe room entry, continuous hostile identities, exact Bell version/tick/player/projectile handoff, and atomic persistent route CAS; commit `a2a5b09` now converts the runtime inside the existing session-owned 30 Hz task, freezes unknown durable outcomes, survives acknowledgement loss, and preserves one observer/writer across reconnect; commits `d63b675` and `8d62f6f` add unregistered Caldus Stop Ring and defeat candidates. Commits `b50a8c0` and `09c9c9e` now generate server-owned fixed-room movement/combat frames inside that same task, preserve tick/input/player/route identity across B1 and reconnect, reject stale transports and early advances without faulting, and freeze lethal/fault states; local strict Clippy, `131/131` sim-content tests, `326/326` server library tests, every enabled server target, and focused real-QUIC proof pass. Commit `a6fb62e` adds an unregistered, provenance-complete B4 `Open`/`Selected`/`Refused` shrine-state review pack outside runtime hashes. Commit `e1e4d7c` binds committed/replayed `Selected`, `Refused`, and authoritative no-offer B4 results to the same session task through an opaque account/character/lineage receipt; local `242/242` persistence and `330/330` server tests plus strict all-target/all-feature Clippy pass, while hosted run [`29648007340`](https://github.com/MikeyPar/Gravebound/actions/runs/29648007340) remains pending. Next, commit the B3 reward and milestone result through the normal coordinator before B4 response delivery, then implement the B5 bridge, Sir Caldus at B6, durable personal rewards/pending inventory, the stable B6 exit, and all five terminal producers. Keep `core_world_flow_integration`, Character Select `Play`, production Realm Gate interaction, and normal extraction/Recall admission disabled until fixed-room combat, rewards, pending inventory, all terminal owners, real-QUIC restart/25-journey evidence, and visual review pass; keep production namespace cutover, Core promotion, M04+ content, Requiem encounters, paid Oath changes, retirement, purge, and unrelated stations disabled under their independent owners.
