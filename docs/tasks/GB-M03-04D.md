# GB-M03-04D — Durable starter and reward item lifecycle

**Status:** Complete — implementation, ADR, authorized disposable-PostgreSQL lifecycle evidence, restart proof, and the completion audit pass.

## Goal

Deliver the first atomic starter initializer and the durable production reward lifecycle: immutable equipment and consumable-unit identities, deterministic replay, `RunBackpack` and personal-ground placement, provenance ledger, and atomic ground expiry.

## Authorities

1. `Gravebound_Production_GDD_v1_Canonical.md`: LOOT-002, starter inventory, reward placement, immutable provenance, and TECH-023.
2. `Gravebound_Content_Production_Spec_v1.md`: exact 18-item Core catalog, source/reward records, Red Tonic stack-six behavior, and Core development revision.
3. `Gravebound_Development_Roadmap_v1.md`: `GB-M03-04`, `GB-M03-02C`, M03 exit gates, and normal-route gating.
4. Approved `SPEC-CONFLICT-007`, `SPEC-CONFLICT-012`, and `SPEC-CONFLICT-013` provide the durable item, reward RNG, UID, and consumable-placement contracts.

## Approved implementation contract

- Starter initialization is idempotent per character and atomically grants Worn Pine Crossbow, Worn Cracked Mark Lens, empty Armor/Charm, and two distinct Grant-provenance Red Tonic units in Belt index `0`.
- Reward and starter item UIDs are opaque 16-byte, domain-separated BLAKE3 derivations. Zero and collisions fail closed.
- Every consumable unit keeps its own UID and provenance; stack projections order and consume units by ascending unsigned UID.
- Reward equipment and Tonics enter `RunBackpack`; Tonics merge matching stacks before the lowest empty slot. Full legal capacity falls back to recipient-only `PersonalGround` for 60 seconds.
- Personal-ground expiry atomically records `Destroyed(ground_expired)` and its immutable ledger event.
- Reward request retry returns the stored plan/result without consuming RNG or mutating inventory again. Canonical request mismatch is an idempotency conflict.
- Wipeable development instances use only `core-dev.blake3.<full-manifest-hash>` content revisions.

## Delivery checklist

- [x] Owner approval for production reward entropy and audit contract.
- [x] Owner approval for UID derivation and reward consumable placement.
- [x] Typed forward migrations with exact constraints, indexes, normalized units, and wipe-safe ledger ownership.
- [x] Deterministic UID derivation and reward placement helpers with canonical test vectors.
- [x] Atomic idempotent starter initializer and existing-character backfill path. Bootstrap and mutation reconciliation fail closed until every returned character has its exact durable kit.
- [x] Secret-backed production reward seed/request/audit derivation with redacted configuration, unbiased ChaCha8 draws, and fixed vectors.
- [x] Replay-first reward transaction with pre-plan request reservation, complete normalized placement result, epoch-rotation-safe replay, and immutable ledger.
- [x] Atomic personal-ground expiry transaction with inventory-first locks, exact tick boundary, deterministic event IDs, and once-per-owner version advance.
- [x] Unit, corruption, concurrency, replay, and disposable-PostgreSQL evidence. The live fixture proves starter backfill, reward replay across secret rotation, ground placement/expiry, ledger conservation, corruption refusal, restart, and concurrent writers.
- [x] Full workspace gates, ADR/task audit, and normal-route gate verification. Workspace tests, warnings-denied Clippy, ADR-030, route gating, and `docs/milestones/GB-M03-04D-audit.md` pass.

## Current handoff

`GB-M03-04E`-`04G` and parent `GB-M03-04` are complete. Continue [`GB-M03-06A`](GB-M03-06A.md)/[`GB-M03-02D`](GB-M03-02D.md), followed by extraction/Recall and successor recovery. Keep the reward service and expiry worker off the normal route until those terminal packages close.
