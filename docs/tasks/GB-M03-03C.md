# GB-M03-03C - Lantern Hall and private-microrealm simulation/presentation

**Status:** In progress - exact simulation and native standard/reduced-motion presentation foundations pass focused gates and inspected release-build evidence; scripted runtime trace/evidence closure is next.

## Objective

Build the renderer-independent simulation and native graybox presentation for the exact Lantern Halls and capacity-one Core private microrealm compiled by `GB-M03-03A`, using the durable location/transfer seams closed by `GB-M03-03B`. This slice must prove collision, movement, safety, interaction projection, portal state, navigation, palette/readability, localization, and deterministic traces without enabling the normal player route or implementing `03D` encounter behavior.

## Three design authorities

1. `Gravebound_Production_GDD_v1_Canonical.md`: `WRLD-001`, `UI-030`, `ART-001`-`002`, `ART-005`-`006`, `ART-010`-`011`, `ART-020`, `TECH-004`, and `TECH-020`-`023`.
2. `Gravebound_Content_Production_Spec_v1.md`: `CONT-WORLD-001`, `CONT-WORLD-003`, `CONT-HUB-001`-`002`, exact Core stage manifests, world/hub IDs, geometry, roads, safe/portal volumes, station range/hold rules, and closed-reason copy.
3. `Gravebound_Development_Roadmap_v1.md`: `GB-M03-03`, the M03 graybox Hall/microrealm scene-kit requirement, cumulative presentation targets, private-life exit gate, and approved `SPEC-CONFLICT-006` `03C` split/capacity-one/route gate.

## Scope

- A renderer-independent `sim_core` world-scene module with validated northwest-origin bounds, radius-aware solid collision, safe volumes, roads, station/portal volumes, stable object IDs, and deterministic state hashing.
- A `sim_content` compiler adapter that converts only the immutable `core-dev-world-flow` records into simulation definitions; no duplicate hand-authored coordinates or runtime defaults.
- Exact Lantern Halls simulation: `64x48`, solid shell/desks/plinth, radius-0.30 player, default and character-select-return arrivals, five Core station projections, noncombat creation prohibitions, and direct navigability.
- Exact private microrealm simulation: `48x48`, solid shell, clear-mud floor, walkable Realm Gate, player spawn, width-five road, Lantern Fork safe circle, Bell portal area, and capacity one.
- A deterministic encounter-trigger seam for first movement beyond one tile or primary release, one-second delay, 900 ms warning request, active/reset/cleared lifecycle, five-second empty reset, and Bell-portal availability. `03D` supplies `pack.bell.01` entities and clear authority.
- Server-owned interaction projection with range 1.5, exact instant/500 ms semantics, one panel per player, authoritative disabled reasons, and no client-selected destination or enabled state.
- Native Bevy graybox scenes generated from the compiled definitions, with camera, local movement, collision, readable station/portal markers, safe-zone language, muted wet-stone/brass/ash palette, exact localized names, keyboard controls, and standard/reduced-motion behavior.
- Explicit disposable showcase/test entry only. Character Select `Play`, production Realm Gate admission, and `core_world_flow_integration` advertising remain disabled.
- Fixed-trace, geometry, collision, navigation, safety, interaction, lifecycle, content-drift, native render, 1920x1080, 1280x720, and reduced-motion evidence gates.

## Acceptance criteria

1. Runtime geometry is compiled from the exact `03A` records and rejects missing, extra, mismatched, out-of-bounds, overlapping, or semantically invalid scene data.
2. Hall movement cannot penetrate the shell, desks, or plinth; every enabled station remains reachable by the authored radius-0.30 player; hostile, damage, projectile, pickup, drop, and death creation are structurally unavailable.
3. Microrealm movement follows the exact shell and road geometry; Realm Gate, Lantern Fork, safe area, Bell portal, spawn, and capacity-one rules match the content specification without a macro scheduler, realm cycle, Siege, retirement, party, or public admission.
4. The trigger seam fires once with exact tick timing, survives deterministic replay, resets only after the specified empty interval, and exposes Bell portal availability only after an authoritative clear.
5. Interaction prompts/panels derive from authoritative range/state, use exact localization and typed disabled reasons, and support keyboard/pointer focus without allowing presentation to mutate simulation state directly.
6. Native standard, reduced-motion, and 1280x720 evidence is inspected at actual game scale for collision alignment, navigation, labels, safe/portal distinction, contrast hierarchy, and unobstructed playfield.
7. Focused and workspace tests, warnings-denied Clippy, strict content validation, deterministic traces, generated-schema checks, optimized Windows construction, and CI pass before the completion audit is recorded.

## Exclusions

`pack.bell.01` enemy behavior and rewards; fixed Bell dungeon rooms; Core normal enemies/minibosses; Sir Caldus; extraction commit; loading/reconnect production UX; real item/vault preflight; death, Recall, and crash resolution; normal route activation; multiplayer/public allocation; ship-quality M05 Hall art/audio; release packaging; and Core promotion.

## Completed foundation

- Exact `03A` hub/world records compile into immutable renderer-independent scene definitions with stable IDs, localization, independent integration gates, dynamic conditions, and deterministic BLAKE3 digests.
- Radius-aware fixed-point shell/interior collision, bounded movement with wall sliding, explicit safe-creation policy, and grid-connected navigation pass for both Hall arrivals and every enabled Core station.
- The capacity-one microrealm implements the exact movement/primary-release trigger, 30-tick delay, 27-tick warning request, active/empty-reset/cleared lifecycle, and terminal Bell-portal condition without constructing `03D` enemies.
- Server-owned range projections, exact instant/15-tick holds, typed stage/condition rejections, release/focus reset, one-panel-per-player exclusion, and Escape close behavior pass focused tests.
- A disposable native Bevy showcase renders only compiled geometry and exact localization, uses authoritative fixed-step movement/interactions, clamps its camera to scene bounds, culls off-camera semantic labels, preserves the normal-route gate, and exposes standard/reduced-motion modes.
- Inspected release-build evidence covers Hall readability at 1280x720 (`A4A66A1A5041FD49DFE5E1214C2E860CFFAAB92C8EB7388B4F83E268BD5C87CC`) and reduced-motion microrealm readability at 1920x1080 (`368442EA6C20D2D7B4C0692ED225FBBB14CE17A8EC1CFB10EE633CA55F0264C3`).

## Current Next Step

Add a deterministic showcase-scenario harness that drives Hall disabled interactions and the microrealm trigger, warning, Active, and Cleared sequence without constructing `03D` enemies. Pin its runtime trace, capture the interaction/lifecycle states from the optimized native client, then run the cumulative workspace gates and author the `03C` completion audit without enabling Character Select `Play` or production Realm Gate admission.
