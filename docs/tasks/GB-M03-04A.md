# GB-M03-04A — Core progression, XP, and level stats

**Status:** In progress — exact content/simulation and durable roots are complete; transactional awards, protocol projection, restore provider, and HUD remain.

## Objective

Deliver exact, server-authoritative Core levels 1–10, XP eligibility and first-clear behavior, durable progression, Grave Arbalist level stats, crash-restore participation, and accessible HUD projection without enabling the normal player route.

## Three design authorities

1. `Gravebound_Production_GDD_v1_Canonical.md`: `PROG-001`–`003`, `CLS-002`, `CLS-020`, `SOC-010`–`011`, `UI-001`, `UI-003`–`004`, and `TECH-020`–`023`.
2. `Gravebound_Content_Production_Spec_v1.md`: `CONT-000`–`003`, `CONT-010`–`012`, `CONT-ENEMY-002`–`003`, and `CONT-VALID-001`–`003`.
3. `Gravebound_Development_Roadmap_v1.md`: `GB-M03-04`, the M03 exit gate, and all seven owner-approved `SPEC-CONFLICT-007` resolutions.

## Completed slices

- Strict independently hashed, non-promotable progression target with one exact level curve, one Grave Arbalist growth record, 13 XP profiles, six Core source bindings, and complete en-US reference closure.
- Exactly `xp.normal_t1`, `xp.miniboss_t1`, and `xp.boss_caldus` are Core-enabled. Realm Elite and every later-tier/event/boss profile remain authored but disabled.
- Pure deterministic simulation implements ordinary and `SOC-010` eligibility boundaries, integer contribution coefficients, Core XP saturation at 2,700, level derivation, first-clear rounding, Grave Arbalist growth, and nonhealing/nonlethal health rebuild.
- Migration 0003 adds normalized progression, immutable typed award evidence/results, account-wide boss-first-clear ownership, and progression entry-restore children.
- Existing wipeable characters backfill only from the approved level-1 state. New character creation seeds progression atomically, and identity reads reject a divergent temporary level cache.

## Current implementation slice

Implement the replay-first award transaction repository and authoritative service. Identical retries must return the stored result before mutable eligibility checks; payload conflicts, stale/dead/foreign state, concurrent first clears, cap saturation, and rollback must remain typed and all-or-nothing.

## Remaining acceptance work

- Protocol progression projection/event without a client-authored grant command.
- Real progression entry-restore capture/restore provider.
- Accessible native health/level/XP HUD and capped-level presentation.
- Disposable PostgreSQL concurrency/restart/corruption/crash-restore evidence, real-QUIC projection tests, full gates, ADR, and completion audit.

## Exclusions

Items and rewards (`04B`–`04G`), Oaths/Bargains (`05`), death (`06`), extraction/Recall (`08`), future-stage XP source activation, levels 11–20, mastery trials, release packaging, Core promotion, and normal route activation.
