# GB-M03-03G - Normal private-life runtime integration

**Status:** In progress under [`ADR-037`](../decisions/ADR-037-normal-core-private-route-composition.md) and [`SPEC-CONFLICT-006`](../spec-conflicts/SPEC-CONFLICT-006-m03-world-flow-contract.md). The persistent shared-writer session, terminal-first recovery, microrealm, B0–B5 fixed-dungeon owner, durable B3/B4 acknowledgement, complete route-bound B6 fight, account-first Caldus terminal arbitration, automatic durable reward execution, coherent post-reward custody read, protocol 1.19 pending-inventory projection, and `LinkLost`-safe extraction binding are implemented. Exact implementation history remains in the linked evidence records below. Normal admission stays disabled pending Caldus publication/actor construction, bound production construction, hosted adverse/restart proof, and final full-loop evidence.

## Objective

Compose the completed M03 authorities into one persistent, ordinary, wipeable Core runtime and one native application route. This package is complete only when a private tester can play Character Select -> Hall -> microrealm -> fixed Bell dungeon -> Sir Caldus -> extraction/death -> Hall/successor without developer commands and with every mutation remaining server-authoritative and durable.

## Three design authorities

1. `Gravebound_Production_GDD_v1_Canonical.md`: Core Prototype scope; `LOOP-001`-`003`; `DTH-001`, `DTH-010`-`011`, `DTH-020`-`021`; `UI-002`-`007`; `TECH-010`-`023`; and `QA-101`.
2. `Gravebound_Content_Production_Spec_v1.md`: `CONT-WORLD-001`, `CONT-ROOM-007`, Core encounter/boss records, `CONT-BOSS-001`-`002`, and `CONT-HUB-001`-`002` define the only legal Core route and later-stage closures.
3. `Gravebound_Development_Roadmap_v1.md`: `GB-M03-03` and the M03 exit gate require an explicit no-developer-command private loop, restart/idempotency proof, login and successor timing, and 25 scripted journeys.

## Implementation slices

### 1. Normal world-flow authority

- Route location queries through the durable read projection.
- Route `CharacterSelect -> Hall`, Hall return-to-select, Hall Realm Gate -> `world.core_microrealm_01`, and cleared Bell portal -> `dungeon.bell_sepulcher` through authenticated, versioned, replay-first mutations.
- Preserve one lineage/restore root across microrealm-to-dungeon transfer. Capture a restore point only on safe-to-danger entry.
- Reject uncleared portal use, invalid source/range/content, later destinations, BB1/BS1, and seeded/public allocation without mutation.

### 2. Persistent normal server composition

- Add a dedicated `BoundCorePrivateLifeServer`; retain `BoundCoreIdentityServer` as the fail-closed regression endpoint.
- Construct persistent identity, progression, death view, Oath/Bargain, safe inventory, ResolutionHold, successor, world-flow, Recall, terminal, and scene authorities before advertising their capabilities.
- Own transport/session generations, reliable response sequencing, terminal completion delivery, actor cleanup, and zero-residue shutdown.

### 3. Append-only route projection and live private-life actor

- Append a negotiated `CorePrivateRouteStateV1` reliable event after all existing discriminants. It exposes only server-owned lineage, scene/room identity, phase, readiness, and state version.
- Own exact compiled Hall and microrealm movement/interactions, capacity-one admission, `pack.bell.01`, Bell portal clear state, the fixed `B0 -> B6` room graph, Core enemies/minibosses, Bargain rest room, Sir Caldus, rewards, and pending inventory.
- Feed authoritative snapshots/events through existing bounded protocol channels. Client input never authors collision, targets, damage, rewards, room completion, destinations, or terminal outcomes.
- Integrate the shared five-producer terminal coordinator, durable death, extraction, explicit/LinkLost Recall, crash restore, and process-resume behavior.

### 4. Ordinary native route

- Add one `core-private-life` client mode using the real negotiated server, Core identity roster, `CoreWorldTransitionModel`, compiled scenes, combat HUD, inventory/Vault, Oath/Bargain, death/Memorial, successor, Recall, and ResolutionHold projections.
- Add a real Character Select `Play` action and Hall Realm Gate interaction only when exact capabilities and authoritative state permit them.
- Require matching location and `CoreSceneReadiness` before control; preserve protected playfield, accessibility, keyboard/pointer focus, and standard/reduced-effects parity.
- Keep all standalone showcase commands isolated from the normal state machine.

### 5. Evidence and closure

- Unit, property/fixed-trace, transactional PostgreSQL, real-QUIC, reconnect/restart, malformed/adverse, performance, cleanup, and visual tests.
- Twenty-five ordinary scripted full loops without fixture-only route commands, covering extraction, Recall, lethal death, Memorial, successor, and return to checked danger control.
- Optimized native captures at 1280x720 and 1920x1080 in standard and reduced effects, with hashed build/content/evidence manifests and updated README screenshots.
- Updated tester package, parent `GB-M03-03` audit, and hosted cumulative CI before removing the normal-route gate.

## Slice 1 implementation record

- `CorePrivateWorldFlowRouter` preserves distinct durable location-read and transactional mutation owners without altering authenticated frames.
- `Blake3WorldFlowIds` derives restart-stable, domain-separated transfer, lineage, and restore identities from authenticated account, selected character, and mutation identity. Completed `03B`/`03F` fixtures retain a fail-closed compatibility alias.
- The exact Hall Realm Gate performs the existing safe-inventory preflight and captures one complete danger root. The Bell transfer preserves that lineage and restore point, does not move safe aggregate versions, and targets only `dungeon.bell_sepulcher`.
- Bell admission uses a two-phase actor reservation: validate and explicitly release PostgreSQL locks, prepare an exact content/lineage/character binding with nonzero permit, actor generation, and route-state version, and retain a non-clone owned lease that pins that generation. The prepare contract requires an internal RAII guard before any suspension so cancellation cannot strand a reservation. A fresh serializable transaction then revalidates and commits before bounded commit/abort handling consumes the lease after lock release. Committed death, actor replacement, or other durable drift therefore cannot pass between phases; timeout/cancellation drops the internal guard or returned lease; response-loss replay uses bounded reconciliation without acquiring a new permit.
- Recognized production portals from the wrong scene return `InvalidSource`; uncleared/range denials are durably replayable; transient actor failures remain retryable; all later destinations remain `DestinationDisabled`.
- Exact source `26f9c66` passes hosted CI [`29616169514`](https://github.com/MikeyPar/Gravebound/actions/runs/29616169514): Linux formatting/strict lint/tests/content/schema validation, the mandatory PostgreSQL suite, optimized Windows construction, and optimized native evidence are green. The PostgreSQL proof covers restart replay, changed-binding conflict, same-root preservation, all five V3 restore components, unchanged safe versions, same-aggregate actor reads without lock inversion, an exclusive concurrent permit whose loser receives `TransferInProgress` and then replays the one receipt, cancellation during prepare followed by generation release and successful exact retry, stable/transient actor decisions, and actor bypass for invalid/stale/content-mismatched requests.

## Slice 3 protocol/client projection record

- Protocol `1.18` appends only `ReliableEvent::CorePrivateRouteState` at reliable-event discriminant `20`; outer `MessageKind` bytes `1`-`23` remain unchanged and no client request or destination-authoring frame exists. The explicit `1.17` compatibility encoder preserves the pinned successor request/result hashes and rejects the new event.
- `CorePrivateRouteStateV1` binds selected character/version, a domain-separated aggregate of every compiled Hall/world/room/encounter/Caldus input, capacity-one actor generation, per-generation state version, danger lineage, closed Hall/micro-realm/B0-B6 scene/room enums, exact authority phase, and typed readiness. BB1, BS1, seeded layouts, free-text phases, boss-defeat-before-durable-exit extraction, impossible readiness, zero authority, and terminal input fail closed.
- The native `CorePrivateRouteClientModel` composes the existing durable world-transition snapshot with the route event and room-aware compiled-scene readiness. Either location/event arrival order buffers safely; control requires exact character/content/location/lineage/room/generation/state/version agreement. LinkLost, terminal pending, malformed authority, content drift, changed same-version replay, unauthorized generation advance, and retired-generation events cannot retain or recreate input.
- Commits `5e1c5f9` and `eae98f4` pass 82 protocol tests, eight focused native authority tests, strict protocol/client/workspace Clippy, all-target workspace compilation, and the complete runnable workspace suite locally. Exact source `342889b` is green under hosted CI [`29619764945`](https://github.com/MikeyPar/Gravebound/actions/runs/29619764945), including Linux quality/content/schema gates, mandatory PostgreSQL coverage, optimized Windows construction, and optimized native evidence.

## Slice 3 actor/content/generation record

- `CorePrivateLifeContent` loads and validates only the compiled Core Hall, capacity-one microrealm, fixed encounter-room set, `layout.core_private_life_01`, and Sir Caldus authority. Its length-prefixed, domain-separated aggregate revision covers the ordered world-flow, room, boss-record, asset, and localization inputs; disabled BB1/BS1 branches and any identity/capacity drift fail closed without promotion or route admission.
- `CorePrivateRouteActor` is the capacity-one route-phase authority for one authenticated account and selected character. It owns the exact Hall -> microrealm -> B0-B6 -> Caldus -> terminal phase graph, room safety/readiness phases, monotonic state versions, bounded mailbox, RAII Bell permit, cancellation cleanup, reconciliation without rewind, one active generation per account, and zero-residue shutdown. Commit `b02f0f6` routes ordinary advances and Bell prepare/commit/reconciliation through that mailbox; synchronous lease drop only releases the reservation token and cannot mutate route state. Extraction, Recall, or death retires the danger generation; returning to Hall requires a newly allocated higher generation rather than mutating the terminal actor backward.
- This phase actor does not yet own compiled movement, live combat, reward grants, pending inventory, or terminal execution. Those live owners remain explicit slice-3 work and are prerequisites for truthful normal capability advertisement.
- PostgreSQL migration `0062_private_route_actor_generations_v1.sql` and `PostgresPersistence::allocate_private_route_generation_v1` allocate strictly increasing generations for the authenticated selected living character under serializable retry. The immutable allocation audit and deferred head-closure constraint prevent reuse, foreign/dead-character allocation, audit mutation, or a committed head without its matching audit; gaps after ambiguous response loss are legal and never reused.
- Exact source `e069987` is green under hosted CI [`29633952947`](https://github.com/MikeyPar/Gravebound/actions/runs/29633952947): Linux formatting/strict lint/tests/content/schema validation, mandatory PostgreSQL migration and transaction coverage, optimized Windows construction, and optimized native death evidence pass. It cumulatively includes the terminal-first/schema-63 intent foundations, dormant persistent graph, and generation-safe shared-writer session/Recall owner. Commit `cf933f2` adds the dynamic extraction actor directory, opaque coordinated publication proof, complete tick/time/version validation, one cancellation-safe reliable completion, exact replay/handoff behavior, generation/lineage/restore authority, two-step Hall installation acknowledgement, all-terminal loser retirement, and aggregated route/runtime cleanup. Local formatting, 9/9 extraction-intent tests, 11/11 extraction execution tests, the complete 286-test server suite, all workspace targets, strict workspace all-target Clippy, and the independent authority re-audit pass.
- Commit `a30e644` adds the [terminal-first bootstrap and transition-reconciliation contract](../evidence/GB-M03-03G-private-life-bootstrap-transition-evidence.md). Exact Hall generations, stored terminal precedence, process-restart-only crash restoration, retained reconnect authority, post-commit Character Select/Hall/microrealm convergence, exact replay tombstones, changed-material rejection, and zero-residue retirement pass 5 focused bootstrap tests, 11 actor tests, the complete 293-test server suite, and strict server all-target Clippy.
- Commit `314fa7f` records two provenance-complete [Sir Caldus combat-silhouette drafts](../../Concept%20Art/m03-sir-caldus-combat-silhouette-drafts/README.md). They are high-resolution art-direction sources only; they are not registered runtime sprites, content-hash inputs, or route evidence. The unregistered [v2 attack-motion review](../evidence/GB-M03-03G-sir-caldus-motion-review-v2.md) accepts four-frame Shield Arc and Charge Lane strips; the [v3 idle/recovery review](../evidence/GB-M03-03G-sir-caldus-idle-recovery-review-v3.md) adds idle and recovery-to-guard; and the [v4 Bell Ring review](../evidence/GB-M03-03G-sir-caldus-bell-ring-review-v4.md) adds a seed-locked guard, chest-bell anticipation, hand-bell release, and guarded return with corrected `BELL RING` standard/reduced mocks. All remain versioned, provenance-complete static candidates outside the registry and content hashes. Runtime registration remains blocked on one combined in-engine timing, attack-origin/hurtbox/collision, anchor-drift, phase-three applicability, and optimized native evidence pass.

## Slice 5 shared terminal writer record

- Commit `93dde0a` composes Recall and extraction behind the private-life session's one `CoreReliableWriter` using symmetric recoverable prepare/commit/abort handoffs, session-owned retirement, exact Hall acknowledgment, and zero-residue shutdown.
- Restart delivery replays only stored Recall authority or an exact accepted extraction retry. It preserves stored inner request correlation, uses the current outer server tick, validates authenticated account/selected character/content/version authority, and produces no Hall control itself.
- The combined real-QUIC proof binds session, Recall, and extraction to the same writer identity, emits contiguous sequences `1/2/3`, rebinds both dynamic leases before reconnect returns, and rejects stale detach without disturbing the winning generation.
- Local strict server/workspace Clippy, `301/301` server library tests, every enabled server/workspace test, four content validators, and generated-schema verification pass. Full contract details are recorded in [`GB-M03-03G-shared-terminal-writer-evidence.md`](../evidence/GB-M03-03G-shared-terminal-writer-evidence.md).
- Normal admission remains disabled; this implementation advances the parent integration package but does not close `GB-M03-03G` or `GB-M03-03`.

## Microrealm landmark presentation record

- Commit `45c8bcb` integrates project-generated, provenance-recorded Realm Gate, Lantern Fork, and Bell Sepulcher RGBA art into the disposable native microrealm by exact stable object ID. It does not map the Hall station or return portal and does not alter content, collision, interaction, route, or terminal authority.
- The renderer resolves an explicit repository asset root and fails closed unless the source manifest and all three runtime images match their reviewed BLAKE3 values. The source manifest retains prompts, source/runtime hashes, normalization, anchors, design-authority citations, and scope boundaries.
- Optimized standard/reduced-motion captures at 1280x720 and 1920x1080 pass original-resolution inspection. A dedicated deterministic Lantern Fork state completes the in-engine three-landmark review; the [hashed visual manifest](../evidence/GB-M03-03G-microrealm-landmark-visual-manifest.md) binds source commit, executable, content revisions, scenarios, dimensions, modes, and screenshot hashes.
- Strict client all-target Clippy and all `193/193` client library tests pass. Normal route registration remains blocked on persistent live movement/combat/reward/pending-inventory composition and the full-loop matrix.

## Live microrealm authority record

- Commit `37cf99d` adds the first normal-play live owner: exact fixed-point movement/collision, monotonic input sequencing, microrealm lifecycle, compiled Bell-range derivation, and route-state synchronization.
- All fallible movement/lifecycle work stages before the route actor accepts one expected-version command. Phase and range commit together under the actor lock; a stale version, reservation, foreign clear proof, or actor mismatch leaves local state unchanged.
- Pack clear requires an opaque server proof bound to character, actor generation, lineage, and tick. Ordinary input cannot author clear, phase, Bell eligibility, or route state.
- The [local evidence record](../evidence/GB-M03-03G-live-microrealm-authority-evidence.md) covers `4/4` focused tests, `305/305` server library tests, every enabled server target, strict all-target Clippy, the full spawn-to-Bell movement path, and rollback checks. Session attachment, live enemies, rewards/pending inventory, terminal competition, real QUIC, restart, and ordinary native control remain open.

## Core Bell enemy-art candidate record

- Commit `736cb59` adds isolated, provenance-complete Drowned Pilgrim, Bell Reed, and Chain Sentry idle candidates for the repeated microrealm/B1/B5 compositions. All are normalized 48x48 RGBA images with bottom-center anchors, transparent corners, and no baked combat effects.
- The source manifest retains exact prompts, generation/postprocess method, source/runtime/preview hashes, design citations, content dependencies, and the no-authority boundary. Main-agent verification recomputed all `11/11` recorded hashes and inspected the trio at 48 px and nearest-neighbor 96 px.
- The [v1 review](../evidence/GB-M03-03G-core-bell-enemy-art-review-v1.md) accepts the silhouettes only as unregistered candidates. Content records/hashes and runtime rendering remain unchanged pending optimized B1/B5 telegraph, anchor, grayscale, and effects-priority evidence.

## Live microrealm session record

- Commit `e5e1a9b` binds one live microrealm owner and its exact route lease to the persistent account session rather than to a disposable transport.
- Authoritative handoff returns the same allocation before visibility, stale transports cannot access or unbind it, and `LinkLost` preserves the owner for continued danger simulation. Exact terminal/transfer unbind leaves the shared reliable writer alive.
- The [session evidence](../evidence/GB-M03-03G-live-microrealm-session-evidence.md) covers real-QUIC replacement, detach, reconnect, pointer-identical authority, exact route-lease retirement, and zero-residue shutdown. Formatting, strict server all-target Clippy, `306/306` server library tests, and every enabled server target pass locally.
- This does not itself run the owner on an independent server scheduler. Normal admission remains disabled.

## Live microrealm combat record

- Commit `302ccb3` extracts `CoreMicrorealmPackCombat`, a lifecycle-free capacity-one owner of the exact compiled Bell participant/wave handoff. It preserves authoritative warning, reset, clear, projectile cleanup, and never-reused spawn ordinals while rejecting active-wave handoff.
- Commit `f07a282` splits immutable/versioned character metadata from the one mutable combat allocation. Health, Belt, Bell Debt, cooldown, and projectile state move into one live participant and rejoin only through exact entity and immutable-axis validation.
- Commit `587924e` removes tick, displacement, release, damage, and clear from microrealm ingress. Each invoked frame generates its own next tick, equipment-speed displacement, combat/collision step, hostile step, and lifecycle projection; only an exact live wave clear can mint the crate-private clear proof.
- The [combat evidence](../evidence/GB-M03-03G-live-microrealm-combat-evidence.md) records strict all-feature/all-target Clippy, `125/125` simulation tests, `309/309` server tests, focused handoff/rollback/authority coverage, and the explicit remaining scheduler/Slipstep boundary. The production 30 Hz driver is not claimed by this component alone.

## Candidate VFX and pending-loot UI record

- Commit `ffd74d6` adds provenance-complete spawn-warning, Drowned Pilgrim fan, and Bell Reed gap-ring candidates. The [VFX review](../evidence/GB-M03-03G-core-bell-vfx-review-v1.md) verifies all hashes, dimensions, RGBA files, exact three/six direction counts, and the adjacent Reed gap; runtime registration remains blocked on optimized timing/accessibility review.
- Commit `370b53e` adds provenance-complete at-risk slot, personal ground-drop, and Bell Brass pouch candidates. The [pending-loot review](../evidence/GB-M03-03G-pending-loot-risk-review-v1.md) verifies all hashes, dimensions, RGBA files, and risk-state separation; runtime registration remains blocked until the authoritative reward/pending-inventory projection exists.
- Both packages cite the three canonical authorities, retain raw sources and prompts, and remain outside registries, runtime bundles, and content hashes.

## Live Slipstep and fixed-route landmark record

- Commits `f75c662`, `8d74ca7`, and `3c6af83` add the shared checked simulation-to-world projection, exact compiled microrealm shell geometry, and same-frame live Slipstep composition. Movement, collision, damage reduction, hostile targeting, lifecycle, and route compare-and-swap now commit or roll back together; the [Slipstep evidence](../evidence/GB-M03-03G-live-slipstep-evidence.md) records `388/388` simulation-core, `126/126` content, and `310/310` server tests plus strict Clippy.
- Commit `ea6445c` adds provenance-complete B0 vestibule, B4 Bargain shrine, and B6 post-reward exit candidates. The [landmark review](../evidence/GB-M03-03G-bell-fixed-route-landmark-review-v1.md) verifies all hashes, dimensions, RGBA canvases, route semantics, and the unregistered boundary; runtime use remains blocked on fixed-room authority and optimized native review.
- At those commits, neither slice supplied the independent 30 Hz driver, opened ordinary admission, or claimed hosted CI before completion; the following driver record closes only that scheduler boundary.

## Independent microrealm driver record

- Commit `c3bc57e` makes input sequence a retained acknowledgement at the runtime boundary; transport validation and coalescing now belong to session ingress.
- Commit `995151c` adds the exclusive 30 Hz driver, one-slot continuous input, separate reliable ability sequences, `MissedTickBehavior::Skip`, `LinkLost` neutralization with continued danger ticks, bounded observation, exact lethal/fault freeze, frame-complete shutdown, and residue metrics without acquiring a reliable writer.
- Commit `4516b1a` replaces the session's shared runtime mutex with observer-only generation-bound bindings, linearizes current-transport ingress against detach, preserves one driver across reconnect, and adds transport-independent retirement after `LinkLost`.
- The [driver evidence](../evidence/GB-M03-03G-independent-microrealm-driver-evidence.md) records `5/5` paused-time tests, the real-QUIC handoff/reconnect/retirement lifecycle, `315/315` server tests, and strict all-target/all-feature Clippy. Persistent fixed-dungeon binding, B6, rewards, and durable terminal composition remain open.

## Fixed-dungeon ownership, persistent route binding, and Caldus presentation record

- Commit `42d9f85` makes B1/B2/B3 completed-room handoff consuming and fail closed before doors reopen. The same player entity and monotonic hostile-projectile allocator survive every transfer.
- Commit `9694790` adds `CoreFixedDungeonCombat`, the lifecycle-free B0-B6 owner for the exact `CONT-ROOM-007` chain. It stages every route advance, owns typed B1/B2/B3/B5 steps, retains one participant through B4, requires an explicit replay-safe Bargain outcome, observes B3 reward handoff before B4, and exposes the B6 participant only after committed B5 clear.
- The [fixed-dungeon evidence](../evidence/GB-M03-03G-fixed-dungeon-combat-owner-evidence.md) records the full B0-B6 trace, early-advance rejection, exact B2/B3 authored boundaries, Bargain replay/conflict, identity continuity, `129/129` simulation tests, and strict all-target/all-feature Clippy. Hosted runs `29642037538` and `29642381573` were still in progress when recorded and are not claimed green.
- Commit `d63b675` adds the provenance-complete, unregistered v5 Caldus Charge Lane -> Stop Ring pose strip. The [v5 review](../evidence/GB-M03-03G-sir-caldus-stop-ring-review-v5.md) verifies all frame/source/preview/mock hashes, 192/96 px readability, standard/reduced parity, and the presentation-only boundary.
- Commit `5deee6d` relocates every transferred participant to a collision-safe authored room entry without rebuilding its life-persistent combat state. Commit `f044a61` carries the next microrealm hostile ordinal into B1 so no run-local hostile identity can repeat.
- Commit `6749141` adds the [persistent route-binding contract](../evidence/GB-M03-03G-fixed-dungeon-route-binding-evidence.md): one staged fixed-dungeon runtime carries the exact tick, character/player/projectile identities, rebased Bell character version, and route lease; one actor-lock CAS maps legal B0-B6 advances, same-frame room phases, and DNG-005 resets or rolls back entirely.
- Commit `a2a5b09` adds the [same-task driver conversion contract](../evidence/GB-M03-03G-fixed-dungeon-driver-conversion-evidence.md): Bell resolution pauses between frames, explicit rejection restores the exact owner, unknown resolution remains frozen, and a committed/replayed transition consumes the microrealm inside the original task. A dropped acknowledgement cannot detach the allocation; reconnect retains the same binding, observer, and shared reliable writer.
- Commit `b50a8c0` shares one rollback-safe live movement/combat frame primitive between microrealm and fixed rooms. B1/B2/B3/B5 expose only their carried player and legally damageable hurtboxes; B2/B3 warning frames cannot accept early damage.
- Commit `09c9c9e` keeps the converted runtime inside that same task, accepts only destination-free server control, neutralizes Bell-held movement/fire at room relocation while preserving sequence watermarks, publishes fixed-room running/lethal frames through the original observer, rejects stale transports and premature advances, and preserves tick 32 -> 33 continuity across real-QUIC reconnect. The [live-driver evidence](../evidence/GB-M03-03G-live-fixed-room-driver-evidence.md) records the full local gate.
- Commit `8d62f6f` adds the provenance-complete, unregistered v6 Caldus defeat-to-pre-reward strip. The [v6 review](../evidence/GB-M03-03G-sir-caldus-defeat-review-v6.md) verifies source/frame/preview hashes, 192/96 px readability, standard/reduced mocks at both certified resolutions, and the no-reward/no-exit presentation boundary.
- Commit `a6fb62e` adds three unregistered B4 shrine-state candidates (`Open`, `Selected`, `Refused`) with exact anchors, source prompts, raw/alpha sources, deterministic previews, and twelve verified hashes. Art cannot select, apply, or advance the Bargain; runtime registration remains blocked on durable projection and optimized native review.
- Commit `e1e4d7c` adds the [durable B4 task-binding contract](../evidence/GB-M03-03G-durable-b4-task-binding-evidence.md): persistent selected/refused receipts and committed no-offer milestone results become opaque account/character/lineage-bound proofs; the current session generation and sole task validate them; exact replay is read-only; foreign or changed authority cannot advance; and only a resolved task can select canonical B4 -> B5.
- Commit `ac31961` adds an unregistered [B6 Caldus resolution-state pack](../../assets/core/ui/caldus_resolution_states/v1/README.md) that distinguishes reward-unresolved from reward-committed/still-at-risk without implying secured inventory. It corrects the shorthand explicitly: B5 is the bridge; Caldus is B6.
- Later commits deliver the B3 reward/milestone result and complete route-bound Caldus combat. Commit `78b7ff0` adds the [frozen Caldus reward handoff](../evidence/GB-M03-03G-caldus-reward-handoff-evidence.md): exact defeat evidence is immutable, later combat is blocked, durable owner identity is validated, response-loss replay converges, and only a matching acknowledged result advances once to the stable exit. PostgreSQL/session execution, pending-inventory publication, production extraction binding, and normal admission remain open.
- Commits `308767f` and `57e4ca2` add the [terminal-arbitration contract](../evidence/GB-M03-03G-caldus-terminal-arbitration-evidence.md): the Bell restore root is frozen through defeat; fresh Caldus item, XP, and exit phases share the terminal writers' account-first order and exact active-root validation; stored results remain replayable after closure; fresh late writes fail closed.
- Commits `f4ad323`, `e5f7dc8`, `1bd230a`, and `47ad6c3` add the [Caldus extraction-prerequisite contract](../evidence/GB-M03-03G-caldus-extraction-prerequisites-evidence.md): one session-owned worker retries and acknowledges the frozen durable result, one terminal-first transaction reloads exact post-reward versions and pending custody, protocol 1.19 appends a bounded server-owned inventory event while preserving 1.18 bytes, and the registered extraction actor remains bound across `LinkLost` for reconnect attachment. Normal admission remains disabled.
- Commits `42a632b`, `be52c29`, and `f5c12b3` harden and complete publication: impossible or non-Core pending custody fails at persistence, projection, and protocol boundaries; an exact uncommitted extraction permit reopens `BossExitReady` with a monotonic version while stale/foreign/replaced authority is inert; and the Caldus worker publishes pending inventory before the matching route state, retains both events across writer generations, and permits exact `LinkLost` loser cleanup without resurrecting a retired actor. Production extraction actor construction/registration remains disabled.

## Explicit exclusions

Production namespace cutover, `core.1.0.0` promotion, M04+ content, parties, public realm scheduling, seeded layouts, Requiem encounters, Echo encounters, Forge/salvage, paid Oath changes, purge, retirement, wardrobe, commerce, Steam runtime integration, telemetry, support lookup, and hosting/platform evidence.

## Current Next Step

Commits `42a632b`, `be52c29`, and `f5c12b3` close custody hardening, exact permit rollback, ordered worker publication, reconnect replay, and transport-free `LinkLost` loser cleanup under [extraction-prerequisite evidence](../evidence/GB-M03-03G-caldus-extraction-prerequisites-evidence.md). Next construct/register production extraction from the exact committed `BossExitReady` exit plus coherent storage version vector, bind it through the exact danger lease, and prove active, reconnected, `LinkLost`, response-loss, and competing-terminal ownership. Then run hosted PostgreSQL/real-QUIC adverse and restart proof. Bound normal-server construction, normal admission, cumulative journeys, and final native evidence remain open.

### Historical cumulative record

Parent [`GB-M03-03`](GB-M03-03.md) normal integration remains active in [`GB-M03-03G`](GB-M03-03G.md) under [`ADR-037`](../decisions/ADR-037-normal-core-private-route-composition.md). Exact source `e069987` is green under hosted CI [`29633952947`](https://github.com/MikeyPar/Gravebound/actions/runs/29633952947) for the dormant persistent foundation, generation-safe shared-writer session/Recall ownership, terminal-first bootstrap and schema-63 intent foundations, strict quality/content/schema checks, PostgreSQL transactions, Windows release construction, and optimized native evidence. Commit `26c420b` constructs the dormant persistent foundation, commit `551c527` owns the monotonic per-account transport generation and shared reliable writer with dynamic Recall, and commit `cf933f2` adds the dynamic extraction runtime. Extraction now requires an opaque five-producer executor proof, samples a generation-bound authoritative tick only after command dequeue, publishes one cancellation-safe completion, marks retries/handoffs as replayed, binds Hall installation to the exact transport/terminal/result, retires safely for every competing terminal winner, and reports route/runtime residue without closing the continuing Hall writer. Local formatting, 9/9 extraction-intent runtime tests, 11/11 extraction execution tests, the complete 286-test server suite, all workspace targets, and strict workspace all-target Clippy pass at `cf933f2`; an independent authority re-audit found no remaining runtime blocker. Commit `a30e644` adds the terminal-first process bootstrap adapter and post-commit/replayed world-flow reconciliation: first-time identity defers cleanly; Character Select and storage-blocked states create no route actor; restart danger atomically restores to Hall or yields to a committed terminal; Hall generations are durable and reused in-process; stored death/extraction/Recall outcomes remain ordered before destination control; and exact Hall/microrealm/Character Select transition replays converge without rewind while changed material fails closed. Corrective commit `51be807` withholds extraction and Recall Hall actors until the exact stored terminal result is delivered and acknowledged; restart never invents extraction request correlation or exposes destination control first. Focused bootstrap tests pass 5/5, route-actor tests pass 11/11, the complete 293-test server suite and all server targets pass, and strict server all-target Clippy is green. Commit `93dde0a` unifies dynamic Recall, extraction, and restart recovery on the private-life session's single writer: symmetric two-phase handoffs are recoverable and exact; reconnect rebinds both owners before visibility; only the session retires shared transports; stored Explicit/LinkLost Recall and exact extraction retries remain account/character/content/version bound; current outer ticks and stored inner correlation are preserved; and Hall remains gated behind exact terminal delivery. Real-QUIC composition proves one writer identity, contiguous sequences `1/2/3` per transport, stale-detach safety, coordinated reconnect, and zero residue. Local formatting/diff checks, strict server/workspace all-target Clippy, 301/301 server library tests, all workspace tests, four content validators, and generated-schema verification pass. Commit `45c8bcb` integrates the provenance-complete, hash-locked Realm Gate, Lantern Fork, and Bell Sepulcher art into the optimized disposable microrealm at both certified resolutions and effects modes without changing compiled authority or normal admission. Commit `37cf99d` adds rollback-safe live microrealm movement, lifecycle, server-clear, and Bell-range ownership; focused tests cover the complete spawn-to-Bell route and stale-authority rollback. Commit `736cb59` adds provenance-complete Drowned Pilgrim, Bell Reed, and Chain Sentry candidates; keep them outside runtime/content hashes until optimized B1/B5 telegraph and anchor review passes. Commit `e5e1a9b` binds the live microrealm owner and exact route lease to the persistent account session: reconnect receives the same allocation before visibility, stale transports are denied, `LinkLost` preserves danger authority, exact unbind leaves the shared writer alive, and shutdown reports zero residue. Formatting, strict server all-target Clippy, `306/306` server library tests, every enabled server target, and focused real-QUIC handoff/reconnect proof pass locally. Commits `302ccb3`, `f07a282`, and `587924e` now extract the lifecycle-free `pack.bell.01` owner, move the single mutable character-combat allocation through an exact rejoin envelope, and remove tick/displacement/damage/clear authority from ingress. Each invoked frame generates the next tick, equipment-speed displacement, player combat/collision, hostile wave step, and exact clear projection under one staged route compare-and-swap. Strict all-feature/all-target Clippy, all `125/125` simulation tests, and all `309/309` server tests pass locally. Cumulative hosted run [`29639908490`](https://github.com/MikeyPar/Gravebound/actions/runs/29639908490) is green for exact source `587924e`, including the prior pack and handoff commits. Commits `ffd74d6` and `370b53e` add provenance-complete Bell VFX and pending-loot risk candidates; both remain outside registries and content/runtime hashes pending optimized in-engine review. Commits `f75c662`, `8d74ca7`, and `3c6af83` now compose exact compiled-shell movement and Slipstep in the same rollback-safe live combat frame; local `388/388` simulation-core, `126/126` content, `310/310` server, formatting, and strict all-feature/all-target Clippy gates pass. Commit `ea6445c` adds verified unregistered B0/B4/B6 landmark candidates without changing registry or content authority. Hosted CI runs [`29640683239`](https://github.com/MikeyPar/Gravebound/actions/runs/29640683239), [`29640707913`](https://github.com/MikeyPar/Gravebound/actions/runs/29640707913), and [`29640811604`](https://github.com/MikeyPar/Gravebound/actions/runs/29640811604) are green for the Slipstep, landmark, and evidence sources. Commits `c3bc57e`, `995151c`, and `4516b1a` move input validation to session ingress, exclusively own the runtime behind a one-slot/reliable-action 30 Hz driver, preserve danger ticks with LinkLost neutralization, freeze exact terminal/fault frames, retain one generation-safe observer across reconnect, and join through a transport-independent binding lease. Local paused-time tests pass `5/5`, the cumulative server suite passes `315/315`, and strict all-target/all-feature Clippy is green; hosted run [`29641751122`](https://github.com/MikeyPar/Gravebound/actions/runs/29641751122) was still in progress at the latest poll and is not yet claimed green. Commits `42d9f85` and `9694790` provide fail-closed completed-room handoffs and one lifecycle-free B0-B6 owner; commits `5deee6d`, `f044a61`, and `6749141` add collision-safe room entry, continuous hostile identities, exact Bell version/tick/player/projectile handoff, and atomic persistent route CAS; commit `a2a5b09` now converts the runtime inside the existing session-owned 30 Hz task, freezes unknown durable outcomes, survives acknowledgement loss, and preserves one observer/writer across reconnect; commits `d63b675` and `8d62f6f` add unregistered Caldus Stop Ring and defeat candidates. Commits `b50a8c0` and `09c9c9e` now generate server-owned fixed-room movement/combat frames inside that same task, preserve tick/input/player/route identity across B1 and reconnect, reject stale transports and early advances without faulting, and freeze lethal/fault states; local strict Clippy, `131/131` sim-content tests, `326/326` server library tests, every enabled server target, and focused real-QUIC proof pass. Commit `a6fb62e` adds an unregistered, provenance-complete B4 `Open`/`Selected`/`Refused` shrine-state review pack outside runtime hashes. Commit `e1e4d7c` binds committed/replayed `Selected`, `Refused`, and authoritative no-offer B4 results to the same session task through an opaque account/character/lineage receipt; local `242/242` persistence and `330/330` server tests plus strict all-target/all-feature Clippy pass, while hosted run [`29648007340`](https://github.com/MikeyPar/Gravebound/actions/runs/29648007340) remains pending. Next, commit the B3 reward and milestone result through the normal coordinator before B4 response delivery, then implement the B5 bridge, Sir Caldus at B6, durable personal rewards/pending inventory, the stable B6 exit, and all five terminal producers. Keep `core_world_flow_integration`, Character Select `Play`, production Realm Gate interaction, and normal extraction/Recall admission disabled until fixed-room combat, rewards, pending inventory, all terminal owners, real-QUIC restart/25-journey evidence, and visual review pass; keep production namespace cutover, Core promotion, M04+ content, Requiem encounters, paid Oath changes, retirement, purge, and unrelated stations disabled under their independent owners. The unregistered Sir Caldus [v2 attack-motion](../evidence/GB-M03-03G-sir-caldus-motion-review-v2.md), [v3 idle/recovery](../evidence/GB-M03-03G-sir-caldus-idle-recovery-review-v3.md), [v4 Bell Ring](../evidence/GB-M03-03G-sir-caldus-bell-ring-review-v4.md), [v5 Stop Ring](../evidence/GB-M03-03G-sir-caldus-stop-ring-review-v5.md), and [v6 defeat](../evidence/GB-M03-03G-sir-caldus-defeat-review-v6.md) packs are static candidates with locked continuity, 96 px readability, provenance, and standard/reduced mocks. Exercise the combined set against authoritative state timelines in-engine and inspect attack-origin/hurtbox/collision alignment, cadence, phase-three applicability, defeat/reward ordering, anchor drift, opposite-gap readability, and optimized native evidence before proposing any asset-registry or runtime content-hash change.
