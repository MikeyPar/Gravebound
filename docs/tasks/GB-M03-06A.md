# GB-M03-06A - Death records, repository, and reliable protocol

**Status:** In progress - migrations `0031`-`0034`, schema version 34, terminal constraint evolution, normalized death/memorial/trace/Echo/material tables, strict cross-aggregate bindings, and mandatory five-component danger-entry restore V3 capture are implemented locally; exact-head hosted PostgreSQL, all-component crash restoration, death repository, and protocol work remain open.

## Objective

Establish the typed, bounded persistence and wire contracts that later death slices can populate and commit without inventing schema, authority, or replay behavior.

## Authority mapping

1. `Gravebound_Production_GDD_v1_Canonical.md`: `DTH-001`, `DTH-020`, `ECH-002`, and `TECH-020`-`022` require immutable death/trace/summary/memorial records, transactional mutation identity, and durable acknowledgement before presentation.
2. `Gravebound_Content_Production_Spec_v1.md`: `CONT-ECHO-009` and `CONT-HUB-002` require stored Echo transition state and a read-only Memorial Wall over the exact death snapshot.
3. `Gravebound_Development_Roadmap_v1.md`: `GB-M03-02D`, `GB-M03-06`, `GB-M03-13`, and the M03 restart/atomicity gates require real PostgreSQL and transport-visible replay proof.

Approved [`SPEC-CONFLICT-009`](../spec-conflicts/SPEC-CONFLICT-009-m03-death-memorial.md) supplies the exact record family, UUIDv7 `death_id`, final-death idempotency key, and in-transaction Echo seam.

## Bounded record and protocol contract

- `DeathEvent` stores namespace, death/account/character IDs, request hash, pre/post aggregate versions, commit time/tick, instance/region/room/content revision, typed cause, killer/pattern/attack IDs, raw/final damage, damage type, pre-hit health, fixed-point source position, statuses, Recall/network snapshot, trace reference, and ordered destruction references.
- `DeathSummarySnapshot` is one-to-one with `death_id` and stores approved Core hero label, class, level, optional Oath, ordered Bargains, `lifetime_ms`, final deed ID, ordered last-five damage entries, and exact `Lost`/`Preserved`/`Created` projections including Echo status.
- `MemorialRecord` is one-to-one with `death_id` and stores account, `death_at`, summary revision, and the Core default presentation snapshot. It stores no entitlement and no item roll.
- Combat trace and destruction entries have explicit schema revisions, contiguous zero-based ordinals, bounded typed identifiers, fixed-point coordinates, and immutable foreign keys. The repository rejects gaps, duplicate ordinals, noncanonical order, nonfinite/caller-authored values, unknown discriminants, and incompatible content revisions.
- The final-death receipt stores `(character_id, "permadeath-v1")`, canonical payload hash, original typed result, issue time, and authoritative versions. Replay is checked before mutable-state validation.
- Extend protocol `1.12` append-only to `1.13`; preserve existing message-kind bytes `1`-`17` and assign the death-view/request capability the next unused byte `18`. Existing enum variants and compatibility fixtures are never reordered.
- The negotiated V1 surface exposes only authenticated account-bound views: latest committed death, summary by an account-owned historical `death_id`, bounded Memorial pages, and bounded trace pages. No client message can initiate lethal resolution or author the new `death_id`, cause, trace, destruction list, destination, Echo eligibility, presentation strings, or authoritative versions.
- Migration `0031` also adds the normalized immutable inventory/Belt component for the existing danger-entry restore root. Accepted danger entry snapshots exact Equipped/Belt UIDs and versions, transitions their unchanged locations from `Safe` to `AtRiskEquipped`, and records deterministic ledgers in the same caller-owned world-flow transaction; this prerequisite must be restart- and rollback-safe before `06C` can destroy danger custody.
- Malformed, oversized, unavailable-feature, foreign-owner, stale, corrupt, or unknown-version frames fail closed and do not touch the repository. A summary result is unavailable until its transaction commit is acknowledged.

## Delivery and test checklist

- [ ] Complete `GB-M03-02D` migration `0031` and strict persistence DTOs.
- [ ] Add the transaction-bound danger-entry Equipped/Belt snapshot and `Safe -> AtRiskEquipped` transition, including immutable ledgers, exact replay, and crash-restore fixtures.
- [ ] Add server-domain record builders and validators without coupling `persistence` to combat rules.
- [ ] Append protocol `1.13` message kind `18`, feature negotiation, strict validation, size limits, byte fixtures, and old-fixture compatibility checks.
- [ ] Dispatch only through authenticated account/selected-character binding and return typed stored results.
- [ ] Test every field boundary, empty/maximum collections, ordinal gaps, invalid UTF-8-length IDs, zero UUIDs, version drift, altered replay payload, foreign ownership, malformed/oversized frames, and disabled capability.
- [ ] Test exact response-loss replay, restart reconstruction, database outage, corruption, serialization retry, and injected rollback against hosted PostgreSQL and real QUIC.
- [ ] Publish the `06A`/`02D` audit before `06C` can use the repository as final authority.

## Exclusions

Clock accumulation, deed qualification, cause selection, trace capture, destruction planning, terminal arbitration, Echo eligibility/projecting, death-summary rendering, Memorial Wall UI, successor recovery, extraction, Recall, telemetry export, and normal-route admission.

## Current Next Step

Forward-only migration `0034` and locally green V3 capture DTOs implement accepted [`SPEC-CONFLICT-027`](../spec-conflicts/SPEC-CONFLICT-027-m03-crash-restore-completeness.md), with the exact 64-UID legal bound recorded by [`SPEC-CONFLICT-028`](../spec-conflicts/SPEC-CONFLICT-028-m03-crash-baseline-identity-capacity.md). The schema-33 hosted failure's typed bind and immediate cleanup FK are repaired only in `0034`. Require exact-head hosted PostgreSQL to pass component completeness, combined-version, Safe-to-`AtRiskEquipped`, 64-identity, cleanup, replay, rollback, and restart fixtures. Then implement exact all-component crash restoration and its stored replay result before adding strict death DTOs and the transaction-scoped repository for protocol `1.13` kind `18`. Do not expose lethal or player-visible death behavior until `06B`, `06C`, `GB-M03-13`, and `06D` complete.
