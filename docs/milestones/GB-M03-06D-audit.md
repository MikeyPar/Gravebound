# GB-M03-06D completion audit

**Status:** PASS on main implementation commit `094d70e` plus read-only Back hardening `92994f9`; exact hosted CI [`29471819642`](https://github.com/MikeyPar/Gravebound/actions/runs/29471819642) supplies the mandatory PostgreSQL and Windows release gates for the evidence binary.

## Three-authority closure

| Authority | Closure |
|---|---|
| `Gravebound_Production_GDD_v1_Canonical.md` | `DTH-020`, `UI-001`, `UI-002`, `UI-009`-`UI-011`, `UI-030`, and `TECH-022` are represented by one durable-acknowledged native summary, exact ordered evidence, explicit waiting/error states, accessible focus/scroll behavior, reduced-effects parity, and a disabled largest successor action with no commercial interruption. These are presentation prerequisites for `DTH-021`; its latency and human recovery targets are explicitly deferred. |
| `Gravebound_Content_Production_Spec_v1.md` | Presentation-owned `CONT-HUB-001`, `CONT-HUB-002`, and `CONT-LOC-001` behavior compiles into the Core Memorial Wall, newest-first raw cursor order, exact stored detail selection, Escape-to-read-only-Back handling, canonical `en-US` copy, and immutable presentation/asset hashes. The focused native test proves Escape cannot select an enabled successor action and emits no gameplay mutation. |
| `Gravebound_Development_Roadmap_v1.md` | Roadmap parent `GB-M03-06` is internally decomposed into repository slice `GB-M03-06D`, which now supplies the native readable death/Memorial boundary required before integrated death closure. `GB-M03-07`, `GB-M03-08`, normal route admission, and the final tester metrics remain closed for their owning packages. |

Approved [`SPEC-CONFLICT-009`](../spec-conflicts/SPEC-CONFLICT-009-m03-death-memorial.md) remains the binding death/memorial scope record. This audit closes presentation only; it does not weaken the atomic transaction or promote a player-visible normal route.

## Acceptance evidence

| Criterion | Evidence | Result |
|---|---|---|
| Durable acknowledgement gate | Local health zero projects only `Recording the final moment`; losses and recovery actions appear only after the validated committed-death/latest/summary sequence completes. Late duplicate prediction cannot erase or fabricate acknowledged state. | PASS |
| Exact death order and authority | One immutable `DeathSummaryPresentation` renders hero/class/level/lifetime/deed, lethal source, five-event timeline, network/Recall, `Lost`, `Preserved`, `Created`, and ordered actions. Stable IDs/numbers and the stored presentation revision remain available beside canonical display values. | PASS |
| Echo result is explicit | The Created card renders the authoritative `summary.echo_outcome` label (`Available`, `Dormant`, or not created) rather than a generic count, preserving the `GB-M03-13` result. | PASS |
| Native accessible surface | Bevy UI uses pinned fonts/assets, 14 px effective minimum text, UI scale 80%-150%, pointer/keyboard/controller focus metadata, disabled/focus/hover/pressed states, bounded wheel/page/controller scrolling, and focus-follow without hidden off-screen selection. | PASS |
| Recovery ownership | `Create Successor` is the largest action but remains structurally disabled until `GB-M03-07`. Trace, Memorial, character-select, retry, and back actions reflect the exact projection state; no store or promotional content exists. | PASS |
| Read-only Memorial | Account-bound raw cursors retain newest-first ordering and exact selection. All four evidence rows resolve to their own death ID, character snapshot, summary digest, Echo outcome, and trace; historical views cannot create a successor or initiate gameplay mutations. Escape is unit-locked to the enabled `Back` action even when a successor action is present and cannot emit a mutation command. | PASS |
| Closed content/portrait policy | Canonical Core death copy and source bindings are independently hashed. Every `Asset` portrait must map to the validated atlas before a native snapshot can replace the last safe view; `ExplicitlyAbsent` is rendered distinctly and never guessed. | PASS |
| Adverse presentation | Recoverable refresh/response loss preserves the last safe state; fatal/corrupt/foreign/content-mismatch paths fail closed; pagination, equal timestamps, duplicate IDs, evicted-ID replay, invalid anchors, continuation mismatch, and stale/wrong-kind responses are covered by focused tests. | PASS |
| Optimized visual matrix | The inspected [17-artifact manifest](../evidence/GB-M03-06D-visual-manifest.md) covers summary top/actions/trace, Memorial list/detail, waiting/error, 1280x720, 1920x1080, standard, and reduced-effects variants with executable/content/file hashes. | PASS |
| Scope remains closed | Normal player-visible permadeath, successor creation, extraction/Recall, route admission, Requiem encounters, telemetry, support mutation, commerce, Core promotion, and all M04+ behavior remain disabled. | PASS |

## Cumulative verification

- Focused death/Memorial/native suite on `92994f9`: 45 passed.
- Complete client target on `92994f9`: 152 passed.
- Local format and strict client/workspace Clippy: PASS.
- Local workspace tests, Core/FP content validation, deterministic replay twice, and optimized Windows construction: PASS.
- Independent implementation review found four P1/P2 issues (explicit Echo status, cursor-specific detail, non-overflow focus evidence, and portrait failure policy); all four were fixed and the targeted re-audit is P0-P2 clean.
- Independent closure review found three P2 documentation/evidence issues (`DTH-021` scope, Roadmap decomposition wording, and Escape-without-mutation proof); all three were corrected, the focused Escape test passes, and the targeted re-audit is P0-P2 clean.
- The local persistence wrapper was blocked at parse time by Windows antivirus after earlier local stages passed. Exact hosted CI `29471819642` ran the mandatory PostgreSQL death/restart/real-QUIC inventory and Windows release jobs without that local policy wrapper.

## Deferred ownership

`GB-M03-06E` owns the complete real-QUIC/PostgreSQL canonical signature, exact crash-boundary matrix, cross-process performance/resource evidence, and joint `06`/`02D`/`13` audits. `GB-M03-07` owns successor persistence and the two-confirmation handoff. `GB-M03-08` owns extraction and Emergency Recall. The final M03 route, 25 complete private-loop journeys, human comprehension/fairness metrics, telemetry, support, and platform gates remain open.

## Handoff

Proceed directly to `GB-M03-06E`: compose the completed `06A`-`06D` and `GB-M03-13` authorities into one authenticated real-QUIC/PostgreSQL canonical signature; close response loss, restart, crash, concurrency, rollback, performance, and cleanup evidence; then publish the joint parent audits. Keep normal death presentation and every later-owned route disabled until that integrated gate passes.
