# GB-M03-04A completion audit

## Result

PASS. Core levels 1-10, server-authoritative XP, account-wide first-clear ownership, durable progression, crash restoration, reliable projection, and the native progression HUD are complete. Later levels and the parent `GB-M03-04` package remain open.

## Three-authority review

| Authority | Implemented evidence |
|---|---|
| Canonical GDD | `PROG-001`-`003`, `CLS-002`, `CLS-020`, `SOC-010`-`011`, `UI-001`, `UI-003`-`004`, and `TECH-020`-`023` are represented by exact XP eligibility, level derivation, stat growth, nonhealing health rebuild, first-clear ownership, authoritative projection, accessible presentation, and entry-state crash restoration. |
| Content Production Specification | The independently hashed progression target contains the exact Core level curve, Grave Arbalist growth, source/profile bindings, stage enablement, and localization closure. Disabled profiles cannot enter the runtime authority. |
| Development Roadmap | This closes the progression slice of `GB-M03-04` with durable replay/concurrency/restart evidence. Items, vault, later levels, death, extraction, and the complete M03 route remain assigned to their owning packages. |

## Acceptance evidence

| Requirement | Evidence | Result |
|---|---|---|
| Exact progression content | Strict source types, independent BLAKE3 hashes, exact 1-10 cumulative XP, class growth, six source bindings, and stage allowlists fail closed on drift. | PASS |
| Deterministic authority | Integer eligibility, contribution, first-clear rounding, cap saturation, level derivation, and health rebuild run in `sim_core`; clients never author XP or levels. | PASS |
| Atomic durable awards | Serializable replay-first transactions bind evidence, source/content, progression, character level cache, and account-wide first clear. Identical retries return the stored result and changed reuse conflicts. | PASS |
| Crash restoration | Danger awards bind to the active restore point inside the transaction. Restore returns exact entry XP/level/health, advances the live version monotonically, revokes only bound receipts, and removes only first clears owned by those receipts. | PASS |
| Resolution precedence | Committed death/extraction and safe-location awards cannot be rolled back by the progression provider; revoked receipt replay returns the exact typed result. | PASS |
| Reliable projection and HUD | Owned living-character projections travel over the persistent reliable QUIC route. The native HUD uses text, numbers, segmented shape, and explicit low/critical/cap labels. | PASS |
| Real PostgreSQL behavior | The disposable fixture proves concurrent exactly-once danger awards, restore/replay, first-clear rollback, safe nonbinding, and committed-resolution precedence. The real-QUIC fixture proves the authoritative projection and restart path. | PASS |

## Verification

- [CI run 29236047011](https://github.com/MikeyPar/Gravebound/actions/runs/29236047011): format, warnings-denied lint, workspace tests, Windows release build, PostgreSQL progression restoration, and the persistent real-QUIC journey pass.
- PostgreSQL progression restore fixture: 1 passed with exact concurrency, crash, replay, first-clear, and precedence assertions.
- Protocol, simulation, content, server, bot, and native client regression suites pass on the same head commit.
- Local formatting, focused protocol/server tests, warnings-denied Clippy, and `git diff --check` pass. Local destructive PostgreSQL execution was not available; hosted CI supplied the authorized disposable database.

## Granular delivery

The slice was delivered through separate content, simulation, persistence, service, protocol, client, restore, test, and architecture commits. Live-audit corrections include `cd7917e` (PostgreSQL type decoding), `db873df` (bounded serializable XP retry), `8353c9f` (progressed roster validation), and the mandatory ordered PostgreSQL gate.

## Deferred parent scope

`GB-M03-04B`-`04G` retain the remaining item, inventory, vault, reward, and integrated lifecycle work. Levels 11-20, mastery, death, extraction/Recall, Core promotion, and normal-route activation remain closed.
