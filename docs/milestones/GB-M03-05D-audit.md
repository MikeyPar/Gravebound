# GB-M03-05D completion audit

## Result

PASS. The temporary Core Sepulcher Knight milestone, deterministic Veil Bargain offer, durable select/refuse authority, and accessible native shrine surface are atomic, replay-safe, restart-safe, content-bound, and fail closed outside the approved Core test route. Bargain mechanics, purge, life cleanup, Core promotion, and the normal route remain disabled.

## Three-authority review

| Authority | Implemented evidence |
|---|---|
| Canonical GDD | `BRG-001`-`005` govern the first Core slot, exactly three legal choices, refusal without penalty, no fourth slot, exact 10-Ash fallback, durable active acquisition order, affected-stat comparisons, and two-slot-to-one-slot Lantern Ash projection. |
| Content Production Specification | `CONT-014` owns the exact BLAKE3 preimage, unsigned score ordering, immutable ordered candidates, emergency unavailable cells, source-bound fallback, Core-only trigger, exact records/copy/assets, and `CONT-HUB-002` close-without-mutation behavior. |
| Development Roadmap | This closes the offer/shrine portion of `GB-M03-05` and its restart/idempotency requirements while leaving executable Bargain mechanics to `05E` and lifecycle/route closure to `05F`. |

## Acceptance evidence

| Requirement | Evidence | Result |
|---|---|---|
| Atomic Core milestone | The first level-5+ `miniboss.sepulcher_knight` clear in `layout.core_private_life_01` commits progression, one earned slot, immutable offer or exact fallback, receipt, and outbox together under serializable locks. A per-life milestone identity prevents a different reward UUID from earning another Core slot. | PASS |
| Exact deterministic offer | Candidate filtering enforces enabled Core content, active-ID exclusion, `BRG-004`, and caps. Golden vectors bind raw UUID bytes, length-prefixed immutable content version/ID, BLAKE3 score bytes, unsigned ordering, and ID tie break. Persisted candidates and scores are immutable. | PASS |
| Emergency and no-slot behavior | One/two candidates retain three explicit cells with non-color `UNAVAILABLE`; zero candidates resolve unavailable and award exactly 10 Ash once. No new/unfilled slot creates no offer and awards exactly 10 Ash once. | PASS |
| Durable decision | Open offers transition once to selected or refused. Selection revalidates authority and persists the active Bargain before success; refusal spends nothing and leaves the slot unfilled. Identical retries return the byte-identical stored receipt and changed material conflicts. | PASS |
| Reliable runtime | Protocol 1.10 bounds shrine view/select/refuse messages. The production persistent QUIC route reaches the real coordinator, survives a bound-server restart, replays exactly, and restores the same active Bargain with no open offer. Disabled/ephemeral routes return typed unavailable state. | PASS |
| Accessible native surface | Three ordered cells, explicit review/confirmation, refusal, in-flight lockout, pointer/keyboard/controller controls, local Escape/B cancel, bounded 720p scrolling, affected axes, and exact two-to-one belt comparison are deterministic and non-color-dependent. | PASS |
| Corruption and cleanup safety | Repository validators reject malformed state, versions, candidates, receipts, and cross-domain ownership. Disposable fixture cleanup removes Bargain descendants before account/character roots, including lineage-referencing offers. | PASS |

## Verification

- [Authoritative final run 29255591203](https://github.com/MikeyPar/Gravebound/actions/runs/29255591203) passes formatting, warnings-denied Clippy, all workspace tests, First Playable content validation, deterministic trace verification, generated-schema drift, the mandatory PostgreSQL transaction/restart suite, real QUIC identity/Bargain restart coverage, and the Windows release build.
- [Independent cleanup run 29244937692](https://github.com/MikeyPar/Gravebound/actions/runs/29244937692) passes the same complete gate at the cleanup-order fix immediately before the exact two-slot shrine correction.
- Mandatory PostgreSQL coverage includes two independent Bargain tests plus progression restore, Ash wallet, and the identity/real-QUIC restart fixtures. It proves concurrent identical award/selection/refusal replay, changed-material conflicts, exact stored receipts, terminal invariants, fresh-pool durability, and cross-suite disposable cleanup.
- Local focused protocol, server shrine, and native UI tests pass after the two-slot correction. `cargo fmt --all -- --check`, warnings-denied Clippy for the affected crates, and `git diff --check` pass.

## Visual evidence

- [`GB-M03-05D-shrine.png`](../evidence/GB-M03-05D-shrine.png), 1600x1200, SHA-256 `83B83C1B7413EA2C5CA5A0388E264531643C2357E1EDF9AC44F1782F445EE272`.
- [`GB-M03-05D-shrine-1280x720.png`](../evidence/GB-M03-05D-shrine-1280x720.png), SHA-256 `FD31BAA6E00854264D1B0B7BB30016C835E75D0413B6956ACD9614D48EAD73C1`.
- Both native captures were inspected for hierarchy, contrast, spacing, disabled-state clarity, supported-bound fit, scroll affordance, and visibility of all six Bargain actions. They intentionally show the ephemeral service-disabled state; actual choice, refusal, replay, and restart semantics are proven through the persistent PostgreSQL/QUIC fixtures rather than a visual mock.
- Windows Graphics Capture could not provide a later manual input recording on this host (`0x80004002`, no supported capture interface). No manual-input claim is made; deterministic client input tests cover pointer, keyboard, controller, cancellation, scrolling, and in-flight lockout.

## Delivery history

The slice was delivered as granular commits across exact scoring, normalized schema and ownership constraints, atomic milestone/Ash/decision repositories, protocol/runtime authority, native UI, PostgreSQL/QUIC evidence, responsive polish, cleanup ordering, and the final two-slot correction. ADR-035 records the transaction and deterministic-offer architecture.

## Deferred scope

`GB-M03-05E` owns Cinder Hunger, Bell Debt, and Lantern Ash mechanics plus persisted active-loadout compilation. `05F` owns durable death/retirement/safe-transfer/checkpoint lifecycle wiring, adversarial route closure, final connected audiovisual evidence, and telemetry completion. The 50-Ash purge, later paid Oath change, normal production milestone schedule, Core promotion, and normal player route remain fail closed.
