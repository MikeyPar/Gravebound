# GB-M03-03G sealed death-context planner evidence

## Authority

1. `Gravebound_Production_GDD_v1_Canonical.md`: `DTH-001`, `DTH-020`, `ECH-001`, and `TECH-021`-`023` require atomic permanent loss, stored death presentation, transaction-owned Echo projection, and one authoritative danger lineage.
2. `Gravebound_Content_Production_Spec_v1.md`: `CONT-ECHO-001`, `CONT-HUB-002`, `CONT-WORLD-001`, and `CONT-ROOM-007` define exact Echo arithmetic, stored Memorial authority, the Core micro-realm, and fixed Bell route.
3. `Gravebound_Development_Roadmap_v1.md`: `GB-M03-03`, `GB-M03-06`, and `GB-M03-13` require the ordinary private route, durable death, and Echo projector to compose without client-authored terminal state.

## Implemented boundary

Commit `234fa2c` extends the irreversible death transaction's item locks with nullable item level and rarity, recomputes the exact weighted `CONT-ECHO-001` band from locked equipment, and rejects an altered planned band before any loss commits. Missing slots contribute zero, round-half-up remains integer-only, and the calculation recognizes the Black Unique `+30` authority even though Core does not roll that rarity.

Commit `ff22530` adds one coherent read-only PostgreSQL planning snapshot. It binds the authenticated selected living character to the exact active danger lineage and restore point; verifies current character, progression, inventory, Oath/Bargain, and life versions; verifies the latest lethal-tick clock receipt and danger-entry rollback root; and reads destructive custody, run materials, typed deeds, and account Echo queue state under the same database snapshot. The later death write still locks and revalidates every pre-version.

Commit `3238a0a` moves the complete simulation-to-journal entity map into `PreparedTerminalLiveDamageTrace`. The durable builder now consumes that sealed map rather than accepting a separately authored identity authority.

Commit `8d71a04` adds `PostgresPrivateDeathContextPlanner`. Its only successful output is `PreparedDurableDeathCommit`. It validates the opaque danger authority, exact actor generation and route frame, world content hashes, lethal trace, current character version, fixed Core layout, and room identity; reconstructs death clocks and reward-qualified deeds; plans canonical custody; allocates server-side UUIDv7 identities; derives Echo eligibility, queue promotion, deed tags, and power; and delegates final DTO sealing to the existing durable-death builder. The dormant persistent foundation now constructs this planner and all immutable content dependencies before normal admission can exist.

## Production-blocking verification

Per the owner's instruction, broad workspace, hosted journey, performance, and visual suites remain deferred until GB-M03 implementation is complete. The changed critical contracts passed:

- strict `persistence` and `server_app` library Clippy with warnings denied;
- focused commit-time Echo boundary arithmetic;
- focused coherent-snapshot request/version/location helpers;
- focused lethal-trace identity sealing and missing-identity rejection;
- focused planner Echo boundary arithmetic, including Black Unique `+30` handling;
- formatting and whitespace validation.

No hosted PostgreSQL, real-QUIC, 25-journey, or full workspace result is claimed by this record.

## Current Next Step

The sealed planner is installed in the mandatory production owner by commit `8cd626e`; commit `50d858a` supplies the same-boundary Recall projection. Next add the coherent current-terminal snapshot and route extraction, explicit/LinkLost Recall, disconnect recovery, and verified server-fault restoration beside death through the same coordinator. Keep normal admission disabled until exit-ready heartbeats, three-scene restart, exact replay, cancellation, and zero-residue shutdown proof pass.
